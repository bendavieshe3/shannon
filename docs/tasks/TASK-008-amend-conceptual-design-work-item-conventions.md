# TASK-008: Amend conceptual_design.md with Work-Item Conventions and Version History Entry

## Metadata

- **Status**: ELABORATED
- **Type**: Task
- **Parent**: [EPIC-007](../epics/EPIC-007-work-item-conventions-from-dogfooding.md)
- **Feature**: [FEAT-003](../features/FEAT-003-unified-work-item-model.md)
- **Tags**: #framework #conceptual-design #conventions #work-items
- **Created**: 2026-05-24
- **Updated**: 2026-05-24 (elaborated)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

*Initial intent captured at `/task-create` (2026-05-24). Full Requirements drafted at `/task-elaborate TASK-008`.*

### Overview

**Task 1 of EPIC-007.** This Task lands the four `conceptual_design.md` amendments that codify the work-item-workflow conventions surfaced during EPIC-005 / EPIC-006 dogfooding, plus the Version History entry recording the additive bump (`v1.5 → v1.6`).

The four amendments — locations definitively resolved at EPIC-007 § Plan (implementing Task may override with recorded rationale):

- **EPIC-007 AC#1 — Commit-hash timing.** *Inline* clarification within § Re-elaborating a Work Item → *Flow* step 6 (expanding the existing "where applicable" phrase, not adjoining it).
- **EPIC-007 AC#2 — Bundled re-elaboration.** *New sub-bullet* attached to § Re-elaborating a Work Item → *Flow* step 5 (where Gate-1 satisfaction semantics already live).
- **EPIC-007 AC#3 (doc-half) — Descriptive Work-Item naming convention.** Appended to the *Work Item* entity in § Domain Model (as a naming note immediately following the existing **Attributes** list).
- **EPIC-007 AC#4 (doc-half) — Scope-Boundary AC convention.** New § Business Rules entry titled *"Scope-Boundary Acceptance Criteria Use Cross-Type Guards"*.

**Sibling Task** (EPIC-007 Task 2 — `work-items` skill prompts at § Process: Plan steps 2 and 3 + `.claude/` re-sync) lands after this Task and cross-references the section anchors this Task creates. Must rebase against EPIC-008 Task 2 if that lands first (both edit `shannon/skills/work-items/skill.md`) — but that sequencing concern does not affect this Task.

**Cross-Epic forward reference** at AC#6's fourth pair (scope-guard rule → EPIC-008 AC#1 editing-order convention in `development_guide.md` § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts*): the EPIC-008-side anchor **already exists** as of 2026-05-24 — `development_guide.md` v1.3 landed via TASK-007 (APPROVED, commit `eac486b`), and the editing-order bullet at line 79 includes a forward HTML comment pointing back at this Task's about-to-be-created scope-guard rule (per EPIC-008 Plan Risk 7). When this Task lands, the forward-only state in `development_guide.md:79` self-heals — the bidirectional cross-reference becomes complete.

### Acceptance Criteria

*Drafted at `/task-elaborate TASK-008` (2026-05-24) — anchors confirmed against the live `docs/conceptual_design.md` v1.5 (Last Reviewed / Approved 2026-05-21). Each criterion names a verifiable outcome — the section, the surrounding anchor, and the required verbatim wording where AC text demands it. Closes EPIC-007 AC#1 (AC1–AC2 below), AC#2 (AC3), AC#3 doc-half (AC4), AC#4 doc-half (AC5), AC#5 (AC6 + AC10), AC#6 outbound half (AC7–AC9); AC11 covers additivity verification; AC12 covers the scope-boundary guard (a pleasing recursion: AC12 disciplines this Task's own scope assertion by the very rule it ratifies as AC5); AC13 covers the no-commit-mid-implementation rule.*

- [ ] **AC1 — Commit-hash timing clarification lands inline at § Re-elaborating a Work Item → *Flow* step 6.** § Re-elaborating a Work Item *Flow* step 6 (currently line 275: *"Activity Log entry is added recording **(a) trigger category** … **(b) upstream commit hash** where applicable, and **(c) summary of what changed**"*) is expanded inline at the existing **"where applicable"** phrase. The expansion states two things in this order: **(i)** when the re-elaboration is bundled inside a Task whose own commit lands after Gate 3 (per `development_guide.md` § Commit Cadence — commits land after Gate 3, not mid-implementation), the entry **cannot** cite its own work item's commit hash because that hash does not exist at write-time; **(ii)** the entry instead cites the **upstream commit hash** — the hash that delivered the change being absorbed — and **references the bundling work item by ID**. The clarification names **TASK-003** explicitly as the worked example (the case where this exact pattern was exercised live, citing upstream `09844df` and referencing TASK-003 by ID). The existing **"where applicable"** phrase is preserved — the new prose expands it, does not remove or replace it. The bold-italic markers around `(a) trigger category`, `(b) upstream commit hash`, `(c) summary of what changed` are preserved. Verifiable by `grep`: `grep -n "upstream commit hash" docs/conceptual_design.md` returns at least one hit at step 6's anchor; `grep -n "where applicable" docs/conceptual_design.md` continues to return step 6 (phrase preserved); `grep -n "reference.*by ID" docs/conceptual_design.md` returns one hit (the new clarification); `grep -n "TASK-003" docs/conceptual_design.md` returns at least one hit at step 6's anchor.

- [ ] **AC2 — Commit-hash clarification carries the back-reference to AC#2's bundled re-elaboration clause.** The new AC1 inline prose carries a back-reference naming the bundled re-elaboration sub-bullet (the AC3 sibling, landed at § Re-elaborating *Flow* step 5) as the typical surrounding context for the bundled-Task case. The back-reference uses a section pointer (e.g. *"see the bundled re-elaboration sub-bullet on step 5 above"*) so the AC#1 ↔ AC#2 pair (per EPIC-007 AC#6's first cross-reference pair) is bidirectional within `conceptual_design.md`. Both anchors land atomically within this Task — no forward-only state.

- [ ] **AC3 — Bundled re-elaboration sub-bullet lands at § Re-elaborating a Work Item → *Flow* step 5.** § Re-elaborating *Flow* step 5 (currently line 274: *"Implementer applies the approved refinements; directing party explicitly approves the new state via **Gate 1**"*) gains a new sub-bullet immediately beneath it (indented under step 5 in the existing numbered list). The sub-bullet text contains the phrase **"bundled re-elaboration"** verbatim and states: **(i)** multiple work-item re-elaborations may be applied inside a single Task's implementation; **(ii)** the Task's own Gate 2 and Gate 3 serve as the per-item directing-party refinement-approval the workflow's step 5 otherwise requires; **(iii)** this is permitted **only when** **(a)** the bundle is *additive* per § Status semantics and **(b)** the parent (Epic or Task) **explicitly enumerates** the re-elaborated work items by ID in its Plan or Acceptance Criteria. The sub-bullet cross-references AC1's commit-hash clarification by section pointer to step 6, and cites **TASK-003** as the worked precedent (the case where bundled re-elaboration was exercised live). Verifiable: `grep -n "bundled re-elaboration" docs/conceptual_design.md` returns at least one hit at step 5's anchor; the three permission conditions (Gate 2 / Gate 3, additive, parent enumerates by ID) are textually present; the cross-reference to step 6 and citation of TASK-003 are present.

- [ ] **AC4 — Naming convention against opaque plan-letter labels lands as a naming note after the Attributes list on the *Work Item* entity in § Domain Model.** The *Work Item* entity (lines 45–62) gains a brief naming note immediately following the existing **Attributes** list (closing at line 56 with `- **Activity Log** — Timestamped history`) and before the existing **Relationships** block (line 58). The note is a one-paragraph addition, not a sixth attribute. Text: *"Child work items in Epic plans are named by descriptive title (and by real ID once allocated) — never by opaque plan-letter labels (TASK-A, TASK-B, TASK-C). Ordering, when relevant, is expressed by position language ('the first Task', 'the verification Task'). Letter placeholders that appear in transient planning notation must not propagate into the Epic § Plan, child work-item files, indexes, or downstream prose."* The note carries a forward cross-reference to the matching `work-items` skill prompt at `shannon/skills/work-items/skill.md` § Process: Plan step 3 (*Cascading Preparation (for Epics)*) — landed by EPIC-007 Task 2; the cross-reference is permitted to land pointing at the named-but-not-yet-present anchor with intra-Epic forward-reference treatment per TASK-007 AC7 precedent (no verifiable-anchor HTML comment required — intra-Epic dependency inside the directing party's planned sequence). Verifiable: `grep -n "TASK-A, TASK-B" docs/conceptual_design.md` returns one hit at the *Work Item* entity anchor; `grep -n "the first Task" docs/conceptual_design.md` returns at least one hit (the position-language alternative); the cross-reference to `shannon/skills/work-items/skill.md` § Process: Plan step 3 is textually present.

- [ ] **AC5 — Scope-Boundary AC convention lands as a new § Business Rules entry titled *Scope-Boundary Acceptance Criteria Use Cross-Type Guards*.** A new bullet is inserted into § Business Rules (lines 153–177), placed **after *Approved Tasks Are Archived* (line 175)** and before *DRAFT Documents Are Not Authoritative* (line 177) — among the work-item lifecycle / bookkeeping rules and before the broader document-authority rule. The bold lead is exactly **"Scope-Boundary Acceptance Criteria Use Cross-Type Guards"** (the exact title `development_guide.md:79` already references — landing this title verbatim self-heals the dangling forward HTML comment landed by TASK-007). The rule's text matches EPIC-007 AC#4's quoted block: *"When a work-item Acceptance Criterion expresses scope (what is not touched), phrase it as a guard against changes to other types or layers — e.g. 'no epic/task/spike template, index, or skill modified'. Do not enumerate a literal file count or a fixed file list as the scope assertion: work-item transitions necessarily touch routine bookkeeping files (work-item index entries, parent work-item Tasks-line entries) that literal counts and fixed enumerations cannot anticipate."* The rule includes the second paragraph distinguishing edit-target ACs from scope-guard ACs and **the failure-mode test phrasing letter-for-letter**: *"if the AC's failure mode is 'an unintended file changed', it is a scope-guard AC and must use the cross-type-guard form"*. The scope-of-applicability parenthetical (test phrasing general; rule scoped to work-item ACs; non-work-item scope assertions outside this Epic's mandate) is preserved. The rule cites the three-instance precedent: **TASK-002 AC#6** (the "only two files modified" literal-count contradiction with AC#5), **TASK-002 AC#8** (mis-worded example grep), **TASK-003 AC#9** (enumerated six files when the diff carried eight). The rule carries a forward cross-reference to the matching `work-items` skill prompt at `shannon/skills/work-items/skill.md` § Process: Plan step 2 (*Spawn Planning Subagent*, AC-drafting bullet) — landed by EPIC-007 Task 2; intra-Epic forward-reference treatment as AC4 above. The rule also carries the bidirectional back-reference to **EPIC-008 AC#1's editing-order convention** in `development_guide.md` § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts* (line 79). This back-reference completes the cross-Epic pair whose EPIC-008 side already exists at `development_guide.md:79` and self-heals on landing. Verifiable: `grep -n "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" docs/conceptual_design.md` returns one hit at § Business Rules; `grep -n "if the AC's failure mode is 'an unintended file changed'" docs/conceptual_design.md` returns one hit (failure-mode test verbatim); `grep -n "TASK-002" docs/conceptual_design.md` returns at least one hit (precedent citation) and `grep -n "TASK-003" docs/conceptual_design.md` returns at least the AC1-anchor hit plus the AC#9 precedent citation. The cross-reference target `development_guide.md` § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts* is named verbatim.

- [ ] **AC6 — Version History entry `v1.5 → v1.6` lands at the top of § Version History.** A new `### YYYY-MM-DD - v1.6` entry (date = implementation date) is inserted at the top of § Version History, above the existing `### 2026-05-21 - v1.5` entry at line 348, preserving the file's most-recent-first ordering (precedent at lines 348 / 356 / 364 / 371 / 376 / 386 — v1.5 above v1.4 above v1.3 above v1.2 above v1.1 above v1.0). The entry satisfies EPIC-007 AC#5's four must-requirements: **(i)** names **EPIC-007** by ID as the source ("Per EPIC-007 …" or equivalent); **(ii)** lists each amendment by section anchor — § Re-elaborating a Work Item *Flow* step 6 (AC1: commit-hash clarification); § Re-elaborating a Work Item *Flow* step 5 (AC3: bundled re-elaboration sub-bullet); § Domain Model *Work Item* entity Attributes (AC4: naming note); § Business Rules *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* (AC5: new entry); **(iii)** classifies the bump verbatim as *"additive amendment per § Re-reviewing"* (matching EPIC-007 AC#5 wording); **(iv)** records the new Approved date matching the top-of-file Approved date update (AC10). The entry follows the formatting precedent of the v1.5 entry (lines 348–354): date header, bulleted change list, and a trailing `Status: APPROVED (YYYY-MM-DD)` line. Verifiable: `grep -n "### .* - v1\." docs/conceptual_design.md` returns v1.6 at a lower line number than v1.5 (chronological ordering preserved); `grep -n "additive amendment per" docs/conceptual_design.md` returns at least one new hit (the v1.6 entry classification); `grep -n "EPIC-007" docs/conceptual_design.md` returns at least one hit in § Version History.

- [ ] **AC7 — AC#1 ↔ AC#2 bidirectional cross-reference is explicit.** The bundled re-elaboration sub-bullet (AC3 above) references the commit-hash clarification (AC1 above) by section pointer to step 6; the commit-hash clarification (AC1) references the bundled re-elaboration sub-bullet (AC3) by section pointer to step 5 (per AC2 above). Both directions land atomically within this Task — no forward-only state. Verifiable: each anchor's prose contains a textual pointer naming the other's section.

- [ ] **AC8 — Naming convention → skill prompt cross-reference is named.** The naming note appended to the *Work Item* entity (AC4 above) names the target file path (`shannon/skills/work-items/skill.md`) and the section anchor (§ Process: Plan step 3 *Cascading Preparation (for Epics)*) where the matching prompt will be landed by EPIC-007 Task 2. The cross-reference is forward (intra-Epic — Task 2 follows this Task strictly). No verifiable-anchor HTML comment is required because Task 2 is inside the directing party's planned sequence within EPIC-007 (mirroring TASK-007 AC7's intra-Epic treatment).

- [ ] **AC9 — Scope-guard rule → skill prompt cross-reference is named.** The Scope-Boundary AC entry (AC5 above) names the target file path (`shannon/skills/work-items/skill.md`) and the section anchor (§ Process: Plan step 2 *Spawn Planning Subagent*, AC-drafting bullet) where the matching prompt will be landed by EPIC-007 Task 2. The cross-reference is forward (intra-Epic) and follows the same treatment as AC8. The cross-reference uses the **failure-mode test phrasing verbatim** in the pointer prose so the skill-half prompt has the canonical phrase to repeat.

- [ ] **AC10 — Top-of-file metadata is updated to v1.6.** The `**Status**: APPROVED` line at line 3 stays **APPROVED** — additive amendments do not require DRAFT transition per `conceptual_design.md` § Re-reviewing → *Status semantics* (lines 249–254) and per EPIC-007 AC#5. The `**Last Reviewed**` and `**Approved**` dates (currently `2026-05-21`, lines 4–5) are updated to the implementation date matching AC6's Version History entry. The file does not transition to DRAFT at any point during this Task.

- [ ] **AC11 — Additivity verification performed and recorded.** Per `conceptual_design.md` § Re-reviewing an APPROVED Mandated Document → *Status semantics* (lines 249–254): an amendment is *additive* iff it does not contradict existing approved claims; the document stays APPROVED through the edit. The implementing Task verifies — by reading each new piece of prose (AC1 commit-hash clarification, AC3 bundled re-elaboration sub-bullet, AC4 naming note, AC5 Scope-Boundary § Business Rules entry, AC6 Version History entry, AC10 metadata refresh) against the existing prose of the same sections — that no new prose contradicts any existing approved claim. The verification is recorded in Implementation Notes as a single sentence per amendment (e.g. "AC1 commit-hash clarification — additive: existing 'where applicable' is silent on the bundled-Task case; new prose expands the hook without revising it"). If any amendment turns out non-additive (unexpected), the Task pauses and the implementer flags the contradiction to the directing party rather than landing silently — mirroring TASK-007 AC10's protocol.

- [ ] **AC12 — Scope is bounded by the cross-type guard codified by this Task itself.** This Task modifies exactly the following files: `docs/conceptual_design.md` (the substantive amendments per AC1–AC10); `docs/tasks/TASK-008-amend-conceptual-design-work-item-conventions.md` (Implementation Notes, Review, Activity Log — standard work-item bookkeeping); `docs/tasks/task_index.md` (status transitions and the Tasks list entry — standard bookkeeping); `docs/epics/EPIC-007-work-item-conventions-from-dogfooding.md` (the § Plan → Tasks entry for this Task, updated to reflect APPROVED). **No other file is modified.** Specifically out of scope: any file under `shannon/skills/` (EPIC-007 Task 2's territory); any file under `.claude/` (Task 2's re-sync territory); any other mandated document (`vision.md`, `technology_stack.md`, `technical_design.md`, `development_guide.md`, `ux_guide.md`); any work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` (Epic Gate 3 review work per EPIC-007 AC#7); `docs/knowledge/` (single-sourced in EPIC-008 AC#7's knowledge note; not EPIC-007 Task work). **This AC is phrased as a cross-type guard per the very rule this Task codifies as AC5 — *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* — not as a literal file count.** A pleasing recursion: the Task that ratifies the scope-guard rule disciplines its own scope assertion by it. Verified at Gate 3 by `git status` against the cross-type guard.

- [ ] **AC13 — Implementation does not commit mid-implementation.** Per `development_guide.md` § Commit Cadence, the implementer does not commit until Gate 3 approval. Pre-Gate-3 experimentation is fine — multiple in-progress diffs are expected — but no `git commit` runs before Gate 3 approval lands. The Gate-3 commit also triggers a push per `development_guide.md` § Push Cadence trigger (a) — after every Gate 3 approval (the rule TASK-007 itself ratified at commit `eac486b`). This AC governs the Task's own execution, not its output; verified at Gate 3 by checking `git log master..HEAD` for any pre-Gate-3 commit on this Task's diff range — expectation: zero such commits.

### Context

- **Parent Epic**: [EPIC-007 — Work-Item Conventions Surfaced Through Dogfooding](../epics/EPIC-007-work-item-conventions-from-dogfooding.md) — this is Task 1 of 2; closes EPIC-007 AC#1, AC#2, AC#3 doc-half, AC#4 doc-half, AC#5 (additive Version History entry), AC#6 outbound (cross-references whose origin lives in `conceptual_design.md`). EPIC-007 AC#3 skill-half, AC#4 skill-half, AC#6 inbound are Task 2; AC#7 (scratchpad bookkeeping) is Epic Gate 3 review work.
- **Parent Feature**: [FEAT-003 — Unified Work Item Model](../features/FEAT-003-unified-work-item-model.md) — Ideal State bullet 4 (added 2026-05-24 re-elaboration) anchors EPIC-007; this Task delivers the `conceptual_design.md` half of bullet 4.
- **Mandated document amended**: `docs/conceptual_design.md` v1.5 (current APPROVED). Specific anchor points to confirm at elaboration: § Re-elaborating a Work Item *Flow* (for AC#1 step-6 inline + AC#2 step-5 sub-bullet); § Domain Model *Work Item* entity Attributes (for AC#3); § Business Rules (for AC#4 new entry); § Version History (for AC#5).
- **Sibling Task within EPIC-007** (Task 2): not yet created; follows this Task; will add the matching `work-items` skill prompts at § Process: Plan steps 2 and 3 and re-sync `.claude/skills/work-items/`.
- **Cross-Epic sibling**: EPIC-008's TASK-007 (APPROVED 2026-05-24, commit `eac486b`) is the landed counterpart for the cross-Epic AC#6 pair — `development_guide.md` v1.3 § Code Style → Patterns to Follow line 79 already carries the forward reference and HTML comment that self-heal when this Task lands AC#4's § Business Rules entry.
- **Source artefacts** (the scratchpad items and prior session experience that motivate the conventions): the scratchpad sub-items addressed by this Task — *"Framework clarifications buried in TASK-003's Plan"* sub-items (b) commit-hash timing and (c) bundled re-elaboration; *"Naming convention: avoid opaque plan-letter labels (TASK-A/B/C) for Tasks"*. Item 4 (cross-type scope-guard) has no scratchpad entry — sourced from archived TASK-002 Review (AC#6 internal contradiction; AC#8 mis-worded example grep) and TASK-003 Review ("Third instance of literal-file-count fragility").

---

## Plan

*To be filled at `/task-plan TASK-008` (Gate 2).*

### Approach

*[Will follow the TASK-007 precedent — bottom-to-top edit order for line-count-changing insertions, additivity verification per amendment, scope-confined-diff verification.]*

### Steps

*[Drafted at planning.]*

### Dependencies

- EPIC-007 PLANNED (met).
- `conceptual_design.md` v1.5 APPROVED (to confirm at elaboration — the document being additively amended).
- TASK-007 (EPIC-008 Task 1) APPROVED — the cross-Epic forward reference at `development_guide.md:79` exists and will self-heal when this Task lands AC#4 (met as of commit `eac486b`).

### Risks

*[Drafted at planning. Likely to mirror TASK-007's risk set with item-specific adjustments.]*

---

## Implementation Notes

*Filled during implementation.*

---

## Review

*Filled during review (Gate 3).*

### Verification

- [ ] All acceptance criteria met
- [ ] Changes follow development_guide.md
- [ ] Relevant documents updated
- [ ] Knowledge captured where useful

### Review Notes

*Filled during review.*

---

## Activity Log

- **2026-05-24** — ELABORATED via Gate 1. Elaboration subagent grounded each AC in the live `conceptual_design.md` v1.5 (Last Reviewed / Approved 2026-05-21). Confirmed anchor points: § Re-elaborating a Work Item *Flow* step 6 at line 275 (existing "where applicable" hook for AC1's inline expansion); *Flow* step 5 at line 274 (Gate-1 satisfaction semantics for AC3's sub-bullet); *Work Item* entity in § Domain Model at lines 45–62 (Attributes 49–56 → naming note → Relationships 58–62 for AC4); § Business Rules at lines 153–177 (AC5 insertion after *Approved Tasks Are Archived* at line 175, before *DRAFT Documents Are Not Authoritative* at line 177 — among the work-item lifecycle/bookkeeping cluster); § Version History top entry `### 2026-05-21 - v1.5` at line 348 (AC6 inserts v1.6 above); top-of-file metadata at lines 3–5 (Status APPROVED stays; dates 2026-05-21 → implementation date); § Re-reviewing → *Status semantics* at lines 249–254 (AC11 additivity test source). All four EPIC-007 "implementing Task may override" hooks resolved either at EPIC-007 Plan (AC#1/2/3/4 location decisions) or at elaboration (AC5's specific bullet-cluster position). **Cross-Epic forward-reference target confirmed verbatim** at `development_guide.md:79` — the exact bold lead text `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` is already named there; AC5 landing that exact title atomically self-heals the dangling HTML comment landed by TASK-007 (commit `eac486b`). 13 testable ACs drafted (AC1–AC13), traced 1:1 to EPIC-007 ACs: AC1–AC2 → EPIC-007 AC#1 (commit-hash inline + bidirectional back-reference to AC#2); AC3 → AC#2 (bundled re-elaboration sub-bullet); AC4 → AC#3 doc-half (naming convention); AC5 → AC#4 doc-half (scope-guard § Business Rules entry); AC6 + AC10 → AC#5 (Version History v1.6 entry + top-of-file metadata refresh); AC7–AC9 → AC#6 outbound (intra-file AC#1↔AC#2 explicit, plus the two intra-Epic forward references and the cross-Epic back-reference); AC11 covers additivity verification; AC12 covers the cross-type scope-guard (a pleasing recursion — this Task disciplines its own scope assertion by the very rule it ratifies as AC5); AC13 covers no-commit-mid-implementation. Findings: all Strength; one minor Gap captured by lifting AC2 into a discrete verifiable AC for symmetric back-reference checking; no Drift surfaced. Directing party approved without modification.
- **2026-05-24** — DRAFT: Task created via `/task-create` under EPIC-007 as Task 1 of 2 (the `conceptual_design.md` amendments half). EPIC-007's Plan resolved all four definitive location decisions (AC#1 → inline at § Re-elaborating *Flow* step 6; AC#2 → sub-bullet on *Flow* step 5; AC#3 → naming note after Attributes on *Work Item* in § Domain Model; AC#4 → new § Business Rules entry titled *"Scope-Boundary Acceptance Criteria Use Cross-Type Guards"*) and named out-of-scope items (AC#3 skill-half + AC#4 skill-half + AC#6 inbound → EPIC-007 Task 2; AC#7 scratchpad bookkeeping → Epic Gate 3). Initial intent captured in the Overview; Acceptance Criteria and Plan to be drafted at `/task-elaborate` and `/task-plan`. This Task is independent of EPIC-007 Task 2 (different file) and of EPIC-008's TASK-007 (which is already APPROVED — the cross-Epic forward reference at `development_guide.md:79` self-heals when this Task lands AC#4). This Task is the converse of TASK-007 — TASK-007 amended `development_guide.md`; this Task amends `conceptual_design.md`; together they close the FEAT-003 / FEAT-008 dogfooding-harvest pair.
