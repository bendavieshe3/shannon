# TASK-015: Report Writer and Knowledge Index Updater

## Metadata

- **Status**: APPROVED
- **Type**: Task
- **Parent**: [EPIC-009](../epics/EPIC-009-health-reporting-surface.md)
- **Feature**: [FEAT-009](../features/FEAT-009-supervisor.md)
- **Tags**: #supervisor #report-writer #templates #knowledge-index #hybrid-presentation
- **Created**: 2026-05-30
- **Updated**: 2026-06-27

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

*Prepared during EPIC-009 planning (Gate 2), not yet reviewed. Surfaces at `/task-elaborate TASK-015` (Gate 1).*

### Overview

Implement the report-writing pipeline that aggregates the three checkers' finding fragments into a single dated report at `./docs/supervisor/report-YYYY-MM-DD.md` and indexes that report in `./docs/knowledge_index.md` under Type *Supervisor Report*. The pipeline is the core of EPIC-009's user-visible output — the dated report is the supervisor's durable artefact per `technical_design.md` v1.2 § Data Model → *Supervisor Report Files*. The report's presentation follows the hybrid default per `ux_guide.md` v1.2 § Interaction Patterns → *Supervisor Report Presentation*: a diagnostic header (counts) followed by a one- or two-finding narrative body. This Task closes the writer/index-updater half of parent Epic AC#3 (first report written successfully); the dogfood verification of that AC is TASK-018's scope. Out of scope: the three checker definitions themselves (TASKs 12–14); the two hooks (TASKs 16–17); the dogfood run that verifies the pipeline end-to-end (TASK-018); the zero-findings degenerate case (EPIC-010 territory per parent Epic § Overview's Out-of-scope statement).

### Acceptance Criteria

- **AC#1 — Report templates exist under `./shannon/skills/shannon-supervisor/templates/` (deployed to `./.claude/`).** The `templates/` subdirectory (created by TASK-011 in the deployed copy; established in the tracked `./shannon/` source by TASK-019) is populated with the three template fragments named at `technical_design.md` v1.2 § System Architecture → *Supervisor* → *Skill directory layout*: a header template (run date, checker counts, push lag — the diagnostic-header half of the hybrid default), a finding-section template (per-finding shape using the four-category schema with section/line citation), and a footer template (closing prose). Authored in the tracked `./shannon/` source, then deployed to the active `./.claude/` copy. Derives from `technical_design.md` v1.2 § System Architecture → *Supervisor* → *Skill directory layout* and `ux_guide.md` v1.2 § Interaction Patterns → *Supervisor Report Presentation*.

- **AC#2 — Report-writing pipeline codified in `SKILL.md` references the templates.** `./shannon/skills/shannon-supervisor/SKILL.md` (the tracked source established by TASK-019, originally created by TASK-011 with the `/shannon-report` contract; deployed to `./.claude/`) gains a § Report Pipeline section — inserted at the insertion point TASK-011 reserved, **between § Configuration and § Hook Integration** (per the `SKILL.md` line-41 forward-reference "appended below the Configuration section"), preserving the TASK-011 body order per the M-8 cross-Task editing-order discipline — that codifies the aggregation flow: spawn the three checkers (per TASKs 12–14), collect their finding fragments, instantiate header + finding sections + footer templates into a single Markdown body, write to `./docs/supervisor/report-YYYY-MM-DD.md`. The pipeline must honour same-day-suffix handling (`report-YYYY-MM-DD-2.md` for a second run on the same date) per `technical_design.md` v1.2 § Data Model → *Supervisor Report Files* ("Reports are never overwritten"). Derives from `technical_design.md` v1.2 § Data Model → *Supervisor Report Files* and `development_guide.md` v1.4 § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts* (M-8 insertion-point discipline).

- **AC#3 — Hybrid-presentation default lands in the report shape.** The instantiated report follows the hybrid default per `ux_guide.md` v1.2 § Interaction Patterns → *Supervisor Report Presentation*: a diagnostic header naming counts of findings, stuck items, push lag, followed by a one- or two-finding narrative body. Diagnostic-only and conversational-only presentations are valid customisations but the framework default is hybrid; this Task lands the hybrid default in the templates. Derives from `ux_guide.md` v1.2 § Interaction Patterns → *Supervisor Report Presentation*.

- **AC#4 — `knowledge_index.md` write step codified under Type *Supervisor Report*.** The § Report Pipeline codifies a step that writes an entry into `./docs/knowledge_index.md` for each report the pipeline produces, with the Type field reading *Supervisor Report* (per `conceptual_design.md` v1.7 § Domain Model → *Knowledge Note* — Supervisor Report is a Knowledge Note subtype) and the entry referencing the report by relative path. **At this Task's scope, AC#4 is satisfied by codifying the write step in the § Report Pipeline prose** (naming the Type-field value "Supervisor Report" and the relative-path reference) — not by writing an actual `knowledge_index.md` entry now, since no report exists to index until the pipeline first runs. The first real `knowledge_index.md` entry is written at runtime by the TASK-018 dogfood run. The PreToolUse hook (TASK-016) must permit this runtime write — it is the explicit exception named at parent Epic AC#4 (R-3). Derives from `conceptual_design.md` § Domain Model → *Knowledge Note* and `technical_design.md` v1.2 § Data Model → *Supervisor Report Files* ("indexed in `knowledge_index.md` like any other note").

- **AC#5 — Project-relative paths throughout.** Same portability discipline as parent Epic AC#1 (G-F extension): templates and any pipeline prose in `SKILL.md` use project-relative paths only. Verifiable: literal grep of the authored `./shannon/skills/shannon-supervisor/templates/*.md` plus the new `./shannon/skills/shannon-supervisor/SKILL.md` § Report Pipeline section (and their deployed `./.claude/` copies) for `FEAT-`, `EPIC-`, `Shannon-self`, `/Volumes/`, `~/`, and this repository's absolute path returns zero hits. The six-target grep set matches the lived precedent at TASK-012/013/014 Gate 3 (three consecutive clean checker passes). Derives from parent Epic AC#1 (G-F portability extension) and TASK-012/013/014 AC#5 precedent.

- **AC#6 — Plain-prose substring discipline honoured.** Any verbatim phrase the templates commit to grep-verifying (the four-category schema labels, the hybrid diagnostic-header format) lands as plain prose — no Markdown formatting interposing between substring characters. Per parent Epic AC#7. Derives from parent Epic AC#7 and the scratchpad TASK-009 lesson (2026-05-25).

- **AC#7 — Scope-bounded edit per cross-type-guard rule; authored in the tracked shipping source.** Per `conceptual_design.md` § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards*, and per the directing-party decision at this Task's Gate 1 (scratchpad shipping-source-mirror item, DECISION 2026-06-27): this Task authors its deliverables in the **tracked** shipping source `./shannon/skills/shannon-supervisor/` — creating the three template fragments under `./shannon/skills/shannon-supervisor/templates/` and extending `./shannon/skills/shannon-supervisor/SKILL.md` with the § Report Pipeline section at the reserved insertion point (per AC#2) — then deploys the same content to the active copy under `./.claude/skills/shannon-supervisor/` so the running skill stays in sync. It does **not** modify any checker definition under `checkers/` (TASKs 12–14 territory, all APPROVED); does **not** modify any hook configuration or `settings.json` (TASKs 16–17 territory); does **not** write to `./docs/supervisor/` (TASK-018's dogfood run is the first writer to that path); and does **not** itself write a `./docs/knowledge_index.md` entry — the knowledge_index write is codified as a pipeline step in the § Report Pipeline prose (per AC#4) and first exercised at runtime by TASK-018, since no report exists to index until then. Because `./shannon/` is tracked (not gitignored like `./.claude/`), the scope-bounded edit is verifiable by normal `git diff`. Depends on TASK-019 establishing `./shannon/skills/shannon-supervisor/SKILL.md` as the tracked source first. Derives from `conceptual_design.md` § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards*, the scratchpad shipping-source-mirror DECISION (2026-06-27), and TASK-019 (tracked-source establishment).

### Context

- **Parent Epic**: [EPIC-009 — Health Reporting Surface](../epics/EPIC-009-health-reporting-surface.md) — Task 5 of 8; closes the writer/index half of EPIC-009 AC#3; AC#3's first-report dogfood verification is TASK-018's scope.
- **Parent Feature**: [FEAT-009 — Supervisor](../features/FEAT-009-supervisor.md) — § Ideal State *Hybrid report presentation by default*; § Measurable Targets *Phase 2 adoption* (the first-report artefact is the Measurable Target's evidence — landed by TASK-018 against this Task's pipeline).
- **Sibling Task** (predecessor): TASK-011 (Skeleton — `SKILL.md` and `templates/` directory must exist); TASKs 12–14 (checker definitions — the pipeline aggregates their fragments).
- **Sibling Tasks** (downstream): TASK-016 (PreToolUse hook — must permit the `knowledge_index.md` write per AC#4); TASK-018 (Dogfood run — first end-to-end exercise).
- **Technical Design**: § Data Model → *Supervisor Report Files* (AC#2 and AC#4 source — same-day-suffix handling, knowledge_index integration); § System Architecture → *Supervisor* → *Skill directory layout* (AC#1 source); § Key Algorithms → *Document Alignment Check* → *Finding schema* (the four-category schema the per-finding template instantiates).
- **UX Guide**: § Interaction Patterns → *Supervisor Report Presentation* (AC#3 source — hybrid default).
- **Conceptual Design**: § Domain Model → *Knowledge Note* (AC#4 source — Supervisor Report subtype); § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* (AC#7 source).
- **Development Guide**: § Supervisor Report Files (the dogfood-specific commitment that reports are committed, not gitignored — relevant context for the pipeline's output).
- **Scratchpad**: M-2 (zero-findings degenerate-case presentation pattern — explicitly deferred to EPIC-010 per parent Epic § Overview; this Task does not implement zero-findings handling).

---

## Plan

*Ratified at Gate 2 (2026-06-27), reworked to the author-in-`./shannon/` convention.*

### Approach

Author in the tracked shipping source `./shannon/skills/shannon-supervisor/`, then deploy to the active `./.claude/` copy (per the DECISION 2026-06-27; TASK-019 establishes the tracked source first). Follow the editing-order convention at `development_guide.md` v1.4 § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts*: the templates and the `SKILL.md` § Report Pipeline section are source-of-truth bodies; the runtime output (dated report + knowledge_index entry) is derived. Edit in this order: (1) author header template, (2) author finding-section template, (3) author footer template, (4) extend `SKILL.md` with the § Report Pipeline section that references the templates and codifies the aggregation flow + same-day-suffix handling + knowledge_index integration, (5) deploy all four files to the `./.claude/` active copy. The dogfood verification of the pipeline lands at TASK-018, not here.

### Steps

1. **Author the three templates under `./shannon/skills/shannon-supervisor/templates/`**: header, finding-section, footer. Use plain prose for any substrings the templates commit to grep-verifying (per AC#6). Closes AC#1, AC#3 (template-side), AC#6.
2. **Extend `./shannon/skills/shannon-supervisor/SKILL.md` with the § Report Pipeline section** referencing the three templates, codifying the aggregation flow (spawn checkers → collect fragments → instantiate templates → write to `./docs/supervisor/report-YYYY-MM-DD.md` with same-day-suffix handling), and naming the `knowledge_index.md` write step. Closes AC#2, AC#3 (pipeline-side), AC#4.
3. **Deploy to the active copy.** Copy the three templates + updated `SKILL.md` from `./shannon/skills/shannon-supervisor/` to `./.claude/skills/shannon-supervisor/` so the running skill stays in sync with the tracked source.
4. **Portability discipline verification.** Literal greps against the new templates and `SKILL.md` § Report Pipeline for the six Shannon-self leak substrings (`FEAT-`, `EPIC-`, `Shannon-self`, `/Volumes/`, `~/`, absolute repo path). Each returns zero hits. Closes AC#5.
5. **Scope-bounded-edit verification via `git diff`.** Because `./shannon/` is tracked, `git status` / `git diff` now show the new `templates/*.md` and the `SKILL.md` § Report Pipeline addition directly — verification reverts to normal `git diff` (no `.claude/`-gitignored `ls` workaround needed). Confirm the diff touches only `./shannon/skills/shannon-supervisor/templates/*.md` and the `SKILL.md` § Report Pipeline section (inserted between § Configuration and § Hook Integration, nothing else disturbed), plus the deployed `./.claude/` copies and the lifecycle bookkeeping (TASK-015 + `task_index.md`). Explicit absence checks: no `checkers/*` modification (TASKs 12–14 territory, all APPROVED); `scripts/` still empty; no `settings.json` (TASKs 16–17 territory); no `./docs/supervisor/` writes (TASK-018 territory); no `./docs/knowledge_index.md` entry added by this Task. Closes AC#7.

### Dependencies

- **Depends on**: TASK-019 APPROVED (establishes `./shannon/skills/shannon-supervisor/` as the tracked source, incl. `SKILL.md`, which this Task extends); TASK-011 APPROVED (skeleton); TASKs 12–14 elaborated (the checker contracts must be stable so the pipeline knows what fragment shapes to aggregate — APPROVED is helpful but not strictly required, since the aggregation contract is the four-category schema either way).
- **Depended on by**: TASK-016 (PreToolUse hook's `knowledge_index.md` exception per AC#4 is what allows this Task's writer to function at runtime); TASK-018 (dogfood run exercises this pipeline end-to-end).
- **Cascade docs**: `technical_design.md` v1.2 (APPROVED); `ux_guide.md` v1.2 (APPROVED — AC#3 source); `conceptual_design.md` v1.7 (APPROVED); `development_guide.md` v1.4 (APPROVED).

### Risks

- **Hybrid-presentation default ambiguity at template level.** The ux_guide example shows the shape but the template needs to be the shape, not a tutorial. **Mitigation**: AC#3 names the hybrid shape explicitly; first dogfood (TASK-018) verifies against the rendered output.
- **Same-day-suffix collision logic missing.** A second `/shannon-report` invocation on the same date would overwrite the first without the suffix logic. **Mitigation**: AC#2 names same-day-suffix handling explicitly per `technical_design.md` v1.2 § Data Model → *Supervisor Report Files*.
- **`knowledge_index.md` write conflicting with PreToolUse hook.** TASK-016's hook must permit this write as the explicit exception per parent Epic AC#4 R-3. **Mitigation**: AC#4 of this Task names the dependency; AC#4 of TASK-016 names the reciprocal exception; both Tasks' Gate 3 verifications cross-check.

---

## Implementation Notes

### Deviations from Plan

- None. The 5-Step Plan ran exactly as Gate-2-approved.

### Gotchas

- **First Task authored in the tracked `./shannon/` source.** Per the DECISION 2026-06-27, the three templates + the `SKILL.md` § Report Pipeline extension were authored in `./shannon/skills/shannon-supervisor/` (tracked) and deployed to `./.claude/`. `git status` shows the source changes directly (`M SKILL.md`, `A templates/{header,finding-section,footer}.md`); the deployed `./.claude/` copies are gitignored and do not appear — exactly the intended split. The `git diff`-based scope verification (Step 5) worked cleanly: `SKILL.md` shows **20 insertions / 0 deletions**, confirming the § Report Pipeline block is purely additive at the reserved slot (between § Configuration line 43 and § Hook Integration, now line 86) with nothing existing disturbed — the M-8 cross-Task editing-order discipline held in the additive direction.
- **Template token convention.** Templates use `{{DOUBLE_BRACE}}` slots (e.g. `{{RUN_DATE}}`, `{{FINDING_COUNT}}`, `{{CATEGORY}}`) — plain-ASCII, grep-safe, and outside the verifiable plain-prose substrings. The four-category schema labels land as plain prose (`Drift, Gap, Internal contradiction, Strength`) in both `finding-section.md` (1 hit) and `SKILL.md` § Report Pipeline (the phrase appears alongside the existing `/shannon-report` contract mention — 2 hits total in SKILL.md), satisfying AC#6.
- **Zero-findings degenerate case deliberately omitted.** Step 3 of the pipeline prose names the zero-findings presentation as out of scope (a future Epic) without writing the `EPIC-` token — phrased as "a future Epic" to preserve portability (AC#5). The templates function for the ≥1-finding general case; the clean-run presentation pattern is the deferred work (scratchpad zero-findings item).
- **`allowed-tools:`/runtime exercise still deferred to TASK-018.** This Task codifies the pipeline as `SKILL.md` prose + templates; the first end-to-end run (spawning the three checkers, writing an actual `./docs/supervisor/report-YYYY-MM-DD.md`, and adding the first real `knowledge_index.md` entry) is TASK-018's dogfood. The knowledge_index write step is codified (AC#4) but writes nothing now — consistent with the R-A contradiction fix.

### Documents Updated

- None. TASK-015 ships the report-pipeline templates + the `SKILL.md` § Report Pipeline extension — net-new skill content under the tracked `./shannon/` source. No mandated-document amendment required (closes parent Epic AC#9 trivially).

---

## Review

*Filled at Gate 3.*

### Verification

- [ ] All acceptance criteria met
- [ ] Code follows development_guide.md
- [ ] Tests added or updated, passing
- [ ] Relevant documents updated
- [ ] Knowledge captured where useful

### Review Notes

*Filled at Gate 3.*

---

## Activity Log

- **2026-06-27** — APPROVED (Gate 3 approved). Independent verification subagent re-ran all 7 ACs from raw disk + git artefacts (not from the Activity Log); all PASS. **AC#1**: three templates with correct roles — `header.md` (diagnostic header: run date + findings/stuck/push-lag counts + checkers-succeeded), `finding-section.md` (per-finding shape with four-category schema + source citation), `footer.md` (closing prose). **AC#2**: § Report Pipeline at SKILL.md lines 66–84 — six-step Flow with explicit same-day-suffix handling (`report-YYYY-MM-DD-2.md`, then `-3`). **AC#3**: header carries all three counts; pipeline prose (line 76) + footer name the "one or two highest-signal findings" narrative. **AC#4**: write step codified naming Type *Supervisor Report* + project-relative reference; `./docs/knowledge_index.md` confirmed **not** modified by commit `3b3cf86` (the R-A contradiction fix holds — codified, not executed). **AC#5**: aggregate six-target portability greps across the four files all 0. **AC#6**: four-category labels plain prose (`finding-section.md` ×1, `SKILL.md` ×2). **AC#7**: commit `3b3cf86` touches only TASK-015 bookkeeping + `SKILL.md` (modified) + three added templates; SKILL.md numstat **20 insertions / 0 deletions** (pure insertion, hunk `@@ -63,6 +63,26 @@`); § Report Pipeline sits between § Configuration (43) and § Hook Integration (86); deployed `./.claude/` copies byte-identical to tracked `./shannon/` source (`diff -q` IDENTICAL); no checker / `settings.json` / `./docs/supervisor/` / mandated-doc changes. **Non-blocking reviewer note (recorded, not actioned)**: the finding-section template uses a single generic `{{SOURCE_CITATION}}` slot rather than separate section/line fields — satisfies AC#1 (the comment + pipeline prose specify "file path and section or line"); enforced-structure split is a candidate future refinement, not a defect. **Milestone**: first generative Task authored in the tracked `./shannon/` source under the new convention; `git diff` discipline worked end-to-end. File archived to `./docs/tasks/archive/TASK-015-report-writer-and-knowledge-index-updater.md`; `task_index.md` link updated. Closes the writer/index-updater half of parent Epic AC#3 (the dogfood verification of that AC is TASK-018). **Unblocks TASK-016** (the PreToolUse hook can now reference the pipeline's `knowledge_index.md` exception). Status: REVIEW → APPROVED.
- **2026-06-27** — IMPLEMENTED: All 5 Steps executed cleanly, authored in the tracked `./shannon/` source and deployed to `./.claude/`. **Step 1**: three templates authored under `./shannon/skills/shannon-supervisor/templates/` — `header.md` (diagnostic header: run date + findings/stuck/push-lag counts + checkers-succeeded count), `finding-section.md` (per-finding shape: category / title / originating checker / narrative / source citation, with the four-category schema named as plain prose), `footer.md` (closing prose naming the Supervisor Report subtype + knowledge_index + the hybrid-default rationale). AC#1, AC#3 (template-side), AC#6 closed. **Step 2**: `SKILL.md` extended with the § Report Pipeline section at the reserved slot (between § Configuration and § Hook Integration) — six-step Flow codifying spawn-and-collect → header → finding sections → footer → assemble-and-write (with **same-day-suffix handling** `report-YYYY-MM-DD-2.md`) → **index the report** (`./docs/knowledge_index.md`, Type *Supervisor Report*). AC#2, AC#3 (pipeline-side), AC#4 closed. **Step 3**: all four files deployed to `./.claude/skills/shannon-supervisor/`; `diff`/`diff -r` confirm deployed copies byte-identical to the tracked source. **Step 4**: six-target portability greps across the four new files returned an aggregate **0 hits** — clean. AC#5 closed. **Step 5**: `git diff` scope-bounded verification — `git status` shows only `M SKILL.md` + `A templates/{header,finding-section,footer}.md` (the `./.claude/` deployed copies gitignored); `SKILL.md` numstat **20 insertions / 0 deletions** (purely additive at the reserved slot); absence checks all 0 (no `checkers/`, `settings.json`, `./docs/supervisor/`, `knowledge_index`, or mandated-doc changes). AC#7 closed. **Milestone**: first Task authored in the tracked `./shannon/` source under the new convention — `git diff` discipline worked exactly as TASK-019 set up. Knowledge_index write step is codified but writes nothing now (first real write is TASK-018's dogfood, per the R-A contradiction fix). Ready for Gate 3 review via `/task-review TASK-015`.
- **2026-06-27** — IMPLEMENTING: Started execution of the 5-Step Plan (author 3 templates → extend `SKILL.md` § Report Pipeline → deploy to `./.claude/` → portability greps → `git diff` scope check).
- **2026-06-27** — PLANNED (Gate 2 approved). The Plan — reworked to the author-in-`./shannon/` convention during the source-tracking reconciliation (commit `6c62b87`) — is internally consistent with the Gate-1-ratified Requirements; no further carry-throughs needed at ratification beyond refreshing the stale prepared-draft header marker. The 5-Step Plan covers all seven ACs: Step 1 (author 3 templates) closes AC#1/AC#3-template/AC#6; Step 2 (extend `SKILL.md` § Report Pipeline at the reserved slot) closes AC#2/AC#3-pipeline/AC#4; Step 3 (deploy to `./.claude/`); Step 4 (six-target portability greps) closes AC#5; Step 5 (`git diff` scope verification) closes AC#7. Dependencies confirmed: TASK-019 APPROVED (`8c6bc1a`) — tracked source exists; TASK-011 APPROVED; TASKs 12–14 APPROVED (checker contracts stable). Three risks named and mitigated (hybrid-default template ambiguity → TASK-018 rendered-output check; same-day-suffix collision → AC#2; knowledge_index/PreToolUse-hook conflict → reciprocal AC#4 cross-check with TASK-016). **No new framework-general ambiguities surfaced.** Status: ELABORATED → PLANNED.
- **2026-06-27** — ELABORATED (Gate 1 approved). The Gate 1 refinements prepared earlier this session (commit `6c62b87`) are ratified now that the blocking dependency is satisfied: **TASK-019 is APPROVED** (commit `8c6bc1a`), establishing `./shannon/skills/shannon-supervisor/` as the tracked source — so this Task's `SKILL.md` extension has a tracked home, and the author-in-`./shannon/`-deploy-to-`./.claude/` ACs (AC#1/AC#2/AC#5/AC#7) are actionable. No further AC changes at this ratification — the prepared refinements (R-A contradiction fix, R-E reserved insertion point, R-B six-target grep, R-D author-in-shannon decision, R-C `git diff` verification + deploy Step) stand as recorded in the prior entry. Status: DRAFT → ELABORATED.
- **2026-06-27** — Gate 1 refinements PREPARED (status remained DRAFT — Gate 1 approval deferred pending TASK-019, the tracked-source dependency). Requirements refined from the prepared elaboration draft (carried over from EPIC-009 planning ratification commit `87626bb` on 2026-05-30). TASK-015 is a step-change in scope from the three checker Tasks — the first Task to **extend** TASK-011's `SKILL.md` body (additive cross-Task editing) and create `templates/` content — so the canonical-frontmatter refinements (R-1/R-2) do not apply; instead the refinements target the contradiction and the verification mechanics the larger scope exposes. **Refinements prepared at Gate 1 review (pending approval)**: (R-A — Internal Contradiction, the material catch) the prepared AC#7 asserted this Task "writes through to `./docs/knowledge_index.md`" while also asserting it does **not** write to `./docs/supervisor/` (TASK-018 is the first writer there) — these cannot both hold, since with no report produced there is nothing to index. Resolved: AC#4 reworded so that **at this Task's scope the knowledge_index write is satisfied by codifying the step in the § Report Pipeline prose** (Type-field value "Supervisor Report" + relative-path reference), with the first actual `knowledge_index.md` entry written at runtime by the TASK-018 dogfood; AC#7 reworded to state this Task does **not** itself add a `knowledge_index.md` entry. (R-E) AC#2 strengthened to name the reserved `SKILL.md` insertion point explicitly — between § Configuration and § Hook Integration, per the `SKILL.md` line-41 forward-reference — honouring the M-8 cross-Task editing-order discipline in the additive direction (TASK-011 deliberately reserved this slot). (R-B) AC#5 grep-target set widened to include `~/` — six targets, matching TASK-012/013/014 Gate 3 lived evidence (three consecutive clean checker passes). (R-D — superseding the prepared draft and reversing an in-session refinement) **Directing-party decision (DECISION 2026-06-27, captured in the scratchpad shipping-source-mirror item):** the question "where does the supervisor skill's tracked source live" surfaced when the directing party asked whether the `./.claude/` gitignore is a general-codebase truth or a Shannon-self artifact. It is Shannon-self-specific — `./.claude/` is gitignored here *only because* `./shannon/` is the real tracked source and `./.claude/` is a deployed copy (most Claude Code projects commit `.claude/skills/`). Investigation found EPIC-009's four APPROVED deliverables (SKILL.md + three checkers) live **only** in the gitignored `./.claude/` copy with no tracked home — inconsistent with the other three Shannon skills. Resolved: **author the supervisor skill in tracked `./shannon/skills/shannon-supervisor/` going forward, deploy to `./.claude/`.** Accordingly AC#1/AC#2/AC#5/AC#7 + Approach were reworked to author in `./shannon/` (tracked) and deploy to `./.claude/`; a corrective Task **TASK-019** was created to establish the tracked source and backfill the four APPROVED deliverables, and TASK-015 now **depends on TASK-019 APPROVED** (its `SKILL.md` extension needs the tracked source first). An earlier in-session refinement that had *deferred* the mirror (and routed Step 4 through an `ls`-based `.claude/`-gitignored workaround) was reversed by this decision. **One Plan-section refinement** (R-C, now in the tracked-source direction): Step 4 is normal **`git diff`** scope-bounded-edit verification — because `./shannon/` is tracked, the new templates and the `SKILL.md` § Report Pipeline addition show directly in `git diff`, no `ls` workaround needed; a new Step 3 adds the explicit deploy-to-`./.claude/` copy. No further Internal Contradiction. **Strengths preserved**: cascade-doc citation rigour intact (every AC carries a `Derives from` line); the source-of-truth-body-before-derived-artefacts editing order is correctly the Approach's spine (templates + `SKILL.md` are bodies; the runtime report + knowledge_index entry are derived); hybrid-presentation default correctly sourced to `ux_guide.md` v1.2; same-day-suffix handling correctly sourced to `technical_design.md` § Data Model; the PreToolUse-hook reciprocal-exception dependency with TASK-016 is correctly flagged for cross-Task Gate 3 verification. **No new framework-general ambiguities surfaced** — the G-1 frontmatter convention is settled and does not apply to this template/prose Task. Prepared Plan draft carries forward to `/task-plan TASK-015` (Gate 2) — the Plan now has five Steps (the added Step 3 deploys to `./.claude/`). Status remains DRAFT — Gate 1 approval deferred until TASK-019 (tracked-source establishment) is APPROVED; the refined Requirements above are prepared content awaiting Gate 1.
- **2026-05-30** — DRAFT: Task stub created with prepared elaboration draft and prepared plan draft during EPIC-009 planning (Gate 2). Cascading Preparation pattern per `technical_design.md` v1.2 § Key Algorithms → *Cascading Operations*. When the directing party runs `/task-elaborate TASK-015`, the prepared elaboration surfaces for Gate 1 review; when they run `/task-plan TASK-015`, the prepared plan surfaces for Gate 2 review. Descriptive title (*Report Writer and Knowledge Index Updater*) used from the outset per `conceptual_design.md` § Domain Model → *Work Item* Naming. Parent: EPIC-009. Full elaboration pending `/task-elaborate TASK-015` (Gate 1).
