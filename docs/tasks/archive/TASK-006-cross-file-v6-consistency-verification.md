# TASK-006: Cross-File V6 Consistency Verification

## Metadata

- **Status**: APPROVED
- **Type**: Task
- **Parent**: [EPIC-005](../epics/EPIC-005-v6-skills-and-claude-template.md)
- **Feature**: [FEAT-006](../features/FEAT-006-multi-party-supervision.md)
- **Tags**: #framework #v6 #verification #skills
- **Created**: 2026-05-23
- **Updated**: 2026-05-23 (approved)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

### Overview

TASK-006 is the final Task of EPIC-005's three-Task plan and the last work item before EPIC-005's own Gate 3. It is **pure verification work, not editing** — a cross-file audit confirming that the post-TASK-004 / post-TASK-005 state of the four EPIC-005 files (the three skills + the CLAUDE.md template) is mutually consistent at the phrase level and tracks the canonical sources (conceptual_design v1.5 — *Directing Party* / *Implementer* glossary, *Three Hard Gates*, *Supervisor Distinct From Implementer*, *Iterative Implementation Zone*; technical_design v1.1 — § Gate Enforcement, § Cascading Operations).

The Task was deliberately deferred until [TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md) and [TASK-005](archive/TASK-005-v6-vocabulary-and-plan-pending-work-items-skill.md) were both APPROVED — its `rg` survey and consistency audit must run against final text, not work-in-progress. Both editing Tasks are APPROVED and archived (both 2026-05-23); this audit runs against final text and closes the four cross-file EPIC-005 ACs (AC#1, AC#2, AC#3, AC#8) by writing a single conclusion entry into EPIC-005's Activity Log.

A preliminary `rg "\b[Uu]sers?\b"` and `rg "PENDING"` survey of the four target files was run at elaboration. Findings:

- **`[Uu]sers?` survey — 4 matches**, all on the preserve-list named by TASK-004 / EPIC-005:
  - `shannon/skills/project-setup/skill.md:69` — `do not overwrite user content` (file-merge sense; EPIC-005 AC#7)
  - `shannon/skills/project-setup/skill.md:83` — `Preserve any existing user content` (file-merge sense; EPIC-005 AC#7)
  - `shannon/skills/project-setup/skill.md:93` — `Target Users` (Vision-template section heading; per TASK-004's three-occurrence preserve-list at lines 69/83/93)
  - `shannon/skills/project-setup/templates/CLAUDE.md:25` — `target users` (Vision document content description; EPIC-005 AC#6, line 25 explicitly preserved)
  - `project-documentation/skill.md` — **zero** matches; `work-items/skill.md` — **zero** matches.
- **`PENDING` survey** — zero matches across all four files (TASK-005 cleared all three `PLAN-PENDING` locations: lines 190 / 200 / 282 pre-edit).
- **Self-identification lines** — all six instances verbatim: `project-setup/skill.md` lines 8 + 125 (`"Activating project-setup skill."`); `project-documentation/skill.md` lines 8 + 142 (`"Activating project-documentation skill."`); `work-items/skill.md` lines 8 + 298 (`"Activating work-items skill for [type] [verb]."`).
- **Quality Gates phrase-level comparison** — `project-documentation/skill.md` line 132 has one combined Gate-1 bullet: `Gate 1 (DRAFT → APPROVED): Explicit approval by the directing party. The implementer cannot approve its own work.` `work-items/skill.md` lines 271–275 has three Gate bullets (`Explicit approval by the directing party` each) and a standalone sentence below them: `The implementer cannot approve its own work across a gate.` The two house phrases ("Explicit approval by the directing party"; "The implementer cannot approve its own work") are present verbatim in both files; the bullet-structure variance is the one TASK-005 disclosed in Implementation Notes § Gotchas as acceptable per the no-structural-changes AC.
- **`work-items/skill.md` line 227** — post-TASK-005 text reads: `The directing party or implementer may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless.` The V6 roles are named; conceptual_design's full *Iterative Implementation Zone* "bracket the zone" framing ("Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone") is not lifted. TASK-005 disclosed this at Gate 3 as a minor Gap, non-blocking; the retained "The iterative zone is gateless" wording is V6-compatible and met under TASK-005's "in V6 terms" wording (TASK-005 AC#11 / EPIC-005 AC#5).

The audit's task is to confirm the survey above against the final-text state, classify every surviving `[Uu]sers?` occurrence as preserve-sense, audit the four-way phrase-level equivalence (both skills' § Quality Gates ↔ conceptual_design *Supervisor Distinct From Implementer* / *Three Hard Gates* ↔ technical_design § Gate Enforcement "by convention, not technical control"), and write a single audit-conclusion entry into EPIC-005's Activity Log so EPIC-005's own Gate 3 can verify AC#1 / AC#2 / AC#3 / AC#8 via that one entry.

If the audit surfaces a defect — a missed occurrence, a phrase that drifted from the canonical sources, an accidental structural change — the preferred resolution is **inline correction by this Task** with the appropriate `.claude/skills/` re-sync. Re-opening an archived Task is heavier and is the fallback for structurally significant defects; the Plan will record the chosen route explicitly. Where the line 227 *Iterative Implementation Zone* phrasing nit is concerned, this Task's verdict is **(a) the current wording is sufficient and survives the audit** — TASK-005's Gate 3 already approved it as met under loose-AC wording and the directing party may surface the nit as a future-Epic candidate rather than re-opening here; if the directing party disagrees at Gate 1 of this Task, the alternative **(b) recommends an inline correction with re-sync** and the Plan will sequence it.

**Out of scope**: any vocabulary editing beyond fixing an audit-surfaced defect; any change to a template other than `CLAUDE.md`; any command file (`shannon/commands/*.md`); any other skill index, mandated document, or work-item file other than this TASK-006 file itself and EPIC-005's Activity Log. The literal-file-count, editing-order, commit-hash-timing, bundled-re-elaboration, and naming-convention items captured in `scratchpad.md` are out of scope for this Task.

### Acceptance Criteria

`rg [Uu]sers?` survey and justifications (EPIC-005 **AC#1**):

- [x] Running `rg -n "\b[Uu]sers?\b" shannon/skills/project-setup/skill.md shannon/skills/project-documentation/skill.md shannon/skills/work-items/skill.md shannon/skills/project-setup/templates/CLAUDE.md` returns exactly the four preserve-sense matches identified at elaboration — `project-setup/skill.md:69` (`do not overwrite user content`, file-merge sense), `project-setup/skill.md:83` (`Preserve any existing user content`, file-merge sense), `project-setup/skill.md:93` (`Target Users`, Vision-template section name), and `templates/CLAUDE.md:25` (`target users`, Vision document content description). `project-documentation/skill.md` and `work-items/skill.md` return **zero** matches.
- [x] Each of the four surviving occurrences is classified in the audit's report (entered into EPIC-005's Activity Log per the audit-summary AC below) as preserve-sense, with the per-occurrence justification: lines 69 / 83 = file-merge "user content" (EPIC-005 AC#7); line 93 = Vision template section heading "Target Users" (TASK-004 preserve-list, EPIC-005 AC#6 / AC#7 by parallel); template line 25 = Vision document content description "target users" (EPIC-005 AC#6, line 25 explicitly preserved). No surviving occurrence is an unintended survivor. If the audit finds an unintended survivor, the defect-resolution policy AC below governs the response.

Gate-approval and self-approval-prevention sentences (EPIC-005 **AC#2**):

- [x] Every gate-approval sentence across the four files names the **directing party** as the explicit approver. Audit anchors: `project-documentation/skill.md:121` (§ Process: Review step 4 — `Ask the directing party explicitly: "Approve this document to mark APPROVED?"`); `project-documentation/skill.md:132` (§ Quality Gates Gate 1 — `Explicit approval by the directing party`); `work-items/skill.md:148` (§ Process: Elaborate step 4 / Gate 1 — `Ask explicitly: "Approve these requirements to mark this ELABORATED?"`, with the directing party named as the asker in surrounding context); `work-items/skill.md:198` (§ Process: Plan step 5 / Gate 2 — `Ask explicitly: "Approve this plan to mark this PLANNED?"`); `work-items/skill.md:254` (§ Process: Review step 5 / Gate 3 — `Ask explicitly: "Approve this work to mark this APPROVED?"`); `work-items/skill.md:271–273` (§ Quality Gates Gate 1 / Gate 2 / Gate 3 — three `Explicit approval by the directing party` bullets); `templates/CLAUDE.md:65` (`Three quality gates require explicit directing-party approval`); `templates/CLAUDE.md:133` (`every gate requires explicit directing-party approval`).
- [x] Every self-approval-prevention sentence across the four files names the **implementer** as the agent that cannot approve its own work. Audit anchors: `project-documentation/skill.md:132` (combined Gate-1 bullet — `The implementer cannot approve its own work.`); `work-items/skill.md:275` (standalone sentence below the three Gate bullets — `The implementer cannot approve its own work across a gate.`); `project-setup/skill.md` carries no self-approval-prevention sentence by design (the skill operates before Gate 1; see § Quality Gates note at line 107 — `This skill operates **before** the work item lifecycle, so no Gates 1–3 apply directly.`) and `templates/CLAUDE.md` likewise carries no self-approval-prevention sentence (the template lists "Three quality gates require explicit directing-party approval" without restating the implementer-cannot-self-approve invariant — the canonical statement lives in the skills and conceptual_design).

Four-way § Quality Gates phrase-level consistency (EPIC-005 **AC#3**):

- [x] `project-documentation/skill.md § Quality Gates` and `work-items/skill.md § Quality Gates` use the verbatim TASK-004 house phrases for the same concepts: `Explicit approval by the directing party` (gate-approval) and `The implementer cannot approve its own work` (self-approval prevention). Phrase-level equivalence holds.
- [x] Both files' § Quality Gates wording is consistent with `conceptual_design.md` *Supervisor Distinct From Implementer* (`the implementer cannot approve its own work across a gate; see Supervisor Distinct From Implementer`) and *Three Hard Gates* (`explicit approval by the directing party`), and with `technical_design.md § Gate Enforcement` (enforcement is "by convention, not technical control"). The four-way consistency check is content-anchored, not bullet-count-anchored.
- [x] The bullet-structure variance between the two § Quality Gates sections — `project-documentation/skill.md:132` has one combined Gate-1 bullet (the skill manages only Gate 1 for documents); `work-items/skill.md:271–275` has three Gate bullets (Gate 1, 2, 3 — one per work-item gate) plus a standalone implementer-self-approval-prevention sentence — is **acceptable** per TASK-005's Implementation Notes § Gotchas. Rationale: each skill enumerates only the gates it owns, so the structural difference is *required* by the no-structural-changes scope of TASK-004 / TASK-005, not a residual defect. The audit confirms phrase-level equivalence holds despite the structural difference; no inline correction is required on this finding.

Self-identification lines (EPIC-005 **AC#8**):

- [x] `rg -n "Activating .* skill\."` across the four target files returns the six expected lines verbatim, all six unchanged from before EPIC-005: `project-setup/skill.md:8` and `project-setup/skill.md:125` — `"Activating project-setup skill."`; `project-documentation/skill.md:8` and `project-documentation/skill.md:142` — `"Activating project-documentation skill."`; `work-items/skill.md:8` and `work-items/skill.md:298` — `"Activating work-items skill for [type] [verb]."` Both the When-Invoked instance and the § Self-Identification instance are intact in each skill file. (`templates/CLAUDE.md` has no self-identification line — it is a project template, not a skill.)

Cross-check of the two TASK-005 Gate 3 findings:

- [x] **Line 227 *Iterative Implementation Zone* phrasing nit.** `work-items/skill.md:227` post-TASK-005 reads `The directing party or implementer may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless.` The two V6 roles are named; conceptual_design's "Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone" framing is not lifted. The audit's verdict is that the current wording is **sufficient and survives the audit** — TASK-005's Gate 3 approved this wording under EPIC-005 AC#5 / TASK-005 AC#11 ("in V6 terms"), and the retained "The iterative zone is gateless" wording is V6-compatible. The directing party may surface this as a future-Epic candidate (e.g. a documentation-tightening sweep) but the audit recommends **no inline correction by this Task**.
- [x] **Bullet-structure variance** between `work-items/skill.md § Quality Gates` (three Gate bullets + standalone) and `project-documentation/skill.md § Quality Gates` (one combined bullet) — confirmed phrase-level equivalence holds (same `Explicit approval by the directing party` and `The implementer cannot approve its own work` phrases in both), consistent with the EPIC-005 AC#3 audit above. Structural difference is required by the skills' differing gate scopes and is acceptable per TASK-005's recorded Gotcha; no inline correction is required.

Defect-resolution policy:

- [x] If any audit-surfaced defect is found (an unintended `[Uu]sers?` survivor, a phrase that drifted from the canonical sources, a missed gate-approval or self-approval-prevention sentence, or an accidentally-changed self-identification line), the **preferred resolution is inline correction by this Task** with the corresponding `shannon/skills/` source edit and `.claude/skills/` re-sync. Re-opening an archived sibling Task (TASK-004 / TASK-005) is the fallback for structurally significant defects only. Either choice is named explicitly in this Task's Activity Log and Implementation Notes. The two pre-elaboration findings above (line 227, bullet-structure variance) have been pre-judged in this Task's Acceptance Criteria as "no inline correction required"; this AC governs any *additional* defect the audit surfaces.

Audit summary write-back to EPIC-005's Activity Log:

- [x] At Gate 3 of this Task, an entry is appended to `EPIC-005`'s Activity Log recording the audit's conclusion: (i) every surviving `[Uu]sers?` occurrence justified (`project-setup/skill.md:69` / `:83` file-merge sense; `:93` Vision section name; `templates/CLAUDE.md:25` Vision document content description; `project-documentation/skill.md` and `work-items/skill.md` both zero); (ii) four-way § Quality Gates phrase-level equivalence confirmed; (iii) the two TASK-005 Gate 3 findings (line 227, bullet-structure variance) resolved per the cross-check ACs above; (iv) all six self-identification lines verbatim. EPIC-005's own Gate 3 then verifies AC#1 / AC#2 / AC#3 / AC#8 via that single entry — the Task is the canonical record, EPIC-005's entry is the index.

Scope boundary (cross-type guard, not a literal file count):

- [x] No vocabulary editing is performed by this Task **beyond the inline-correction route** if a defect is found. No template (other than `templates/CLAUDE.md`, which is one of the four audit-target files) is edited. No command file (`shannon/commands/*.md`) is edited. No skill index, mandated document, or work-item file other than this `TASK-006` file itself, EPIC-005's Activity Log, and — only if an audit-surfaced defect mandates inline correction — the directly-defective skill source plus its `.claude/skills/` deployed copy is changed. Phrased as a cross-type guard rather than a literal file count per the TASK-002 / TASK-003 lesson (recorded in TASK-003's archive): bare file-count caps obscure intent; the actual rule is "no editing beyond the audit's verdict and its associated re-sync".

Markdown / development_guide alignment:

- [x] Markdown heading hierarchy in this Task file is intact (one `#` title; `##` for Requirements / Plan / Implementation Notes / Review / Activity Log; `###` for sub-sections); British-English spelling preserved (e.g. "behaviour", "organisation") per development_guide § Code Style. Per development_guide § Commit Cadence, **no commit is made until after Gate 3** — pre-gate experimentation is fine, but the commit lands only when EPIC-005's APPROVED state is batched with TASK-006's APPROVED state at the end of the EPIC-005 A → B → C sequence.

### Context

- **Parent Epic**: [EPIC-005 — V6 Vocabulary in Skills and CLAUDE.md Template](../epics/EPIC-005-v6-skills-and-claude-template.md) — this is the verification Task in its § Plan; closes EPIC-005 AC#1 (`rg` survey + Activity Log justifications), AC#2 (gate-approval consistency), AC#3 (four-way § Quality Gates consistency), AC#8 (self-identification lines unchanged)
- **Parent Feature**: [FEAT-006 — Multi-Party Supervision](../features/FEAT-006-multi-party-supervision.md) — § Ideal State "Skills and commands refuse self-approval flows" bullet (partly met, this Epic closes the skills half)
- **Sibling Tasks (both APPROVED 2026-05-23, archived — their outputs are what this Task audits)**: [TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md) (`project-setup/skill.md`, `project-documentation/skill.md`, `templates/CLAUDE.md` — homogeneous vocabulary judgement; the project-documentation § Quality Gates rewrite established the house phrases) and [TASK-005](archive/TASK-005-v6-vocabulary-and-plan-pending-work-items-skill.md) (`work-items/skill.md` — vocabulary sweep + the AC#4 `PLAN-PENDING` structural rewrite + AC#5 line-227 reword; Implementation Notes § Gotchas disclosed the bullet-structure variance; Review noted the line-227 phrasing nit as non-blocking)
- **Audit targets** (read-only, unless an audit-surfaced defect mandates inline correction): `shannon/skills/project-setup/skill.md`, `shannon/skills/project-documentation/skill.md`, `shannon/skills/work-items/skill.md`, `shannon/skills/project-setup/templates/CLAUDE.md`
- **Canonical sources** (the references the audit compares against): `conceptual_design.md` v1.5 — *Directing Party* / *Implementer* glossary; *Three Hard Gates* business rule ("explicit approval by the directing party"; "The implementer cannot approve its own work across a gate"); *Supervisor Distinct From Implementer* business rule; *Iterative Implementation Zone* business rule ("the implementer and the directing party iterate freely without gates. Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone"). `technical_design.md` v1.1 — § Gate Enforcement (the `Supervisor Distinct From Implementer` rule is "enforced by convention, not by technical control"); § Cascading Operations: Batch Preparation, Individual Gates (the load-bearing reference TASK-005's structural rewrite copied verbatim)
- **Relevant Guide**: `development_guide.md` § Code Style (British-English Markdown conventions; heading hierarchy), § Distribution (the `.claude/skills/` re-sync obligation triggered only if a defect requires inline correction), § Git Workflow → Commit Cadence (no commit until Gate 3 — batched with EPIC-005's APPROVED state at the end of the A → B → C sequence)
- **Pre-elaboration survey** (the data this Task audits against final-text state): `[Uu]sers?` → 4 matches, all preserve-sense (project-setup 69 / 83 / 93; templates/CLAUDE.md 25); `PENDING` → 0; self-identification → six lines verbatim; § Quality Gates → both skills carry the TASK-004 house phrases at the phrase level; bullet-structure variance acceptable; line 227 phrasing sufficient under AC#5's loose-wording precedent

---

## Plan

*Drafted at `/task-plan TASK-006` (Gate 2).*

### Approach

Pure verification, default no-edit. TASK-006 runs a structured cross-file audit against the final post-TASK-004 / post-TASK-005 text of the four EPIC-005 in-scope files (`shannon/skills/project-setup/skill.md`, `shannon/skills/project-documentation/skill.md`, `shannon/skills/work-items/skill.md`, `shannon/skills/project-setup/templates/CLAUDE.md`), classifies every survey result against the elaboration-time baseline, and writes a single audit-summary entry into `EPIC-005`'s Activity Log so EPIC-005's own Gate 3 closes AC#1 / AC#2 / AC#3 / AC#8 by reading that one entry. The four pre-judged findings (4 preserve-sense `[Uu]sers?` matches, zero `PENDING`, six verbatim self-id lines, four-way § Quality Gates phrase-level equivalence) become the audit's expected baseline — the audit confirms each against final text rather than re-deriving from scratch.

The Plan has two paths: the **expected path** (no defect surfaces) is bookkeeping-only — re-run surveys, walk the AC-anchor list, write the EPIC-005 Activity Log entry, verify scope guard. The **conditional path** (a defect surfaces) inlines the correction in this Task per the defect-resolution policy AC, re-syncs `.claude/skills/` per development_guide § Distribution, and records the route in Implementation Notes and the EPIC-005 Activity Log entry. Re-opening an archived sibling Task is the fallback for structurally significant defects only.

The two pre-elaboration cross-check findings are pre-judged in the Acceptance Criteria: line 227's *Iterative Implementation Zone* phrasing nit is **route (a) sufficient** — no inline correction; the bullet-structure variance between `project-documentation/skill.md § Quality Gates` (one combined Gate-1 bullet) and `work-items/skill.md § Quality Gates` (three Gate bullets + standalone implementer sentence) is **structurally required** by the skills' differing gate scopes — no inline correction. The audit confirms both verdicts against final text; it does not re-litigate them.

Canonical phrasing is lifted, not invented: gate-approval phrasing tracks conceptual_design v1.5 *Three Hard Gates* ("explicit approval by the directing party") and *Supervisor Distinct From Implementer* ("the implementer cannot approve its own work across a gate"); enforcement language tracks technical_design v1.1 § Gate Enforcement ("by convention, not technical control"). No commit lands until after Gate 3 of EPIC-005 per development_guide § Commit Cadence (the A → B → C batch).

### Steps

1. **Orientation — re-read the four target files in final-text state and the canonical sources.** Read `shannon/skills/project-setup/skill.md`, `shannon/skills/project-documentation/skill.md`, `shannon/skills/work-items/skill.md`, `shannon/skills/project-setup/templates/CLAUDE.md`, plus `docs/conceptual_design.md` (*Directing Party* / *Implementer* glossary entries, *Three Hard Gates*, *Supervisor Distinct From Implementer*, *Iterative Implementation Zone*) and `docs/technical_design.md` (§ Gate Enforcement, § Cascading Operations: Batch Preparation, Individual Gates). Confirm the elaboration-time line anchors are still valid (no unexpected line-count shifts since TASK-005 archived). No file edits. *(Orientation; no AC closed yet.)*

2. **Re-run the `[Uu]sers?` survey against final text and classify every match.** Execute `rg -n "\b[Uu]sers?\b" shannon/skills/project-setup/skill.md shannon/skills/project-documentation/skill.md shannon/skills/work-items/skill.md shannon/skills/project-setup/templates/CLAUDE.md`. Expect exactly four matches: `project-setup/skill.md:69` (`do not overwrite user content`, file-merge sense; EPIC-005 AC#7), `project-setup/skill.md:83` (`Preserve any existing user content`, file-merge sense; EPIC-005 AC#7), `project-setup/skill.md:93` (`Target Users`, Vision-template section heading; TASK-004 preserve-list), and `templates/CLAUDE.md:25` (`target users`, Vision document content description; EPIC-005 AC#6). Expect zero matches in `project-documentation/skill.md` and `work-items/skill.md`. Record the per-occurrence justification verbatim, ready to paste into the EPIC-005 Activity Log entry at Step 9. *(Closes AC#1 — Acceptance Criterion bullets 1 and 2 in this Task.)*

3. **Re-run the `PENDING` survey and the self-identification survey.** Execute `rg -n "PENDING" shannon/skills/project-setup/skill.md shannon/skills/project-documentation/skill.md shannon/skills/work-items/skill.md shannon/skills/project-setup/templates/CLAUDE.md` — expect zero matches across all four files (TASK-005's structural rewrite cleared the three `PLAN-PENDING` locations at pre-edit lines 190 / 200 / 282). Then execute `rg -n "Activating .* skill\." shannon/skills/project-setup/skill.md shannon/skills/project-documentation/skill.md shannon/skills/work-items/skill.md shannon/skills/project-setup/templates/CLAUDE.md` — expect exactly six matches verbatim: `project-setup/skill.md:8` and `:125` (`"Activating project-setup skill."`); `project-documentation/skill.md:8` and `:142` (`"Activating project-documentation skill."`); `work-items/skill.md:8` and `:298` (`"Activating work-items skill for [type] [verb]."`). `templates/CLAUDE.md` has no self-id line by design. *(Closes AC#8 — Acceptance Criterion bullet on self-identification lines.)*

4. **Audit gate-approval sentences across the four files (directing-party-named).** Read each of the eight anchor locations listed in AC#2's first bullet and confirm each names the *directing party* as approver: `project-documentation/skill.md:121` (§ Process: Review step 4 — `Ask the directing party explicitly: "Approve this document to mark APPROVED?"`); `project-documentation/skill.md:132` (§ Quality Gates Gate 1 bullet — `Explicit approval by the directing party`); `work-items/skill.md:148` (§ Elaborate step 4 / Gate 1); `:198` (§ Plan step 5 / Gate 2); `:254` (§ Review step 5 / Gate 3); `:271–273` (§ Quality Gates Gate 1 / 2 / 3 — three `Explicit approval by the directing party` bullets); `templates/CLAUDE.md:65` (`Three quality gates require explicit directing-party approval`); `templates/CLAUDE.md:133` (`every gate requires explicit directing-party approval`). *(Closes AC#2 — Acceptance Criterion bullet 1.)*

5. **Audit self-approval-prevention sentences (implementer-named).** Confirm `project-documentation/skill.md:132` carries `The implementer cannot approve its own work.` and `work-items/skill.md:275` carries `The implementer cannot approve its own work across a gate.` Confirm `project-setup/skill.md` carries no self-approval-prevention sentence by design (the skill operates before Gate 1 — see the line 107 note `This skill operates **before** the work item lifecycle, so no Gates 1–3 apply directly.`) and `templates/CLAUDE.md` likewise carries no self-approval-prevention sentence (the canonical statement lives in the skills and conceptual_design). *(Closes AC#2 — Acceptance Criterion bullet 2.)*

6. **Audit the four-way § Quality Gates phrase-level equivalence.** Confirm both `project-documentation/skill.md § Quality Gates` (line 132 — one combined Gate-1 bullet) and `work-items/skill.md § Quality Gates` (lines 271–275 — three Gate bullets plus standalone implementer sentence) carry the verbatim TASK-004 house phrases: `Explicit approval by the directing party` (gate-approval) and `The implementer cannot approve its own work` (self-approval prevention). Confirm both files' wording is consistent with `conceptual_design.md` lines 165 (*Three Hard Gates* — `explicit approval by the directing party`; `The implementer cannot approve its own work across a gate`) and 167 (*Supervisor Distinct From Implementer*), and with `technical_design.md:196` § Gate Enforcement (enforcement is `by convention, not by technical control`). The bullet-structure variance is content-anchored equivalence, not bullet-count-anchored. *(Closes AC#3 — both bullets — and the bullet-structure cross-check bullet under AC "Cross-check of the two TASK-005 Gate 3 findings".)*

7. **Audit the line 227 *Iterative Implementation Zone* phrasing nit (cross-check).** Confirm `work-items/skill.md:227` reads `The directing party or implementer may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless.` — V6 roles named, conceptual_design's full "bracket the zone" framing not lifted. Per the pre-judged verdict (TASK-005 Gate 3 approved this under TASK-005 AC#11 / EPIC-005 AC#5's "in V6 terms" wording; directing party confirmed route (a) at this Task's Gate 1), the audit verdict is **sufficient, no inline correction by this Task**. The phrasing is V6-compatible; any tightening is a future-Epic candidate. *(Closes the line-227 cross-check bullet under AC "Cross-check of the two TASK-005 Gate 3 findings".)*

8. **Conditional defect-resolution branch (skipped if Steps 2–7 surface no surprise).** If any audit step surfaces an unexpected defect (an unintended `[Uu]sers?` survivor, a phrase that drifted from conceptual_design / technical_design, a missed gate-approval or self-approval-prevention sentence, an accidentally-changed self-identification line, or a residual `PENDING`), this Task makes the inline correction directly in the directly-defective `shannon/skills/` source file, re-syncs the corresponding `.claude/skills/` deployed copy per development_guide § Distribution, and records both the defect and the chosen route in this Task's Implementation Notes § Gotchas and the EPIC-005 Activity Log entry at Step 9. Re-opening an archived sibling Task is the fallback for structurally significant defects only — name the choice explicitly when made. *(Closes the defect-resolution policy AC; closes AC#1 / AC#2 / AC#3 / AC#8 robustly even under surprise.)*

9. **Write the EPIC-005 Activity Log entry — the single audit-conclusion record.** Append one dated entry to `docs/epics/EPIC-005-v6-skills-and-claude-template.md § Activity Log` recording the audit's four conclusions: **(i)** every surviving `[Uu]sers?` occurrence justified with location and sense — `project-setup/skill.md:69` and `:83` file-merge sense (EPIC-005 AC#7); `project-setup/skill.md:93` Vision-template section heading `Target Users` (TASK-004 preserve-list); `templates/CLAUDE.md:25` Vision document content description `target users` (EPIC-005 AC#6); `project-documentation/skill.md` and `work-items/skill.md` both zero; **(ii)** four-way § Quality Gates phrase-level equivalence confirmed against `conceptual_design.md` *Three Hard Gates* / *Supervisor Distinct From Implementer* and `technical_design.md` § Gate Enforcement, with the bullet-structure variance noted as **structurally required** (each skill enumerates only the gates it owns); **(iii)** line 227 phrasing verdict **route (a) sufficient** — V6-compatible under EPIC-005 AC#5 / TASK-005 AC#11's "in V6 terms" wording, no inline correction by this Task; **(iv)** all six self-identification lines verbatim across the four target files. Note the route taken at Step 8 explicitly (default no-edit, or inline correction with re-sync) so the EPIC-005 Activity Log entry is the index pointing back at this Task's Implementation Notes as the canonical record. *(Closes the "Audit summary write-back to EPIC-005's Activity Log" AC; enables EPIC-005's own Gate 3 to verify AC#1 / AC#2 / AC#3 / AC#8 via this single entry.)*

10. **Verify scope guard — confirm no files outside the audit envelope were touched.** Run `git status` and confirm the only modified files are this `TASK-006` file (Implementation Notes / Review / Activity Log sections), `EPIC-005`'s Activity Log entry from Step 9, and — only if Step 8's conditional branch fired — the directly-defective `shannon/skills/` source plus its `.claude/skills/` deployed copy. No command file (`shannon/commands/*.md`), no template other than the in-scope `templates/CLAUDE.md`, no skill index, no mandated document, no other work-item file. *(Closes the "Scope boundary (cross-type guard)" AC.)*

11. **Markdown / development_guide compliance check on this Task file.** Confirm the file's heading hierarchy is intact (one `#` title; `##` for Requirements / Plan / Implementation Notes / Review / Activity Log; `###` for sub-sections); confirm British-English spelling preserved (`behaviour`, `organisation`, `colour`); confirm no commit has been made — per development_guide § Commit Cadence, no commit lands until after EPIC-005's own Gate 3 batches the A → B → C sequence. *(Closes the "Markdown / development_guide alignment" AC.)*

### Dependencies

- [TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md) APPROVED 2026-05-23 (met); [TASK-005](archive/TASK-005-v6-vocabulary-and-plan-pending-work-items-skill.md) APPROVED 2026-05-23 (met). Both editing Tasks must be APPROVED before this verification audit runs against final text — TASK-005 in particular because its § Cascading Operations structural rewrite shifted line numbers, so all anchor lines in the Audit-target ACs reference *post-TASK-005* numbering.
- EPIC-005 PLANNED (met); `conceptual_design.md` v1.5 (*Directing Party* / *Implementer* glossary, *Three Hard Gates*, *Supervisor Distinct From Implementer*, *Iterative Implementation Zone*) and `technical_design.md` v1.1 (§ Gate Enforcement, § Cascading Operations: Batch Preparation, Individual Gates) APPROVED — both load-bearing canonical references.
- `development_guide.md` v1.2 § Code Style (British-English Markdown), § Distribution (the `.claude/skills/` re-sync obligation triggered only in Step 8's conditional branch), § Commit Cadence (no commit until after Gate 3, batched with EPIC-005's APPROVED state).

### Risks

- **Audit surfaces an unexpected defect requiring inline correction** — a missed `[Uu]sers?` occurrence, a phrase that drifted from canonical sources, an accidentally-changed self-id line. *Mitigation*: Step 8 is the planned conditional branch, not a panic path — the defect-resolution policy AC pre-authorises inline correction with `.claude/skills/` re-sync, the choice of route (inline vs. re-open archived Task) is recorded explicitly in Implementation Notes § Gotchas and the EPIC-005 Activity Log entry, and the surprise itself is captured as a Gotcha for future cross-file audits.
- **Phrasing drift undetected by phrase-level comparison** — both skills carry `Explicit approval by the directing party` and `The implementer cannot approve its own work` verbatim, but a nearby sentence has drifted from conceptual_design / technical_design phrasing in a way the spot-anchor audit misses. *Mitigation*: Step 6 is content-anchored against the canonical sources (the *Three Hard Gates* and *Supervisor Distinct From Implementer* rules at conceptual_design.md:165 / :167, and § Gate Enforcement at technical_design.md:196), not bullet-count-anchored; if drift is found, route through Step 8. The four-way comparison is named explicitly so the audit is reproducible by EPIC-005's own Gate 3 if anything is missed.
- **Re-sync omission if a correction is made** — Step 8 edits `shannon/` source but the implementer forgets to refresh the gitignored `.claude/skills/` deployed copies, leaving the running framework stale. *Mitigation*: Step 8 names the re-sync explicitly as part of the conditional branch; Step 10's scope-guard `git status` check confirms the `.claude/skills/` deployed copy was updated alongside its source if the branch fired; development_guide § Distribution is the canonical reference.
- **The EPIC-005 Activity Log entry drifts from the actual audit findings** — Step 9 paraphrases instead of recording verbatim, or omits one of the four conclusions, leaving EPIC-005's own Gate 3 verifier unable to confirm AC#1 / AC#2 / AC#3 / AC#8 from that single entry. *Mitigation*: Step 9 enumerates the four conclusions (i)–(iv) explicitly, the per-occurrence justifications are recorded verbatim in Step 2 ready to paste in, and Step 9's wording mirrors this Task's Requirements ACs so the EPIC-005 entry is provably the index back to this Task as the canonical record.
- **Scope creep — touching unrelated files** — the audit reads many files and the implementer accidentally tidies an adjacent file, a command file, or a sibling work item. *Mitigation*: Step 10's `git status` scope-guard check is mandatory and is the last step before Gate 3; the AC "Scope boundary (cross-type guard, not a literal file count)" is the policy reference; only this `TASK-006` file, `EPIC-005`'s Activity Log, and — conditionally — one `shannon/skills/` source + its `.claude/skills/` deployed copy may be modified.
- **Line-anchor staleness if TASK-004 / TASK-005 final-text line numbers shifted from elaboration-time** — the anchor lines in the ACs (e.g. `project-documentation/skill.md:121` / `:132`; `work-items/skill.md:148` / `:198` / `:254` / `:271–275` / `:227`; `templates/CLAUDE.md:25` / `:65` / `:133`) may have drifted by a line or two. *Mitigation*: Step 1's orientation reads each file fresh; Steps 4–7 cite the section name plus the line number so a one-line drift is recoverable by section anchor, not solely by line number. If drift is significant, record it in Implementation Notes § Gotchas — it does not change the verdict.

---

## Implementation Notes

Implemented 2026-05-23 via `/task-implement TASK-006`, following the Gate 2 eleven-step plan. The audit completed without surfacing any defect: **Step 8 (conditional inline correction + `.claude/skills/` re-sync) did not trigger**, and no `shannon/` source file was modified by this Task.

**Step 2 — `[Uu]sers?` survey.** `rg -n "\b[Uu]sers?\b"` across the four target files returned exactly the 4 preserve-sense matches the elaboration predicted: `project-setup/skill.md:69/83` ("user content", file-merge sense), `project-setup/skill.md:93` ("Target Users", Vision template section heading), `templates/CLAUDE.md:25` ("target users", Vision document content description). `project-documentation/skill.md` and `work-items/skill.md` returned zero. Per-occurrence justifications enumerated verbatim in EPIC-005's Activity Log entry (i).

**Step 3 — `PENDING` + self-identification.** `rg PENDING` returned zero across all four files. All six self-identification lines verbatim (lines 8 + 125 in `project-setup`; 8 + 142 in `project-documentation`; 8 + 298 in `work-items`).

**Steps 4–5 — gate-approval / self-approval-prevention audit.** Every gate-approval sentence names the *directing party*; every self-approval-prevention sentence names the *implementer*. `project-setup/skill.md` and `templates/CLAUDE.md` carry no self-approval-prevention sentence by design — gate enforcement is owned by `project-documentation` and `work-items`.

**Step 6 — four-way § Quality Gates phrase audit.** Phrase-level equivalence confirmed: both skills carry the verbatim TASK-004 house phrases ("Explicit approval by the directing party"; "The implementer cannot approve its own work"). Bullet-structure variance (1 combined bullet in `project-documentation` vs 3 bullets + standalone in `work-items`) confirmed structurally required — each skill enumerates only the gates it owns. Both files trace cleanly to conceptual_design *Supervisor Distinct From Implementer* / *Three Hard Gates* and technical_design § Gate Enforcement.

**Step 7 — line 227 verdict.** Route (a) confirmed: the current wording ("The directing party or implementer may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless.") names V6 roles and is V6-compatible. The full *Iterative Implementation Zone* "bracket the zone" phrasing is not lifted, but TASK-005's Gate 3 already approved it under loose AC wording. No inline correction.

**Step 8 — conditional inline correction.** Not triggered. No defect surfaced from Steps 2–7.

**Step 9 — EPIC-005 Activity Log audit-summary entry.** Single canonical entry written to EPIC-005's Activity Log enumerating the four audit conclusions (i)–(iv). EPIC-005's own Gate 3 reads that single entry to verify AC#1 / AC#2 / AC#3 / AC#8.

**Step 10 — scope-guard `git status`.** Pre-review working tree shows two files modified: this Task file and EPIC-005 (Activity Log write + incidental cleanup — see Deviations). No `shannon/` source file modified. No template, no command, no other skill modified. Cross-type scope guard holds.

**Step 11 — Markdown / Commit Cadence compliance.** Heading hierarchy intact on this Task file; British-English spelling preserved; the audit-summary entry on EPIC-005 uses dash-bullet style consistent with development_guide § Code Style. Commit deferred to after Gate 3 per § Commit Cadence.

### Deviations from Plan

- **Incidental in-scope cleanup of residual `A` / `B` / `C` bare-letter references in EPIC-005's Activity Log Gate-2 entry.** The earlier naming-convention cleanup (commit `3683606`) used `replace_all` on the full tokens `TASK-A` / `TASK-B` / `TASK-C` and missed three standalone-letter survivors in EPIC-005's Gate-2 Activity Log entry: "Sequenced A → B → C — A sets the vocabulary precedent…, C audits the final text…". The audit's reading of EPIC-005 surfaced these as residual drift. Fixed in passing during the Step 9 EPIC-005 write — the prose now reads "Sequenced TASK-004 → TASK-005 → the verification Task — TASK-004 sets…, the verification Task audits…". This is squarely within the spirit of the prior cleanup (same opaque labels, same intent) rather than scope creep, and is recorded here so Gate 3 review sees the diff. Same in-scope-extension pattern as TASK-003's FEAT-003 intro-line cleanup.

### Gotchas

- The audit-summary write to EPIC-005's Activity Log is the only edit this Task makes to a `docs/` file other than its own — the scope-guard AC explicitly anticipates this (EPIC-005 Activity Log is the single in-scope external write target).

### Documents Updated

- `docs/tasks/TASK-006-cross-file-v6-consistency-verification.md` — Implementation Notes filled; Activity Log IMPLEMENTED entry; status / Updated metadata.
- `docs/epics/EPIC-005-v6-skills-and-claude-template.md` — Step 9 audit-summary entry prepended to § Activity Log; incidental cleanup of three residual `A` / `B` / `C` bare-letter references in the Gate-2 entry.
- No `shannon/` source modified — Step 8 not triggered.

---

## Review

**Gate 3 — APPROVED 2026-05-23.** A verification subagent audited all Acceptance Criteria against the post-edit state of the four EPIC-005 files, the EPIC-005 Activity Log audit-summary entry (Step 9 write), and the recorded Deviation. Verdict: *ready for Gate 3 approval* — all ACs MET, no blocking findings.

### Verification

- [x] All acceptance criteria met
- [x] Changes follow development_guide.md
- [x] Relevant documents updated
- [x] Knowledge captured where useful

### Review Notes

**Re-surveys reproduce the elaboration baseline exactly.** `rg [Uu]sers?` → 4 preserve-sense matches (`project-setup/skill.md:69/83/93` and `templates/CLAUDE.md:25`); `rg PENDING` → 0; 6 self-identification lines verbatim; `git diff --name-only` confined to TASK-006 + EPIC-005 + `task_index.md` (standard bookkeeping); no `shannon/` source modified.

**EPIC-005 audit-summary entry verified self-sufficient.** The single canonical entry at the top of EPIC-005's § Activity Log carries all four conclusions (i)–(iv), each explicitly closing an EPIC-005 AC (AC#1 / AC#2 / AC#3 / AC#8). Both TASK-005 Gate 3 cross-check flags (the `work-items/skill.md` line 227 *Iterative Implementation Zone* phrasing nit — route (a) survives; the bullet-structure variance — structurally required) are explicitly resolved. "No defect surfaced — Step 8 not triggered; no `shannon/` source modified" stated verbatim. EPIC-005's own Gate 3 verifier can close AC#1 / AC#2 / AC#3 / AC#8 from this single entry without diving into TASK-006.

**Recorded Deviation sound.** The incidental cleanup of three residual `A` / `B` / `C` bare-letter references in EPIC-005's Gate-2 Activity Log entry is the *completion* of the earlier `replace_all`-on-full-tokens naming-convention cleanup (commit `3683606`) — same intent, same precedent (TASK-003's FEAT-003 intro-line cleanup). Honest disclosure in Implementation Notes § Deviations.

**Findings — all Strength, no Drift / Gap / internal contradiction.** Step 8 (conditional inline correction) was a planned conditional branch, not a panic path; the Implementation Notes are unusually reproducible (every step named, every conclusion anchored at file:line); the EPIC-005 entry is deliberately the canonical index, with a "Full audit details preserved in TASK-006 (archived on approval)" pointer back.

**Alignment.** Closes EPIC-005 AC#1, AC#2, AC#3, AC#8 via the single canonical Activity Log entry. The remaining EPIC-005 ACs were closed by TASK-004 (AC#6 CLAUDE.md template; AC#7 project-setup user-content vs directing-party distinction) and TASK-005 (AC#4 `PLAN-PENDING` removal; AC#5 work-items § Process: Implement step 5). **All 8 EPIC-005 Acceptance Criteria are now satisfied.** The Epic is ready to reach its own Gate 3.

---

## Activity Log

- **2026-05-23** — APPROVED via Gate 3. Directing party approved after verification — all Acceptance Criteria MET. Re-surveys reproduced the elaboration baseline exactly (4 preserve-sense `[Uu]sers?`; zero `PENDING`; 6 self-id lines verbatim). EPIC-005's audit-summary Activity Log entry verified self-sufficient — all four conclusions (i)–(iv) close EPIC-005 AC#1 / AC#2 / AC#3 / AC#8 cleanly; both TASK-005 cross-check flags resolved. Recorded Deviation (incidental `A` / `B` / `C` bare-letter cleanup completing the prior `replace_all`) sound — honest disclosure with TASK-003 precedent. All findings Strength; no Drift, Gap, or internal contradiction. Step 8 (conditional inline correction) was not triggered; no `shannon/` source modified. Status REVIEW → APPROVED; file archived to `docs/tasks/archive/`. **All 8 EPIC-005 Acceptance Criteria are now satisfied** — the Epic is ready for its own Gate 3.
- **2026-05-23** — REVIEW. Status IMPLEMENTED → REVIEW; Gate 3 verification subagent spawned to audit the 11 Acceptance Criteria against the EPIC-005 Activity Log audit-summary entry (Step 9 write), the post-edit file states, and the recorded Deviation (incidental A/B/C bare-letter cleanup).
- **2026-05-23** — IMPLEMENTED. All 11 plan steps executed. The audit completed cleanly: `rg [Uu]sers?` returned exactly the 4 expected preserve-sense matches; `rg PENDING` returned zero; all 6 self-identification lines verbatim; § Quality Gates phrase-level equivalence confirmed; bullet-structure variance confirmed structurally required; line 227 route-(a) verdict held. **Step 8 (conditional inline correction) was NOT triggered** — no defect surfaced. Step 9 wrote the single canonical audit-summary entry to EPIC-005's Activity Log, enumerating the four conclusions (i)–(iv) that close EPIC-005 AC#1 / AC#2 / AC#3 / AC#8. Incidental in-scope cleanup: 3 residual `A` / `B` / `C` bare-letter references in EPIC-005's Gate-2 Activity Log entry (survivors of the earlier naming-convention `replace_all`) fixed in passing during the Step 9 write — recorded in § Deviations. No `shannon/` source modified; scope-guard `git status` confined to this Task file + EPIC-005. Ready for Gate 3.
- **2026-05-23** — IMPLEMENTING via `/task-implement TASK-006`. Status PLANNED → IMPLEMENTING; executing the 11-step audit against the post-TASK-005 final-text state of the four EPIC-005 files.
- **2026-05-23** — PLANNED via Gate 2. Planning subagent drafted an 11-step plan: orientation → `[Uu]sers?` / `PENDING` / self-id surveys (Steps 2–3) → gate-approval and self-approval-prevention audits (Steps 4–5) → four-way § Quality Gates phrase audit (Step 6) → line-227 verdict route (a) (Step 7) → **conditional Step 8** inline correction + `.claude/` re-sync (runs only if a defect surfaces) → single EPIC-005 Activity Log audit-summary entry (Step 9) → `git status` scope-guard (Step 10) → Markdown / Commit Cadence compliance (Step 11). All 11 Acceptance Criteria mapped to Steps; 6 risks with mitigations. Two execution paths: **expected** (Steps 1–7, 9–11 — bookkeeping only) and **conditional** (Step 8 inline fix if a surprise surfaces). Plan approved by directing party without modification.
- **2026-05-23** — ELABORATED via Gate 1. Elaboration subagent ran the preliminary `rg` survey across the four EPIC-005 target files and drafted 11 testable Acceptance Criteria in 9 groups. Survey findings: 4 surviving `[Uu]sers?` matches (all preserve-sense — `project-setup/skill.md:69/83/93`, `templates/CLAUDE.md:25`); zero `PENDING`; all six self-identification lines verbatim; § Quality Gates phrase-level equivalence between `project-documentation/skill.md` and `work-items/skill.md` (verbatim TASK-004 house phrases). Sharp observation: the bullet-structure variance between the two skills' § Quality Gates is **required** (each skill enumerates only the gates it owns — `project-documentation` owns Gate 1 only; `work-items` owns Gates 1/2/3) — a structural necessity, not a defect. Two TASK-005 Gate 3 cross-check flags pre-judged: (a) the line 227 *Iterative Implementation Zone* phrasing nit is sufficient and survives the audit (V6-compatible, met under TASK-005's loose AC wording); (b) the bullet-structure variance is required and phrase-level equivalence holds. Directing party approved the requirements without modification, confirming route (a) on the line-227 pre-judgment. The audit's formal pass at Implement is now mostly bookkeeping unless surprises emerge.
- **2026-05-23** — DRAFT: Task created via `/task-create` under EPIC-005 — the final verification Task in EPIC-005's three-Task plan. Created after both TASK-004 and TASK-005 were APPROVED, per the Plan's recommendation (children created individually rather than pre-stashed; the verification Task in particular deferred so its `rg` survey runs against final text). Initial intent captured in the Overview and Context, including two cross-check findings flagged at TASK-005's Gate 3 (line 227 *Iterative Implementation Zone* phrasing nit; `work-items` vs `project-documentation` § Quality Gates bullet-structure variance). Acceptance Criteria and Plan to be drafted at `/task-elaborate` and `/task-plan`.
