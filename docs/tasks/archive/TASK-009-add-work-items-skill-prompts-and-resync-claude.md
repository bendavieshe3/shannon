# TASK-009: Add work-items Skill Prompts at § Process: Plan Steps 2 and 3, and Re-sync .claude/

## Metadata

- **Status**: APPROVED
- **Type**: Task
- **Parent**: [EPIC-007](../epics/EPIC-007-work-item-conventions-from-dogfooding.md)
- **Feature**: [FEAT-003](../features/FEAT-003-unified-work-item-model.md)
- **Tags**: #framework #work-items #skills #conventions
- **Created**: 2026-05-25
- **Updated**: 2026-05-25 (approved)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

*Initial intent captured at `/task-create` (2026-05-25). Full Requirements drafted at `/task-elaborate TASK-009`.*

### Overview

**Task 2 of EPIC-007.** This Task lands the two soft prompts in `shannon/skills/work-items/skill.md` that mirror the `conceptual_design.md` conventions TASK-008 ratified at the framework level, then re-syncs the deployed copy at `.claude/skills/work-items/skill.md` per the TASK-005 precedent. The two soft prompts close the skill-half of EPIC-007's AC#3 (naming convention) and AC#4 (scope-guard rule), plus the inbound half of AC#6's two intra-Epic cross-reference pairs.

**The two soft prompts** — locations definitively resolved at EPIC-007 § Plan (implementing Task may override with recorded rationale):

- **EPIC-007 AC#3 skill-half — Naming-convention prompt** lands at `shannon/skills/work-items/skill.md` § Process: Plan **step 3 (*Cascading Preparation (for Epics)*)** — the precise moment the planning subagent allocates child-Task IDs and slugs. Instructs use of descriptive titles from the outset; disallows letter-placeholder transients (TASK-A, TASK-B, TASK-C). References the *Work Item* entity naming note in `conceptual_design.md` § Domain Model by section anchor.

- **EPIC-007 AC#4 skill-half — Scope-guard prompt** lands at `shannon/skills/work-items/skill.md` § Process: Plan **step 2 (*Spawn Planning Subagent*)** — under the bullet describing AC drafting. References the failure-mode test verbatim (*"if the AC's failure mode is 'an unintended file changed'"*, or in the canonical double-quote form per TASK-008's resolution: *"if the AC's failure mode is \"an unintended file changed\""*) and points to the § Business Rules entry in `conceptual_design.md` by section anchor.

**Explicit `.claude/` re-sync step** per the TASK-005 precedent: after editing `shannon/skills/work-items/skill.md` (source), copy to `.claude/skills/work-items/skill.md` (deployed copy) so the deployed framework does not drift from source. Re-sync is a named checklist item the implementer must tick, not an afterthought; verified at Gate 3 by diff-comparing the two files.

**Cross-Epic sequencing — this Task lands first** (per directing-party decision 2026-05-25 after TASK-008 APPROVED). EPIC-008 Task 2 (also editing `work-items/skill.md` for the meta-gap routing-channel prompts at § Process: Plan + § Process: Review) will rebase against this Task's landed state. The four prompts target distinct anchor points (EPIC-007 step-internal at step 2's AC bullet + step 3 Cascading Preparation; EPIC-008 step-adjoining at § Process: Plan as a whole + § Process: Review as a whole) — no textual collision is intrinsic. The mechanical check at EPIC-008 Task 2's implementation time: read `work-items/skill.md`, identify which prompts already exist, add the missing ones, re-sync `.claude/`.

**Minor housekeeping piggyback** noted but **out of scope for this Task** per the convention codified by TASK-008 AC5 (Scope-Boundary ACs use cross-type guards; modifying `development_guide.md` is out of this Task's scope): the now-redundant HTML comment at `development_guide.md:79` (`<!-- anchor will be created by EPIC-007's implementing Task per EPIC-007 AC#4 -->`) — the anchor it points at now exists at `conceptual_design.md:179`. Removal is appropriate for EPIC-007 Gate 3 review work, not for this implementing Task. Mentioned here only so the directing party doesn't expect it to land in TASK-009's diff.

### Acceptance Criteria

*Drafted at `/task-elaborate TASK-009` (2026-05-25) — anchors confirmed against the live `shannon/skills/work-items/skill.md` (298 lines) and the byte-identical deployed copy at `.claude/skills/work-items/skill.md` (298 lines; `diff` empty at elaboration time, so no pre-existing source-vs-deployed drift to remediate). Each criterion names a verifiable outcome — the section, the surrounding anchor, and the required verbatim wording where AC text demands it. Closes EPIC-007 AC#3 skill-half (AC1 + AC5 below), AC#4 skill-half (AC2 + AC6 below), AC#6 inbound half (AC5 + AC6 below — the bidirectional completion of the two intra-Epic cross-reference pairs whose outbound halves TASK-008 landed at `conceptual_design.md:58` and `conceptual_design.md:179`); AC3 covers the explicit `.claude/` re-sync per the TASK-005 precedent; AC4 covers preservation of the existing source↔deployed alignment; AC7 covers additivity verification; AC8 covers the scope-bounded-diff with explicit accounting for the gitignored deployed copy; AC9 covers no-commit-mid-implementation.*

- [ ] **AC1 — Naming-convention prompt lands at § Process: Plan step 3 (*Cascading Preparation (for Epics)*).** § Process: Plan step 3 (currently lines 188–190 of `shannon/skills/work-items/skill.md`: header `### 3. Cascading Preparation (for Epics)` at line 188; single paragraph at line 190 beginning *"If the work item is an Epic, the planning subagent may draft tasks for the epic. …"*) gains a new instruction targeting the planning subagent at the precise moment it allocates child-Task IDs and slugs. The instruction must **(i)** require **descriptive titles from the outset** (a real descriptive title used as soon as the child Task is created, paired with the allocated real ID); **(ii)** **disallow letter-placeholder transients** by naming `TASK-A, TASK-B, TASK-C` verbatim as the exclusion form; **(iii)** name **position language** (*"the first Task"*, *"the verification Task"*) as the permitted way to express ordering when needed. The instruction is added as a new bullet or sentence within step 3's existing paragraph, not as a new numbered sub-step (additive shape preserving step 3's prose). Verifiable by `grep`: `grep -n "TASK-A, TASK-B" shannon/skills/work-items/skill.md` returns one hit inside step 3's anchor; `grep -n "descriptive title" shannon/skills/work-items/skill.md` returns at least one hit at step 3's anchor; `grep -n "the first Task" shannon/skills/work-items/skill.md` returns at least one hit at step 3's anchor. The existing prose of step 3 (the `(for Epics)` header; the *prepared elaboration draft* / *prepared plan draft* mechanic; the per-child Gate 1 / Gate 2 requirement; the `(see § Cascading Operations)` cross-reference) is preserved unchanged.

- [ ] **AC2 — Scope-guard prompt lands at § Process: Plan step 2 (*Spawn Planning Subagent*, AC-drafting bullet).** § Process: Plan step 2 (currently lines 178–186 of `shannon/skills/work-items/skill.md`: header `### 2. Spawn Planning Subagent` at line 178; AC-drafting bullet at line 184: *"- Drafts the Plan section: approach, steps/tasks/epics (depending on type), dependencies, risks"*) gains a new instruction immediately attached to the AC-drafting bullet (either as a sub-bullet indented under line 184, or as an inline qualifying clause on line 184 itself — implementer's choice on a per-Markdown-rendering basis with rationale recorded in Implementation Notes). The instruction must repeat the failure-mode test phrasing **letter-for-letter in the canonical double-quote form TASK-008 resolved at `conceptual_design.md:181`**: `if the AC's failure mode is "an unintended file changed"` (with the inner phrase in double quotes; see TASK-008 § Implementation Notes → *Deviations from Plan* third bullet, which established that the double-quote form matches EPIC-007 AC#4's quoted-block canonical form and supersedes the single-quote form in EPIC-007 AC#4's verifiable-test wrapper). The instruction must direct the planning subagent to apply this failure-mode test when drafting any scope-asserting AC and to use **cross-type-guard phrasing** rather than literal file counts or fixed file lists. Verifiable by `grep`: `grep -n "an unintended file changed" shannon/skills/work-items/skill.md` returns one hit at step 2's anchor; `grep -n "cross-type" shannon/skills/work-items/skill.md` returns at least one hit at step 2's anchor (the recommended-phrasing reference). The existing prose of step 2 (the four pre-existing bullets at lines 182–186 — Requirements read, Informed By read, Plan section draft, child item identification, structured summary return) is preserved unchanged; only the AC-drafting bullet at line 184 acquires the new instruction.

- [ ] **AC3 — `.claude/skills/work-items/skill.md` is re-synced from source per the TASK-005 precedent.** After the AC1 and AC2 edits to `shannon/skills/work-items/skill.md` (the source), the deployed copy at `.claude/skills/work-items/skill.md` is re-synced from source. The re-sync mechanic follows TASK-005's archived precedent: `cp -R shannon/skills/work-items/ .claude/skills/work-items/`. Verified post-sync by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning **no differences** (alternative `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning exit code 0 / empty output is equivalent). Re-sync is a named checklist item in this Task's Plan Steps — not an afterthought — because `.claude/` is gitignored per repository convention and an omission leaves no tracked diff: the deployed framework silently drifts from source until the next time a reader compares the two by hand.

- [ ] **AC4 — Pre-existing source↔deployed alignment is preserved.** At elaboration time (2026-05-25), `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns empty (both files are 298 lines and byte-identical). This Task's only changes to either file are the AC1 and AC2 source-side edits plus AC3's re-sync of those same changes into the deployed copy. **No other content of the deployed copy is altered**: the re-sync mechanic at AC3 produces a deployed copy that differs from the source only inasmuch as the source was newly edited at AC1 and AC2; no whitespace, line-ending, or rendering normalisation is introduced. Verifiable at Gate 3 by `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty (the byte-identical invariant holds across the Task's diff range, mid-edit moments excepted).

- [ ] **AC5 — Naming-convention prompt cross-references `conceptual_design.md` § Domain Model → *Work Item* (Naming note) verbatim.** The AC1 prompt at § Process: Plan step 3 carries an inline cross-reference naming the file path and the section anchor verbatim: **`conceptual_design.md § Domain Model → *Work Item*`** (italic markers around `Work Item` matching the entity-description convention; the Naming note landed by TASK-008 sits in this entity at line 58 of `conceptual_design.md`). The cross-reference uses the same prose form as the existing skill's cross-references (e.g. line 159: `` `conceptual_design.md § Re-elaborating a Work Item` ``; line 291: same form) — backtick-quoted file path, `§` symbol, section name in plain prose, no Markdown link. Verifiable: `grep -n "conceptual_design.md" shannon/skills/work-items/skill.md` returns at least one new hit at step 3's anchor (in addition to the two pre-existing hits at lines 159 and 291); the named target anchor at `conceptual_design.md:58` resolves to the *Work Item* entity Naming note (which exists as of TASK-008 APPROVED 2026-05-25, commit `dd8b0a5` — the inbound half of the bidirectional pair completes on this AC landing, **no dangling-anchor HTML comment required because the target exists**).

- [ ] **AC6 — Scope-guard prompt cross-references `conceptual_design.md` § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* verbatim.** The AC2 prompt at § Process: Plan step 2 carries an inline cross-reference naming the file path and the section anchor verbatim: **`conceptual_design.md § Business Rules → Scope-Boundary Acceptance Criteria Use Cross-Type Guards`**. The bold-lead text *"Scope-Boundary Acceptance Criteria Use Cross-Type Guards"* is reproduced **letter-for-letter** to match the bold lead TASK-008 landed at `conceptual_design.md:179` and the cross-reference text at `development_guide.md:79` (the three landings of the same string must remain identical so the cross-Epic and intra-Epic cross-references all resolve to one canonical anchor). The cross-reference uses the same prose form as the existing skill's cross-references. Verifiable: `grep -n "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" shannon/skills/work-items/skill.md` returns one hit at step 2's anchor; the three landings (skill + conceptual_design + development_guide) all contain the identical bold-lead substring, confirmable by running the same grep against each file. The named target anchor at `conceptual_design.md:179` resolves to the § Business Rules entry (which exists as of TASK-008 APPROVED 2026-05-25, commit `dd8b0a5` — the inbound half of the bidirectional pair completes on this AC landing, **no dangling-anchor HTML comment required because the target exists**).

- [ ] **AC7 — Additivity verification performed and recorded.** Per the dogfooded equivalent of `conceptual_design.md` § Re-reviewing → *Status semantics* applied to skill content (skills are not gated documents but the same additive/substantive logic applies — does the new prose contradict existing prose?): the two new prompts (AC1 at step 3; AC2 at step 2) are verified to be **additive** — they add new instructions to existing step paragraphs without revising any pre-existing instruction. The implementing Task records a single-sentence verification per prompt in § Implementation Notes (e.g. *"AC1 naming-convention prompt — additive: step 3's existing paragraph is silent on title shape and letter placeholders; the new instruction adds guidance without revising the prepared-draft mechanic or the Gate 1 / Gate 2 child-approval rule."*). Specifically: **(a)** step 2's four pre-existing bullets (Requirements read, Informed By read, Plan section draft, structured summary return) and the AC-drafting bullet's existing wording (`approach, steps/tasks/epics …`) are unchanged in semantic content; **(b)** step 3's existing prose about the *prepared elaboration draft* / *prepared plan draft* mechanic, the per-child Gate 1 / Gate 2 requirement, and the `(see § Cascading Operations)` cross-reference is unchanged; **(c)** no other section of the skill (§ Purpose, § When to Invoke, § Templates, § Unified Status Model, § Work Item ↔ Document Relationships, § Process: Create, § Process: Elaborate, § Process: Implement, § Process: Review, § Quality Gates, § Cascading Operations, § Failure Modes, § Self-Identification) is modified. If any prompt turns out to contradict existing prose (unexpected), implementation pauses and the directing party is flagged before landing — mirroring TASK-008 AC11's protocol.

- [ ] **AC8 — Scope is bounded by the cross-type guard codified at `conceptual_design.md:179` (the rule TASK-008 ratified); the gitignored deployed copy is verified separately by `diff`.** This Task modifies exactly the following **tracked** files (those visible in `git status`): `shannon/skills/work-items/skill.md` (the source — AC1 and AC2 edits); `docs/tasks/TASK-009-add-work-items-skill-prompts-and-resync-claude.md` (this file — Status / Activity Log / Implementation Notes per the unified lifecycle); `docs/tasks/task_index.md` (TASK-009 status transitions per the unified lifecycle); `docs/epics/EPIC-007-work-item-conventions-from-dogfooding.md` (§ Plan Task 2 entry status update at line 127). **In addition**, the gitignored file `.claude/skills/work-items/skill.md` is modified by AC3's re-sync — this modification will **not appear in `git status`** because `.claude/` is repository-gitignored; it is verified separately by `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty (the AC3 / AC4 verification mechanism). Specifically out of scope: any other file under `shannon/skills/` (no other skill modified); any other file under `.claude/`; any mandated document (`vision.md`, `technology_stack.md`, `conceptual_design.md`, `technical_design.md`, `development_guide.md`, `ux_guide.md` — `conceptual_design.md` was TASK-008's territory and is now stable; `development_guide.md:79`'s now-redundant HTML comment is EPIC-007 Gate 3 territory, not this Task's); any work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` (EPIC-007 AC#7 Epic Gate 3 territory); `docs/knowledge/` (EPIC-008 AC#7 single-sourcing territory). **This AC is phrased as a cross-type guard per the very rule the prompt at AC2 references — *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* at `conceptual_design.md:179` — not as a literal file count.** A second pleasing recursion (the first was TASK-008 AC12): the Task that lands the skill-half of the scope-guard rule disciplines its own scope assertion by it. Verified at Gate 3 by `git status` against the cross-type guard (for tracked files) plus the AC4 `diff` check (for the gitignored deployed copy).

- [ ] **AC9 — Implementation does not commit mid-implementation.** Per `development_guide.md` § Commit Cadence, the implementer does not commit until Gate 3 approval. Pre-Gate-3 experimentation is fine — multiple in-progress edits across the source and deployed copies are expected — but no `git commit` runs before Gate 3 approval lands. The Gate-3 commit also triggers a push per `development_guide.md` § Push Cadence trigger (a) — after every Gate 3 approval (the rule TASK-007 itself ratified at commit `eac486b`). This AC governs the Task's own execution, not its output; verified at Gate 3 by checking `git log master..HEAD` for any pre-Gate-3 commit attributable to this Task's diff range — expectation: zero such commits.

### Context

- **Parent Epic**: [EPIC-007 — Work-Item Conventions Surfaced Through Dogfooding](../epics/EPIC-007-work-item-conventions-from-dogfooding.md) — this is Task 2 of 2; closes EPIC-007 AC#3 skill-half, AC#4 skill-half, AC#6 inbound half (the skill prompts cross-referencing their `conceptual_design.md` counterparts by file path + section anchor). EPIC-007 AC#7 (scratchpad bookkeeping) remains Epic Gate 3 work.
- **Parent Feature**: [FEAT-003 — Unified Work Item Model](../features/FEAT-003-unified-work-item-model.md) — Ideal State bullet 4 (added 2026-05-24 re-elaboration) anchors EPIC-007; this Task delivers the *"relevant skills"* half of bullet 4, completing the work-item-conventions half (re-elaboration mechanics + AC writing patterns + naming discipline) that TASK-008 delivered at the doc level.
- **Source-of-truth body**: `shannon/skills/work-items/skill.md` (the source). Specific anchor points to confirm at elaboration: § Process: Plan step 2 (*Spawn Planning Subagent*, AC-drafting bullet — for AC#4 skill-half); § Process: Plan step 3 (*Cascading Preparation (for Epics)* — for AC#3 skill-half).
- **Deployed copy** (must re-sync after editing source): `.claude/skills/work-items/skill.md`. Note `.claude/` is gitignored per repository convention but the file exists and is the runtime artefact Claude Code reads; re-syncing it is required for changes to take effect in the deployed framework.
- **Sibling Task (already APPROVED)**: TASK-008 (EPIC-007 Task 1, APPROVED 2026-05-25, commit `dd8b0a5`) landed the four `conceptual_design.md` amendments this Task's prompts cross-reference. Specifically:
  - The naming convention at `conceptual_design.md` § Domain Model → *Work Item* (Naming note, line 58) — what the AC#3 skill-half prompt at step 3 cross-references inbound.
  - The § Business Rules entry *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* (line 179) — what the AC#4 skill-half prompt at step 2 cross-references inbound.
- **Cross-Epic sibling (to follow)**: EPIC-008 Task 2 (to be created) will add meta-gap routing-channel prompts at § Process: Plan and § Process: Review of the same skill file; rebases against this Task's landed state.
- **Precedent for `.claude/` re-sync**: TASK-005 (archived) established that source lives in `shannon/` and deployed copy in `.claude/`; an edit to source without a corresponding `.claude/` copy lets the deployed framework drift from the framework's own ratified rules. TASK-009 explicitly re-syncs.
- **Conventions this Task itself dogfoods**:
  - TASK-007's editing-order convention (`development_guide.md:79`): `shannon/skills/work-items/skill.md` is the source-of-truth body; `.claude/skills/work-items/skill.md` is a derived artefact (a deployed copy of the source); edit the body first, then re-sync the derived copy, then verify by diff-comparing.
  - TASK-008's scope-guard rule (`conceptual_design.md:179`): the scope-bounded-diff AC (mirroring TASK-007 AC11 / TASK-008 AC12) uses cross-type guard phrasing, not a literal file count.
- **Source artefacts** (the scratchpad items addressed by EPIC-007 AC#7 at Epic Gate 3): "Naming convention: avoid opaque plan-letter labels (TASK-A/B/C) for Tasks" (entire item addressed by this Epic — closes at EPIC-007 Gate 3); "Framework clarifications buried in TASK-003's Plan" sub-items (b) commit-hash and (c) bundled re-elaboration (addressed by TASK-008; scratchpad move conditional on EPIC-008 status per the second-approver-moves rule).

---

## Plan

*Drafted at `/task-plan TASK-009` (Gate 2) — 2026-05-25.*

### Approach

A **two-file editing pass** against `shannon/skills/work-items/skill.md` (source-of-truth body) plus an explicit `cp -R` re-sync of `.claude/skills/work-items/` (gitignored deployed copy), followed by `diff` verification, verbatim-phrasing `grep` verification, additivity verification (per the dogfooded *Status semantics* test applied to skill content), derived-bookkeeping updates (`task_index.md` + EPIC-007 § Plan Task 2 entry at line 127), and scope-confined-diff verification against the cross-type guard at AC8 — with **explicit accounting for the gitignored `.claude/` copy that will not appear in `git status`** (verified separately by `diff`). The Task itself dogfoods both TASK-007's editing-order convention (`development_guide.md:79` — source-of-truth body before derived artefacts, then verify) and the very rule its AC2 prompt cites (`conceptual_design.md:179` — *Scope-Boundary Acceptance Criteria Use Cross-Type Guards*) — a paired recursion mirroring TASK-008.

**Edit order — strictly bottom-to-top by pre-edit line number** for the two substantive source insertions. AC1 (step 3 paragraph at **line 190**) has the higher line number → first source edit. AC2 (step 2 AC-drafting bullet at **line 184**) has the lower line number → second source edit. With only two substantive insertions the discipline is trivial here, but stated explicitly per the convention TASK-007 ratified at `development_guide.md:79` and TASK-008's 12-step precedent.

**Sub-position decision for AC2 deferred at elaboration — Plan recommends sub-bullet (a).** The implementer chooses between (a) a sub-bullet indented under the AC-drafting bullet at line 184, or (b) an inline qualifying clause appended to line 184's existing prose. The Plan recommends **(a) sub-bullet** as the cleaner reading: it preserves line 184's existing concise statement intact; the new scope-guard prompt sits visually under it as a refining instruction rather than reshaping the parent bullet. Rationale recorded in Implementation Notes at landing.

**Verbatim phrasings — five literal substrings copied letter-for-letter** from TASK-008's landed canonical forms (not retyped from memory):

- AC1 prompt: `TASK-A, TASK-B, TASK-C` (exclusion form); `the first Task` (position-language alternative); `descriptive title` (positive requirement).
- AC2 prompt: `if the AC's failure mode is "an unintended file changed"` — **canonical double-quote form** per TASK-008 § Implementation Notes → *Deviations from Plan* third bullet (resolved at `conceptual_design.md:181`).
- AC6 cross-reference target: `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` — bold-lead text reproduced letter-for-letter to match the three-way landing across the skill + `conceptual_design.md:179` + `development_guide.md:79`.

**Re-sync is a named explicit step** with the exact `cp -R` command from TASK-005's archived precedent: `cp -R shannon/skills/work-items/ .claude/skills/work-items/`. Verified by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning no differences.

**Scope-confined-diff verification requires two complementary mechanisms** because `.claude/` is gitignored: (i) `git status` confirms exactly 4 tracked files modified; (ii) `diff` confirms the deployed copy matches source. Both must hold for AC8 to pass.

**Three-way bold-lead consistency check** at Gate 3 — the same grep of `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` against source skill + `conceptual_design.md` + `development_guide.md` returns ≥1 hit each, all character-identical.

**Commit deferral per AC9 + § Commit Cadence.** No `git commit` runs mid-implementation; the Gate-3 commit lands after directing-party approval and triggers a push per `development_guide.md` § Push Cadence trigger (a) — the rule TASK-007 itself ratified at commit `eac486b`.

### Steps

Steps proceed in **strict bottom-to-top order by pre-edit line number** through the two substantive source insertions, then the re-sync, then verifications, then derived bookkeeping, then scope-confined-diff verification. Each step names its AC coverage explicitly.

1. **Pre-flight: re-read `shannon/skills/work-items/skill.md` end-to-end and confirm anchors haven't drifted since ELABORATED.** Confirm: `### 2. Spawn Planning Subagent` at line 178; the AC-drafting bullet at line 184 reading exactly `- Drafts the Plan section: approach, steps/tasks/epics (depending on type), dependencies, risks`; `### 3. Cascading Preparation (for Epics)` at line 188; the existing paragraph at line 190 beginning *"If the work item is an Epic, the planning subagent may draft tasks for the epic. …"*. Re-run `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` and confirm empty output (clean baseline still holds — last confirmed empty at elaboration). *(Orientation only — closes no AC; baseline for AC3 / AC4 / AC7.)*

2. **Insert AC1 naming-convention prompt at § Process: Plan step 3 (line 190) — higher pre-edit line number → first source edit.** The prompt is added inside step 3's existing paragraph as an additional bullet or sentence (not as a new numbered sub-step — preserves step 3's additive shape per AC1's *"additive shape preserving step 3's prose"* clause). The instruction must satisfy three required elements: **(i)** require **descriptive titles from the outset** (a real descriptive title paired with the allocated real ID as soon as the child Task is created); **(ii)** **disallow letter-placeholder transients** naming `TASK-A, TASK-B, TASK-C` verbatim as the exclusion form; **(iii)** name **position language** (*"the first Task"*, *"the verification Task"*) as the permitted way to express ordering when needed. Carries the inline cross-reference verbatim: `` `conceptual_design.md` § Domain Model → *Work Item* `` (italic markers around `Work Item`; backtick-quoted file path; `§` symbol; no Markdown link — matches existing skill house style at lines 159 + 291). The cross-reference target at `conceptual_design.md:58` resolves to the *Work Item* entity Naming note (landed by TASK-008 APPROVED 2026-05-25 commit `dd8b0a5`; inbound half of EPIC-007 AC#6 bidirectional pair completes on this AC landing — no dangling-anchor HTML comment required because the target exists). Preserves step 3's existing prose intact: the `(for Epics)` header; the *prepared elaboration draft* / *prepared plan draft* mechanic; the per-child Gate 1 / Gate 2 requirement; the `(see § Cascading Operations)` cross-reference. Edit-tool anchor: the unique substring `If the work item is an Epic, the planning subagent may draft tasks for the epic.` at line 190. *(Closes AC1, AC5.)*

3. **Insert AC2 scope-guard prompt under § Process: Plan step 2 (line 184) — lower pre-edit line number → second source edit.** The prompt is added immediately attached to the AC-drafting bullet at line 184. **Plan recommends sub-bullet form (a) — indented under the parent bullet** — with rationale recorded in Implementation Notes (preserves line 184's existing concise statement; the new prompt sits visually as a refinement under it). (Implementer may override to inline qualifying-clause form (b) with rationale recorded; both forms permitted by AC2.) The instruction repeats the failure-mode test phrasing letter-for-letter in the canonical double-quote form: `if the AC's failure mode is "an unintended file changed"`. It directs the planning subagent to apply this failure-mode test when drafting any scope-asserting AC and to use **cross-type-guard phrasing** rather than literal file counts or fixed file lists. Carries the inline cross-reference verbatim: `` `conceptual_design.md` § Business Rules → Scope-Boundary Acceptance Criteria Use Cross-Type Guards ``. The bold-lead text `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` is reproduced letter-for-letter so the three-way landing across the skill + `conceptual_design.md:179` + `development_guide.md:79` remains character-identical. Preserves step 2's existing four bullets intact (Requirements read; Informed By read; Plan section draft; child item identification; structured summary return). Edit-tool anchor: the unique substring `- Drafts the Plan section: approach, steps/tasks/epics (depending on type), dependencies, risks` at line 184. *(Closes AC2, AC6.)*

4. **Re-sync `.claude/skills/work-items/` from source.** Execute the exact TASK-005 precedent command: `cp -R shannon/skills/work-items/ .claude/skills/work-items/`. The recursive copy preserves the entire skill directory including any sibling templates. *(Closes AC3 — the named re-sync mechanic; supports AC4 — preserves source↔deployed alignment for content other than the two new prompts.)*

5. **`diff` verification post-re-sync.** Run `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` and confirm **no differences output**. (Equivalent alternative: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty / exit code 0.) Record the verification result in Implementation Notes (the verification is the AC3 / AC4 evidence at Gate 3; `.claude/` being gitignored leaves no tracked diff — the Activity Log records the re-sync explicitly so Gate 3 can confirm independently). *(Closes AC3 — re-sync mechanic verified; closes AC4 — source↔deployed byte-identical invariant preserved across the diff range.)*

6. **Additivity verification per the dogfooded *Status semantics* test applied to skill content.** Two single-sentence statements drafted into Implementation Notes — one per new prompt: **AC1 naming-convention prompt** — *additive*: step 3's existing paragraph is silent on title shape and letter placeholders; the new instruction adds guidance without revising the prepared-draft mechanic, per-child Gate 1 / Gate 2 requirement, or `(see § Cascading Operations)` cross-reference. **AC2 scope-guard prompt** — *additive*: step 2's existing four bullets and the AC-drafting bullet's existing wording are unchanged in semantic content; the new sub-bullet adds AC-drafting guidance without revising any pre-existing instruction. If either prompt turns out to contradict existing prose (unexpected), implementation pauses and the directing party is flagged before landing — mirroring TASK-008 AC11 / TASK-007 AC10 protocol. *(Closes AC7.)*

7. **Verbatim-phrasing `grep` verification — six literal-substring searches against the landed source plus the three-way consistency check.**
   - `grep -n "TASK-A, TASK-B" shannon/skills/work-items/skill.md` → **1 hit** at step 3 anchor.
   - `grep -n "the first Task" shannon/skills/work-items/skill.md` → **≥1 hit** at step 3 anchor.
   - `grep -n "an unintended file changed" shannon/skills/work-items/skill.md` → **1 hit** at step 2 anchor.
   - `grep -n "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" shannon/skills/work-items/skill.md` → **1 hit** at step 2 anchor.
   - **Three-way consistency check**: same grep against `docs/conceptual_design.md` returns 1 primary hit at line 179; against `docs/development_guide.md` returns 1 hit at line 79; all three landings character-identical. (Drift here breaks the cross-reference resolution and is a meta-instance of the very fragility AC5/AC6's prompts prevent.)
   - `grep -n "conceptual_design.md" shannon/skills/work-items/skill.md` → **≥3 hits** (2 pre-existing at lines 159 + 291; ≥1 new from this Task's cross-references).

   *(Closes the verbatim-phrasing portion of AC1, AC2, AC5, AC6; provides Gate 3 reviewer's verification baseline.)*

8. **Update derived bookkeeping — `task_index.md` and EPIC-007 § Plan Task 2 entry at line 127.** Update `docs/tasks/task_index.md` TASK-009 line to reflect new status at each transition (PLANNED at Plan acceptance; IMPLEMENTING when implementation begins; IMPLEMENTED at completion of Steps 1–7; REVIEW at Gate 3 entry; APPROVED at Gate 3 exit). Update `docs/epics/EPIC-007-work-item-conventions-from-dogfooding.md` § Plan Task 2 entry (currently labelled `(ELABORATED, 9 ACs grounded in live shannon/skills/work-items/skill.md 298-line anchors)` at line 127) to reflect TASK-009's advancing status at corresponding moments. These are the *derived artefacts* per the editing-order convention TASK-007 codified — landed *after* the source-of-truth body (the skill). *(Bookkeeping per the unified lifecycle — not a single AC closure but supports AC8's scope-confined diff; dogfoods TASK-007's editing-order convention this Task itself is also subject to.)*

9. **Scope-confined-diff verification — two-part for the gitignored split.**
   - **(a) `git status` check** — confirm modified tracked files are exactly: `shannon/skills/work-items/skill.md` (source — AC1 + AC2 edits); `docs/tasks/TASK-009-add-work-items-skill-prompts-and-resync-claude.md` (this file — Status / Activity Log / Implementation Notes); `docs/tasks/task_index.md` (TASK-009 status transitions); `docs/epics/EPIC-007-work-item-conventions-from-dogfooding.md` (§ Plan Task 2 status update at line 127). Exactly 4 tracked files; no more, no fewer.
   - **(b) `diff` check** — `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns empty (the deployed copy matches source; AC3's re-sync landed and was not subsequently disturbed).

   Both must hold for AC8 to pass. Confirm specifically out of scope: no file under `shannon/skills/` outside `work-items/` (no other skill modified); no other file under `.claude/`; no mandated document modified (including the now-redundant HTML comment at `development_guide.md:79` — explicitly out of scope per AC8; removal is EPIC-007 Gate 3 territory); no work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` (EPIC-007 AC#7 territory) and `docs/knowledge/` (EPIC-008 AC#7 territory) unchanged. The check is phrased as the cross-type guard per the very rule landed by AC2 — not a literal file count. *(Closes AC8 — second pleasing recursion of the scope-guard rule disciplining its own implementer's scope assertion; first was TASK-008 AC12.)*

10. **No commit until Gate 3.** Per AC9 and `development_guide.md` § Commit Cadence, no `git commit` runs across Steps 1–9. Pre-Gate-3 diffs accumulate in the working tree; the Gate-3 commit lands after directing-party approval and triggers a push per `development_guide.md` § Push Cadence trigger (a) — the rule TASK-007 itself ratified at commit `eac486b`. Verified at Gate 3 by `git log master..HEAD` showing zero new commits attributable to TASK-009's diff range until the Gate-3 commit lands. *(Closes AC9.)*

### Dependencies

- **EPIC-007 PLANNED** — met (PLANNED 2026-05-24; § Plan Task 2 names TASK-009 by ID at line 127 with descriptive title).
- **TASK-008 (EPIC-007 Task 1) APPROVED** — met (APPROVED 2026-05-25, commit `dd8b0a5`). Both cross-reference targets this Task's prompts point at now exist: `conceptual_design.md` § Domain Model → *Work Item* Naming note at line 58 (AC1 / AC5 target — includes `TASK-A, TASK-B, TASK-C` exclusion verbatim); `conceptual_design.md` § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* at line 179 (AC2 / AC6 target).
- **TASK-005 (archived) precedent** — met for the `cp -R` re-sync mechanic + `diff -rq` verification (Step 4 + Step 5 lift these directly).
- **TASK-007 (EPIC-008 Task 1) APPROVED** — met. Establishes the editing-order convention this Task dogfoods at `development_guide.md:79`; also the source of the three-way bold-lead landing this Task preserves.
- **`shannon/skills/work-items/skill.md` 298 lines** — anchors at lines 178 / 184 / 188 / 190 stable (confirm at Step 1 pre-flight).
- **`.claude/skills/work-items/skill.md` byte-identical to source** — clean baseline (confirm at Step 1 pre-flight; `diff` empty at elaboration time).
- **No dependency on EPIC-008 Task 2** — TASK-009 lands first per directing-party sequencing decision (2026-05-25 after TASK-008 APPROVED); EPIC-008 Task 2 rebases against this Task's landed state. The four prompts target distinct anchor points (this Task: step-internal at step 2 AC-drafting bullet + step 3 paragraph; EPIC-008 Task 2: step-adjoining at § Process: Plan / § Process: Review as wholes) — no textual collision intrinsic.

### Risks

- **`.claude/` re-sync omission — TASK-005's flagged risk.** If Step 4 is skipped, the deployed framework silently drifts from source: `.claude/` is gitignored, so an omission leaves no tracked diff and a reader could mistake Step 5's empty `diff` (against a stale deployed copy that happens to predate this Task's source edits) for "re-sync completed". **Mitigation**: re-sync is an explicit named Step 4 with its own verification Step 5 (`diff -rq` against source); Implementation Notes records the verification result; Gate 3 re-runs the diff against the post-source-edit state.

- **Verbatim-phrasing drift on the failure-mode test.** AC2's prompt must land in canonical double-quote form `if the AC's failure mode is "an unintended file changed"` — the form TASK-008 resolved at `conceptual_design.md:181` (per TASK-008 § Implementation Notes → *Deviations from Plan* third bullet). Plausible variations: single quotes (matching EPIC-007 AC#4's older wrapper form); smart quotes (auto-conversion); missing inner quotes around the phrase; paraphrase. **Mitigation**: copy letter-for-letter from TASK-008's landed text at `conceptual_design.md:181`; Step 7 grep verifies the literal substring `an unintended file changed`; Gate 3 reviewer can re-run the grep and compare with `conceptual_design.md:181`.

- **Three-way bold-lead consistency break.** The exact string `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` must match character-for-character across three files (this Task's skill landing + `conceptual_design.md:179` + `development_guide.md:79`). Any drift — capitalisation, hyphenation, word order, italics, abbreviation — breaks cross-reference resolution and produces a meta-instance of the very fragility AC2's prompt is supposed to prevent. **Mitigation**: Step 7's three-way grep check; Implementation Notes records the three-way comparison; copy the bold-lead text from `conceptual_design.md:179` rather than retyping from memory.

- **Cross-Epic preview — EPIC-008 Task 2 textual collision.** EPIC-008 Task 2 will edit the same skill file at § Process: Plan and § Process: Review for routing-channel prompts. If TASK-009's edits inadvertently consume EPIC-008's natural insertion points (e.g. AC1 expanding to occupy the whole § Process: Plan section header area; AC2 expanding to occupy the whole step-2 area), EPIC-008 Task 2 needs structural rework, not just a rebase. **Mitigation**: AC1 lands strictly inside step 3's existing paragraph (step-internal — does not touch the § header or step boundaries); AC2 lands strictly inside step 2's existing bullet list (step-internal — does not touch the § header, step header, or step boundaries). EPIC-008's step-adjoining anchors remain untouched. Confirmed by Step 9's `git diff` review at landing.

- **Additivity violation — a new prompt unexpectedly contradicts existing prose.** Most plausible failure modes: (a) AC1's letter-placeholder exclusion implicitly contradicts existing prepared-draft handling at step 3 (e.g. if a prepared-draft mechanism somewhere expects letter placeholders as a transient stage); (b) AC2's cross-type-guard requirement implicitly revises the existing AC-drafting bullet's framing (e.g. by requiring a specific AC shape the existing bullet doesn't permit). **Mitigation**: Step 6's per-prompt additivity check, with single-sentence verification recorded in Implementation Notes; pause-and-flag protocol if non-additivity surfaces. The two new prompts are guidance additions, not revisions of existing claims, so the risk is residual but real.

- **Over-formalisation — two small soft prompts becoming heavyweight prose.** Step 2 has 5 existing bullets; step 3 has a single paragraph; each new prompt should be a single sentence (or short paragraph), not a sub-section. AC1's three required elements plus a cross-reference can easily bloat to a paragraph; AC2's failure-mode test + cross-type-guard requirement plus a cross-reference can similarly bloat. **Mitigation**: AC text already constrains shape (AC1 explicit: "new bullet or sentence within step 3's existing paragraph, not as a new numbered sub-step"; AC2 explicit: "sub-bullet … or inline qualifying clause"); Gate 3 reviewer checks proportionality against the step-internal anchor.

- **Bookkeeping drift — `task_index.md` or EPIC-007 § Plan Task 2 line not updated at the right transition.** The unified lifecycle requires both to advance at each status transition. Forgetting an update produces drift between source-of-truth (this Task file's Status field) and derived artefacts — precisely the failure mode the editing-order convention names. **Mitigation**: Step 8 names the bookkeeping update explicitly, including the specific line in EPIC-007 § Plan Task 2 (line 127) and the task_index line for TASK-009. The editing-order convention TASK-007 ratified disciplines this and is itself dogfooded by this Task.

---

## Implementation Notes

*Filled at `/task-implement TASK-009` (2026-05-25). All 10 Plan Steps executed in strict bottom-to-top order; all 9 ACs satisfied; pending Gate 3 review.*

### Additivity Verification (AC7)

Per the dogfooded *Status semantics* test applied to skill content: an amendment is additive iff it does not contradict existing approved claims. Two verification statements, one per new prompt:

- **AC1 naming-convention prompt** at § Process: Plan step 3 (line 193) — *additive*: step 3's existing paragraph (lines 188–190) is silent on title shape and letter placeholders; the new `**Naming**:` paragraph adds guidance without revising the prepared-draft mechanic, the per-child Gate 1 / Gate 2 requirement, or the `(see § Cascading Operations)` cross-reference. The existing paragraph is unchanged in semantic content.
- **AC2 scope-guard prompt** at § Process: Plan step 2 (line 185, sub-bullet form) — *additive*: step 2's existing four bullets (Requirements / Informed By reads, Plan-section draft, child-item identification, structured-summary return) and the AC-drafting bullet's existing wording (`approach, steps/tasks/epics …`) are unchanged in semantic content; the new sub-bullet adds AC-drafting guidance (apply the failure-mode test; use cross-type-guard phrasing) without revising any pre-existing instruction.

No prompt turned out non-additive; no pause-and-flag required.

### Verbatim-Phrasing Verification (Step 7)

Five literal-substring `grep` searches against the landed source skill plus the three-way bold-lead consistency check:

- `grep -nc "TASK-A, TASK-B" shannon/skills/work-items/skill.md` → **1** hit at line 193 ✓ (initially returned 0 — see *Deviations from Plan* below for the resolution)
- `grep -nc "the first Task" shannon/skills/work-items/skill.md` → **1** hit at line 193 ✓
- `grep -n "descriptive title" shannon/skills/work-items/skill.md` → **1** hit at line 193 ✓ (positive requirement)
- `grep -nc "an unintended file changed" shannon/skills/work-items/skill.md` → **1** hit at line 185 ✓ (failure-mode test, canonical double-quote form)
- `grep -nc "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" shannon/skills/work-items/skill.md` → **1** hit at line 185 ✓ (scope-guard bold-lead cross-reference target)
- `grep -n "conceptual_design.md" shannon/skills/work-items/skill.md` → **4** hits: 2 pre-existing (lines 159, 294 — both stable across the edit) + 2 new from this Task (line 185 AC2 cross-reference; line 193 AC1 cross-reference) ✓

**Three-way bold-lead consistency check** (the critical cross-reference resolution test):
- `shannon/skills/work-items/skill.md` line 185 — `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` ✓
- `docs/conceptual_design.md` line 179 — `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` ✓
- `docs/development_guide.md` line 79 — `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` ✓
- **All three character-identical.** The cross-reference resolution chain (skill → `conceptual_design.md:179` ← `development_guide.md:79`) is intact.

### Scope-Confined-Diff Verification (AC8, Step 9)

**Two-part verification** for the gitignored split:

**(a) `git status` — tracked files** showed exactly the four expected files modified:
- ` M shannon/skills/work-items/skill.md` (source — AC1 + AC2 edits at lines 185 and 193)
- ` M docs/tasks/TASK-009-add-work-items-skill-prompts-and-resync-claude.md` (this file — Status / Activity Log / Implementation Notes per the unified lifecycle)
- ` M docs/tasks/task_index.md` (TASK-009 status update)
- ` M docs/epics/EPIC-007-work-item-conventions-from-dogfooding.md` (§ Plan Task 2 entry status update at line 127)

Exactly 4 tracked files. No file under `shannon/skills/` outside `work-items/`; no mandated document modified (including the now-redundant HTML comment at `development_guide.md:79` — explicitly out of scope per AC8; removal is EPIC-007 Gate 3 territory); no work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` and `docs/knowledge/` unchanged.

**(b) `diff` — gitignored deployed-copy check**: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns **empty** post-final-re-sync — the deployed copy matches source byte-for-byte. AC3 re-sync mechanic and AC4 alignment invariant both satisfied.

Cross-type guard at AC8 satisfied. **Second pleasing recursion** confirmed (the first was TASK-008 AC12): the Task that lands the skill-half of the scope-guard rule disciplined its own scope assertion by it.

### Commit Deferral (AC9)

No `git commit` ran across Steps 1–9. Pre-Gate-3 diffs accumulate in the working tree per § Commit Cadence. `git log master..HEAD` confirmed empty over this Task's diff range. The Gate-3 commit will land after directing-party approval and trigger a push per `development_guide.md` § Push Cadence trigger (a) — landing all three pending Gate commits (`9746630` Gate 1 + `6b47ef2` Gate 2 + the Gate-3 commit) in one push.

### Deviations from Plan

- **Initial backtick formatting of `TASK-A, TASK-B, TASK-C` failed the verbatim-phrasing grep**, caught at Step 7. The original landing used Markdown code-style backticks around each letter (`` `TASK-A`, `TASK-B`, `TASK-C` ``), which is stylistically reasonable for code-token highlighting but breaks the `grep -n "TASK-A, TASK-B"` verbatim substring test (the backticks interpose between `TASK-A` and `, `). **Resolution**: removed the backticks; re-landed as plain prose `TASK-A, TASK-B, TASK-C` to match `conceptual_design.md:58`'s landed form (which also uses plain prose). Re-synced `.claude/` immediately after the fix; re-verified the grep returns 1 hit. **Lesson worth capturing**: when verbatim-phrasing tests target substrings, the landing must match the test character-for-character — Markdown code-style formatting around the substring chars breaks the test. The same principle applies to italic markers (`*`) interposed mid-substring. This drift was caught by Step 7's grep check exactly as the Plan intended — the verbatim-phrasing verification regimen is doing what it was designed to do.

- **`cp -R` command form**: Plan stated `cp -R shannon/skills/work-items/ .claude/skills/work-items/`. The shell behaviour of this exact command when target exists is to copy `work-items/` *into* `.claude/skills/work-items/` as a nested subdirectory (`.claude/skills/work-items/work-items/`) — which is not what we want. **Used instead**: `cp -R shannon/skills/work-items/. .claude/skills/work-items/` (trailing `/.` on source = "copy the contents of this directory") — copies file-by-file into the existing target. Verified post-sync by `diff -rq` returning empty. This is a sub-deviation in the Plan's command-form specification, not in the intent. **Lesson worth capturing**: the trailing-dot form is the safe pattern for `cp -R contents-into-existing-target`; documenting this on a future precedent-Task would help.

- **AC2 sub-position decision**: per Plan recommendation, landed as **sub-bullet form (a)** — indented 2-space sub-bullet under the AC-drafting bullet at line 184. The sub-bullet inherits the AC-drafting bullet's parent context naturally; no override to inline qualifying-clause form (b) needed. Rationale recorded: line 184's existing concise wording is preserved intact; the new scope-guard prompt sits visually below it as a refining instruction.

### Gotchas

- **The TASK-A/B/C backtick issue (covered above in Deviations).** Caught by Step 7's verbatim-phrasing grep — exactly the failure mode the regimen was designed to catch. Resolved in two additional edits (remove backticks; re-sync `.claude/`) before scope-confined-diff verification at Step 9, so no AC affected. Worth promoting in a future framework lesson: **verbatim phrasings used in `grep` verification cannot be Markdown-formatted with chars that break substring matching** (backticks, italic `*`, bold `**`). The plain-prose form is the safe default for substrings that participate in verifiable tests.

- **`cp -R` directory-target behaviour (covered above in Deviations).** Cost: one Plan-deviation note; no AC affected because Steps 4 + 5 verified by `diff -rq` empty, which catches both the right-command-right-result and wrong-command-right-result cases by construction. The Plan's specified command would have produced a wrong result if executed verbatim; substituting the safer trailing-dot form during implementation is a small documented divergence rather than a defect.

### Documents Updated

- **`shannon/skills/work-items/skill.md`** (source) — two soft prompts added per EPIC-007's AC#3 skill-half and AC#4 skill-half. File grew from 298 lines to 301 lines.
  - Line 185 — new sub-bullet under § Process: Plan step 2's AC-drafting bullet: scope-guard prompt with verbatim failure-mode test in canonical double-quote form + cross-reference to `conceptual_design.md` § Business Rules → *Scope-Boundary Acceptance Criteria Use Cross-Type Guards*.
  - Line 193 — new `**Naming**:` paragraph appended after § Process: Plan step 3's existing paragraph: naming-convention prompt with verbatim `TASK-A, TASK-B, TASK-C` exclusion + position-language alternative + cross-reference to `conceptual_design.md` § Domain Model → *Work Item*.
  - All other content of the skill (§ Purpose, § When to Invoke, § Templates, § Unified Status Model, § Work Item ↔ Document Relationships, § Process: Create, § Process: Elaborate, § Process: Implement, § Process: Review, § Quality Gates, § Cascading Operations, § Failure Modes, § Self-Identification) unchanged.
- **`.claude/skills/work-items/skill.md`** (deployed copy, gitignored) — re-synced from source via `cp -R shannon/skills/work-items/. .claude/skills/work-items/`. Post-sync `diff` against source returns empty (byte-identical). Re-sync ran twice in total: once after the initial AC1 + AC2 landing (Step 4), then again after the backtick-fix landing (covered in Deviations) to keep the byte-identical invariant after the source correction.

### Recursive Dogfood

**Second instance of the paired recursion pattern TASK-008 introduced** (the first was TASK-008 AC12 disciplined by TASK-008 AC5):

- **The Task dogfooded TASK-007's editing-order convention** (`development_guide.md:79`). `shannon/skills/work-items/skill.md` was the source-of-truth body; `.claude/skills/work-items/skill.md` (deployed copy) and `docs/tasks/task_index.md` + `docs/epics/EPIC-007-...md` § Plan Task 2 line (derived bookkeeping) were the derived artefacts. Steps 4 + 8 updated the derived artefacts *after* the body landed, exactly per the convention TASK-007 ratified.
- **The Task disciplined its own AC8 scope assertion by the very rule its AC2 prompt cites** — *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* at `conceptual_design.md:179`. AC8's phrasing was a cross-type guard ("no file under `shannon/skills/` outside `work-items/`, no other mandated docs, ...") rather than a literal file count. Landing the skill-half of the rule retroactively reinforces the discipline AC8 had already adopted at planning time — the rule's reach has extended from doc to skill, and the next Task that uses scope ACs will reference the skill prompt directly.
- **The three-way bold-lead consistency check** (skill + `conceptual_design.md:179` + `development_guide.md:79`) is now a routine Gate 3 verification that catches drift across the FEAT-003 / FEAT-008 split. This Task's AC6 was the second exercise of that check (the first was TASK-008's cross-Epic self-heal verification); both passed.

**One new lesson surfaced** (recorded above in Gotchas): verbatim phrasings used in `grep` verification cannot be Markdown-formatted with chars that interpose between the test substring's characters. The plain-prose form is the safe default for verbatim-test substrings — a small but high-leverage convention that will save future implementing Tasks from the same kind of late catch.

---

## Review

*Filled at `/task-review TASK-009` (Gate 3) — 2026-05-25. Independent verification subagent verdict: APPROVE. All 9 ACs Met; no Gaps or Issues.*

### Verification

- [x] All acceptance criteria met — 9/9 Met per independent subagent (AC1 line 193; AC2 line 185; AC3 + AC4 both diffs empty / both files 301 lines; AC5 line 193 cross-reference; AC6 line 185 cross-reference with three-way bold-lead match confirmed; AC7 Implementation Notes additivity block; AC8 git status 4-file tracked scope + diff empty for deployed; AC9 git log 6b47ef2..HEAD empty)
- [x] Changes follow `development_guide.md` — Markdown § Code Style conventions adhered to; the very editing-order convention TASK-007 ratified (`development_guide.md:79`) executed exactly during this Task's execution — `shannon/skills/work-items/skill.md` is the source-of-truth body; `.claude/skills/work-items/skill.md` + `task_index.md` + EPIC-007 § Plan Task 2 line are the derived artefacts (Steps 4 + 8 enacted the convention); Pre-Commit Checklist all five items tickable; § Push Cadence trigger (a) will fire on Gate-3 commit landing
- [x] Relevant documents updated — `shannon/skills/work-items/skill.md` v[298-line] → v[301-line] with two new soft prompts at § Process: Plan steps 2 + 3; `.claude/skills/work-items/skill.md` re-synced byte-identical; no mandated document modified
- [x] Knowledge captured where useful — three pleasing patterns recorded in Implementation Notes § Recursive Dogfood (second instance of TASK-008's paired-recursion pattern); two Plan deviations recorded transparently in § Deviations (TASK-A/B/C backtick fix; `cp -R` command-form trailing-dot substitution); **one framework-general lesson surfaced and flagged for scratchpad promotion**: verbatim phrasings used in `grep` verification require plain-prose substring landings — Markdown formatting (backticks, italic `*`, bold `**`) that interposes between substring chars breaks the test (caught by Step 7's grep regimen exactly as designed); appropriate for EPIC-007 or EPIC-008 Gate 3 scratchpad bookkeeping pickup

### Review Notes

- **Three-way bold-lead consistency check verified character-by-character**: `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` is identical at `shannon/skills/work-items/skill.md:185`, `docs/conceptual_design.md:179`, and `docs/development_guide.md:79`. (Plus an expected non-blocking hit at `conceptual_design.md:363` in v1.6 Version History entry — same string, expected.) The cross-reference resolution chain (skill → `conceptual_design.md:179` ← `development_guide.md:79`) is intact across the FEAT-003 / FEAT-008 split.
- **`.claude/` re-sync verified**: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` → empty; `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` → empty; both files at 301 lines. The gitignored half of the scope-confined-diff verification (which `git status` cannot show) holds independently.
- **Scope-confined-diff (tracked half) verified**: `git status -s` shows exactly the four expected tracked files (`shannon/skills/work-items/skill.md`, `docs/tasks/TASK-009-...md`, `docs/tasks/task_index.md`, `docs/epics/EPIC-007-...md`); no file under `shannon/skills/` outside `work-items/`; no mandated document modified (including no removal of the now-redundant HTML comment at `development_guide.md:79` — correctly deferred to EPIC-007 Gate 3 territory per AC8); no `docs/scratchpad.md` or `docs/knowledge/` change.
- **AC9 commit-deferral verified**: `git log 6b47ef2..HEAD --oneline` returns empty — Gate-2 commit `6b47ef2` is the most recent commit; no mid-implementation commit on this Task's diff range.
- **EPIC-007 coverage closed by this Task**: AC#3 skill-half, AC#4 skill-half, AC#6 inbound half. EPIC-007 now has only **AC#7 (scratchpad bookkeeping)** remaining for Epic Gate 3.
- **FEAT-003 § Ideal State bullet 4 ("relevant skills" half)** substantively delivered: AC writing patterns (AC2 prompt) and naming discipline (AC1 prompt) — at the moments work-item planning happens. The bullet's annotation `*(not yet met — addressed by EPIC-007)*` flips at EPIC-007 Epic Gate 3 (not at this Task in isolation).
- **Second instance of the paired-recursion pattern TASK-008 introduced** (recorded in Implementation Notes § Recursive Dogfood): (a) Task dogfoods TASK-007's editing-order convention during execution; (b) AC8 disciplined by AC2's referenced rule — scope-guard reach extended from doc to skill.
- **One non-blocking framework-general lesson flagged** for scratchpad promotion at EPIC-007 or EPIC-008 Gate 3: verbatim-phrasing `grep` tests require plain-prose substring landings (no interposing Markdown formatting). This Task's backtick-fix Deviation was the first observation of the pattern; promoting it now prevents repeat catches in future Tasks.
- **Findings**: Strengths only; Gaps/Issues: None; Suggested fixes: None required.

---

## Activity Log

- **2026-05-25** — APPROVED via Gate 3. Independent verification subagent returned all 9 ACs Met with no Gaps or Issues; full per-AC report folded into § Review. Three-way bold-lead consistency check verified character-by-character: skill.md:185 + conceptual_design.md:179 + development_guide.md:79 all identical. `.claude/` re-sync verified byte-identical to source (both 301 lines; `diff` empty). EPIC-007 coverage closed for this Task — AC#3 skill-half, AC#4 skill-half, AC#6 inbound half. Final disposition: Status REVIEW → APPROVED; file archived to `docs/tasks/archive/`; `task_index.md` link path updated; EPIC-007 § Plan Task 2 entry updated to APPROVED. Commit message: *"Approve TASK-009 (Gate 3) — Add work-items skill prompts and re-sync .claude/"*. Push triggered per § Push Cadence trigger (a) — landing all three pending Gate commits (`9746630` Gate 1 + `6b47ef2` Gate 2 + this Gate-3 commit) in one push. **EPIC-007 now has only AC#7 (scratchpad bookkeeping) remaining for Epic Gate 3** — both implementing Tasks complete. One non-blocking framework-general lesson flagged for scratchpad promotion at EPIC-007 or EPIC-008 Gate 3: verbatim-phrasing `grep` tests require plain-prose substring landings (no interposing Markdown formatting that breaks substring matching). This lesson is the durable knowledge captured by TASK-009's backtick-fix Deviation.
- **2026-05-25** — IMPLEMENTED. All 10 Plan Steps executed in strict bottom-to-top order: Step 1 (pre-flight anchor + clean-baseline confirmation — all anchors stable; source ↔ deployed byte-identical at 298 lines) → Step 2 (AC1 naming-convention prompt inserted at § Process: Plan step 3 as new `**Naming**:` paragraph — landed at line 193) → Step 3 (AC2 scope-guard prompt inserted as sub-bullet under step 2's AC-drafting bullet — landed at line 185; sub-bullet form (a) per Plan recommendation) → Step 4 (`.claude/` re-sync via `cp -R shannon/skills/work-items/. .claude/skills/work-items/` — trailing-dot form substituted for Plan's specified form per Deviation noted below) → Step 5 (`diff -rq` verification post-re-sync — empty output, byte-identical) → Step 6 (additivity verification — two single-sentence statements recorded in Implementation Notes; both additive; no pause-and-flag) → Step 7 (verbatim-phrasing `grep` verification — **initial `TASK-A, TASK-B` grep returned 0** because the landed text used backticks around each letter; resolved by removing backticks to match `conceptual_design.md:58`'s plain-prose form + re-syncing `.claude/` again; final all-grep verification passed including the three-way bold-lead consistency check across skill + `conceptual_design.md:179` + `development_guide.md:79`) → Step 8 (derived bookkeeping updated: `task_index` TASK-009 line; EPIC-007 § Plan Task 2 entry at line 127) → Step 9 (two-part scope-confined-diff verified — `git status` shows exactly 4 expected tracked files; `diff` against deployed copy empty; no out-of-scope file modified) → Step 10 (no commit mid-implementation; verified by `git log master..HEAD` empty). All 9 ACs satisfied. **Three deviations recorded** in Implementation Notes: (a) TASK-A/B/C backtick fix (caught by Step 7's verbatim-grep regimen exactly as designed — no AC affected; lesson worth capturing: verbatim phrasings used in `grep` cannot be Markdown-formatted); (b) `cp -R` command form (used trailing-dot form `shannon/skills/work-items/.` for safer `contents-into-existing-target` semantics — sub-deviation in command-form specification, not intent; both `cp -R` invocations verified by post-sync `diff` empty); (c) AC2 sub-position decision landed as sub-bullet form (a) per Plan recommendation. **Second instance of the paired recursion pattern TASK-008 introduced**: the Task dogfoods TASK-007's editing-order convention during execution AND disciplines its own AC8 scope assertion by the very rule its AC2 prompt cites. **One new framework-general lesson surfaced** (gotchas → potential scratchpad item for promotion at EPIC-007 or EPIC-008 Gate 3): verbatim-phrasing tests require plain-prose substring landings, not Markdown-formatted ones. Pending `/task-review TASK-009` (Gate 3).
- **2026-05-25** — IMPLEMENTING via `/task-implement TASK-009`. Status PLANNED → IMPLEMENTING; executing the 10-step bottom-to-top plan against `shannon/skills/work-items/skill.md` + `.claude/skills/work-items/` re-sync.
- **2026-05-25** — PLANNED via Gate 2. Planning subagent drafted a 10-step bottom-to-top plan adapted from TASK-008's 12-step precedent for the two-file scope: pre-flight anchor + clean-baseline confirmation → insert AC1 naming-convention prompt at § Process: Plan step 3 (line 190 — higher pre-edit line, first source edit) → insert AC2 scope-guard prompt under § Process: Plan step 2 AC-drafting bullet (line 184 — lower pre-edit line, second source edit; Plan recommends sub-bullet form (a)) → `cp -R` re-sync of `.claude/skills/work-items/` per TASK-005 precedent → `diff -rq` verification post-re-sync → additivity verification (per dogfooded *Status semantics* test) → verbatim-phrasing `grep` verification including three-way bold-lead consistency check (skill + `conceptual_design.md:179` + `development_guide.md:79`) → derived bookkeeping (task_index + EPIC-007 § Plan Task 2 at line 127) → two-part scope-confined-diff verification (`git status` for tracked files + `diff` for the gitignored deployed copy — explicit gitignored-split accounting per AC8) → no commit until Gate 3. **Bottom-to-top sequencing**: only two substantive insertions but the discipline is stated explicitly per the convention TASK-007 ratified. **Sub-position decision at AC2 deferred to implementer with Plan recommendation** (sub-bullet form (a) preferred; rationale to be recorded at landing). **Five verbatim phrases** copied letter-for-letter from TASK-008's landed canonical forms (the failure-mode test in canonical double-quote form; the scope-guard bold lead matching three-way landing; the naming-convention exclusion + position-language alternative). **A paired recursion mirroring TASK-008**: (a) the Task dogfoods TASK-007's editing-order convention (`development_guide.md:79`) during execution; (b) AC8 disciplined by AC2's referenced rule (`conceptual_design.md:179`) — the second instance of the scope-guard rule disciplining its own implementer's scope assertion. All 9 ACs covered by the 10 Steps (1:1 mapping; Step 7 provides cross-cutting grep evidence; Step 8 provides unified-lifecycle bookkeeping). 7 risks with mitigations (`.claude/` re-sync omission; failure-mode test verbatim drift; three-way bold-lead consistency break; cross-Epic textual collision foreshadowing; additivity violation; over-formalisation; bookkeeping drift). No drift, no internal contradiction. Plan approved by directing party without modification.
- **2026-05-25** — ELABORATED via Gate 1. Elaboration subagent grounded each AC in the live `shannon/skills/work-items/skill.md` (298 lines) and confirmed the deployed copy at `.claude/skills/work-items/skill.md` is byte-identical (also 298 lines; `diff` empty at elaboration time — clean baseline). Confirmed anchor points: § Process: Plan step 2 (`### 2. Spawn Planning Subagent` at line 178; AC-drafting bullet at line 184 reading *"Drafts the Plan section: approach, steps/tasks/epics (depending on type), dependencies, risks"* — landing point for AC2 scope-guard prompt); § Process: Plan step 3 (`### 3. Cascading Preparation (for Epics)` at line 188; existing paragraph at line 190 — landing point for AC1 naming-convention prompt). House style of existing soft prompts noted (declarative/imperative; backtick-quoted file paths with `§` symbol; no Markdown links; single-sentence-or-two bullets indented under numbered step headers). All four EPIC-007 "implementing Task may override" hooks for the skill-half are taken as the Plan's resolved choices (no override at AC level; one sub-position decision at AC2 — sub-bullet vs inline qualifying clause — deferred to implementer with rationale recorded in Implementation Notes). 9 testable ACs drafted (AC1–AC9), traced 1:1 to EPIC-007 ACs: AC1+AC5 → EPIC-007 AC#3 skill-half (naming-convention prompt + cross-reference to *Work Item* entity Naming note at `conceptual_design.md:58`); AC2+AC6 → EPIC-007 AC#4 skill-half (scope-guard prompt + cross-reference to § Business Rules at `conceptual_design.md:179`); AC3 covers the explicit `.claude/` re-sync per TASK-005 `cp -R` + `diff -rq` precedent; AC4 covers preservation of the byte-identical source↔deployed invariant; AC7 covers additivity verification per the dogfooded *Status semantics* test applied to skill content; AC8 covers cross-type-guard scope (with explicit accounting for the gitignored `.claude/` copy that won't appear in `git status` — verified separately by `diff`); AC9 covers no-commit-mid-implementation. **Cross-document verbatim consistency**: AC2's failure-mode test repeats `if the AC's failure mode is "an unintended file changed"` letter-for-letter in TASK-008's resolved canonical double-quote form (per TASK-008 § Implementation Notes → Deviations from Plan third bullet); AC6's scope-guard cross-reference reproduces the bold lead `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` letter-for-letter to match the three-way landing across skill + `conceptual_design.md:179` + `development_guide.md:79`. **Cross-Epic preview**: EPIC-008 Task 2's natural insertion points (§ Process: Plan as a whole + § Process: Review as a whole — step-adjoining) do not collide with this Task's step-internal insertions; EPIC-008 Task 2 rebases mechanically against this Task's landed state. Findings: all Strength + one minor non-blocking Gap (sub-position decision at AC2 deferred to implementer — appropriate); no Drift — section header text matches exactly between EPIC-007 AC text and live skill; no pre-existing source↔deployed drift. Directing party approved without modification.
- **2026-05-25** — DRAFT: Task created via `/task-create` under EPIC-007 as Task 2 of 2 (the `work-items` skill prompts + `.claude/` re-sync half). EPIC-007's Plan resolved both location decisions for the skill-half prompts (AC#3 skill-half → § Process: Plan step 3 *Cascading Preparation (for Epics)*; AC#4 skill-half → § Process: Plan step 2 *Spawn Planning Subagent*, AC-drafting bullet) and named out-of-scope items (`conceptual_design.md` amendments → TASK-008 already APPROVED; AC#7 scratchpad bookkeeping → Epic Gate 3 review). Initial intent captured in the Overview; Acceptance Criteria and Plan to be drafted at `/task-elaborate` and `/task-plan`. This Task lands first per directing-party sequencing decision (2026-05-25 after TASK-008 APPROVED); EPIC-008 Task 2 will rebase. The Task is the completion of EPIC-007's work-item-conventions half of the FEAT-003 / FEAT-008 dogfooding-harvest pair; after this Task's APPROVED, EPIC-007 needs only its Gate 3 scratchpad bookkeeping (per AC#7) to close. Two dogfooding lenses applied to this Task itself: (a) TASK-007's editing-order convention (source body first; deployed-copy re-sync after; diff-verify) per `development_guide.md:79`; (b) TASK-008's scope-guard rule per `conceptual_design.md:179` for the cross-type scope-bounded-diff AC. One minor housekeeping piggyback noted but **explicitly out of scope per AC12-style scope guard**: removing the now-redundant HTML comment at `development_guide.md:79` is appropriate for EPIC-007 Gate 3 review, not this Task.
