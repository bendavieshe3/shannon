# TASK-010: Add Meta-Gap Routing-Channel Skill Prompts at § Process: Plan and § Process: Review, and Re-sync .claude/

## Metadata

- **Status**: APPROVED
- **Type**: Task
- **Parent**: [EPIC-008](../epics/EPIC-008-development-conventions-from-dogfooding.md)
- **Feature**: [FEAT-008](../features/FEAT-008-development-discipline.md)
- **Tags**: #framework #work-items #skills #meta-gap #routing-channel
- **Created**: 2026-05-27
- **Updated**: 2026-05-27 (approved)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

*Initial intent captured at `/task-create` (2026-05-27). Full Requirements drafted at `/task-elaborate TASK-010`.*

### Overview

**Task 2 of EPIC-008** — the closing implementing Task for the meta-gap routing channel that EPIC-008 AC#3 ratifies as the framework's resolution-routing convention. TASK-007 (EPIC-008 Task 1, APPROVED 2026-05-24) landed the development-guide half at `docs/development_guide.md` § Testing Strategy → Pre-Commit Checklist (the meta-gap checklist item at line 114). This Task lands the work-items-skill half: two soft prompts in `shannon/skills/work-items/skill.md` at § Process: Plan and § Process: Review that mirror the checklist line's trigger phrasing and direct the planning / review subagents to surface framework-general resolutions at the moments of action.

**The two soft prompts** — locations definitively resolved at EPIC-008 § Plan (implementing Task may override with recorded rationale):

- **EPIC-008 AC#3b (Gate 2 half) — Soft prompt at § Process: Plan**. Step-adjoining: a new bullet under § Process: Plan as a whole (not inside any single numbered step), positioned where it surfaces at the moment of planning regardless of which step the planning subagent is in. Mirrors the verbatim trigger phrasing from EPIC-008 AC#3 (the same phrasing landed by TASK-007 at `development_guide.md:114`): *"framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive"*. References the `development_guide.md` § Testing Strategy → Pre-Commit Checklist line by section anchor.

- **EPIC-008 AC#3b (Gate 3 half) — Soft prompt at § Process: Review**. Step-adjoining: a new bullet under § Process: Review as a whole, mirroring the trigger phrasing and cross-reference structure of the Gate 2 half.

**Cross-Epic coordination**: TASK-009 (EPIC-007 Task 2, APPROVED 2026-05-25 commit `1293b98`) landed step-internal prompts in the same skill file (line 185 inside § Process: Plan step 2; line 193 inside § Process: Plan step 3). TASK-010's step-adjoining insertions (under § Process: Plan as a whole; under § Process: Review as a whole) target distinct anchors and do **not** collide with TASK-009's step-internal landings — this was foreshadowed at EPIC-007 / EPIC-008 planning and verified at TASK-009 review. The mechanical check at TASK-010's implementation time: read `shannon/skills/work-items/skill.md`, identify which prompts already exist (TASK-009's two are at step level), add the missing ones at section level.

**Bidirectional cross-reference pair** completed on this Task's landing: AC#3a (the `development_guide.md` Pre-Commit Checklist line at line 114) ↔ AC#3b (the two skill prompts this Task lands). TASK-007 landed the outbound half (checklist line → skill anchors); TASK-010 lands the inbound half (skill prompts → checklist line).

**Explicit `.claude/` re-sync step** per the TASK-005 / TASK-009 precedent: after editing `shannon/skills/work-items/skill.md` (source), copy to `.claude/skills/work-items/skill.md` (deployed copy) so the deployed framework does not drift from source. Re-sync is a named checklist item the implementer must tick, not an afterthought; verified at Gate 3 by diff-comparing the two files.

### Acceptance Criteria

*Drafted at `/task-elaborate TASK-010` (2026-05-27) — anchors confirmed against the live `shannon/skills/work-items/skill.md` (301 lines; post-TASK-009 state with step-internal prompts intact at lines 185 + 193) and the byte-identical deployed copy at `.claude/skills/work-items/skill.md` (301 lines; `diff` empty at elaboration time, so no pre-existing source-vs-deployed drift to remediate). Each criterion names a verifiable outcome — the section, the surrounding anchor, and the required verbatim wording where AC text demands it. Closes EPIC-008 AC#3b Gate-2 half (AC1 + AC5 below), AC#3b Gate-3 half (AC2 + AC6 below), AC#5 inbound half (AC5 + AC6 below — the inbound bidirectional completion of the AC#3a ↔ AC#3b cross-reference pair whose outbound half TASK-007 landed at `development_guide.md:114`); AC3 covers the explicit `.claude/` re-sync per the TASK-005 / TASK-009 precedent; AC4 covers preservation of the existing source↔deployed alignment; AC7 covers additivity verification (including the cross-Epic preservation of TASK-009's step-internal prompts at lines 185 + 193); AC8 covers the scope-bounded-diff with explicit accounting for the gitignored deployed copy; AC9 covers no-commit-mid-implementation.*

- [ ] **AC1 — Gate-2 soft prompt lands as step-adjoining prose under § Process: Plan (Gate 2).** § Process: Plan (Gate 2) (currently lines 172–208 of `shannon/skills/work-items/skill.md`: header `## Process: Plan (Gate 2)` at line 172; first numbered step `### 1. Identify Target` at line 174) gains a new step-adjoining soft prompt inserted between the section header at line 172 and the first numbered step at line 174 — i.e. under the section as a whole, not inside any single numbered step. The prompt is a single-sentence soft directive to the planning subagent, phrased as guidance (not a blocking check). The instruction must **(i)** repeat the verbatim trigger phrasing from `development_guide.md:114` letter-for-letter as a plain-prose substring (no backticks, italic, bold, or other Markdown formatting interposing): **framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive**; **(ii)** direct the planning subagent to surface any such ambiguity at the moment of planning by routing it to `scratchpad.md` for promotion or by opening a follow-up work item against the relevant mandated document (mirroring the disposition language from `development_guide.md:114`); **(iii)** cross-reference the canonical convention at `development_guide.md` § Testing Strategy → Pre-Commit Checklist by file path + section anchor in the same prose form as TASK-009's existing cross-references at lines 185 + 193 (backtick-quoted file path; `§` symbol; `→` for sub-anchor; section name in plain prose; no Markdown link). The step-adjoining placement is deliberate — the prompt must reach the subagent before it spawns into any numbered step, so the routing discipline applies across all of Gate 2's work, not just one step. Verifiable by `grep`: `grep -n "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` returns **2** hits (this AC's at the § Process: Plan landing + AC2's at the § Process: Review landing); `grep -n "Pre-Commit Checklist" shannon/skills/work-items/skill.md` returns **≥2** new hits (the two cross-references this Task adds). The existing prose of § Process: Plan is preserved unchanged: the six numbered steps (174 *Identify Target*; 178 *Spawn Planning Subagent*; 189 *Cascading Preparation (for Epics)*; 195 *Present and Iterate*; 199 *Gate 2 Approval*; 206 *Resume*) and TASK-009's step-internal prompts at lines 185 + 193 are untouched.

- [ ] **AC2 — Gate-3 soft prompt lands as step-adjoining prose under § Process: Review (Gate 3).** § Process: Review (Gate 3) (currently lines 232–270 of `shannon/skills/work-items/skill.md`: header `## Process: Review (Gate 3)` at line 232; first numbered step `### 1. Identify Target` at line 234) gains a new step-adjoining soft prompt inserted between the section header at line 232 and the first numbered step at line 234 — i.e. under the section as a whole, not inside any single numbered step. The prompt mirrors AC1's structure and trigger-phrase letter-for-letter, adapted to the Gate-3 framing (directed at the review subagent rather than the planning subagent; "the moment of review" rather than "the moment of planning"). Specifically: **(i)** repeats the same verbatim trigger phrasing as plain prose: **framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive** (no Markdown formatting interposing); **(ii)** directs the review subagent to surface any such ambiguity at the moment of review by routing it to `scratchpad.md` for promotion or by opening a follow-up work item against the relevant mandated document; **(iii)** cross-references `development_guide.md` § Testing Strategy → Pre-Commit Checklist by file path + section anchor in the same prose form as AC1 and TASK-009's existing cross-references. Verifiable by `grep`: the same `grep -n "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` from AC1 returns **2** hits (one at each section). The existing prose of § Process: Review is preserved unchanged: the six numbered steps (234 *Identify Target*; 238 *Mark REVIEW*; 242 *Verify Against Acceptance Criteria*; 251 *Present Verification*; 255 *Gate 3 Approval*; 268 *Resume*) are untouched.

- [ ] **AC3 — `.claude/skills/work-items/skill.md` is re-synced from source per the TASK-005 / TASK-009 precedent.** After the AC1 and AC2 edits to `shannon/skills/work-items/skill.md` (the source), the deployed copy at `.claude/skills/work-items/skill.md` is re-synced from source. The re-sync mechanic uses the **trailing-dot form** TASK-009 captured in its Deviations from Plan as the safe contents-into-existing-target pattern: `cp -R shannon/skills/work-items/. .claude/skills/work-items/`. Verified post-sync by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning **no differences** (alternative `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning exit code 0 / empty output is equivalent). Re-sync is a named checklist item in this Task's Plan Steps — not an afterthought — because `.claude/` is gitignored per repository convention and an omission leaves no tracked diff: the deployed framework silently drifts from source until the next time a reader compares the two by hand.

- [ ] **AC4 — Pre-existing source↔deployed alignment is preserved.** At elaboration time (2026-05-27), `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns empty (both files are 301 lines and byte-identical — the clean post-TASK-009 baseline). This Task's only changes to either file are the AC1 and AC2 source-side edits plus AC3's re-sync of those same changes into the deployed copy. **No other content of the deployed copy is altered**: the re-sync mechanic at AC3 produces a deployed copy that differs from the source only inasmuch as the source was newly edited at AC1 and AC2; no whitespace, line-ending, or rendering normalisation is introduced. Verifiable at Gate 3 by `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty (the byte-identical invariant holds across the Task's diff range, mid-edit moments excepted).

- [ ] **AC5 — Gate-2 prompt cross-references `development_guide.md` § Testing Strategy → Pre-Commit Checklist verbatim.** The AC1 prompt at § Process: Plan carries an inline cross-reference naming the file path and the section anchor verbatim: **`development_guide.md` § Testing Strategy → Pre-Commit Checklist** (backtick-quoted file path; `§` symbol; `→` for sub-anchor; plain prose; no Markdown link — matches TASK-009's existing cross-references at lines 185 + 193). This completes the inbound half of EPIC-008 AC#5's AC#3a ↔ AC#3b cross-reference pair. The outbound half exists at `development_guide.md:114` (landed by TASK-007 APPROVED 2026-05-24, commit `eac486b`) which names the inbound target as *"the matching soft prompts in `shannon/skills/work-items/skill.md` § Process: Plan and § Process: Review"* — confirmable by reading the parenthetical at the end of the line 114 checklist item. The two halves resolve to each other on this AC landing. Verifiable: `grep -n "development_guide.md" shannon/skills/work-items/skill.md` returns at least one hit at the § Process: Plan step-adjoining anchor (in addition to AC6's hit at § Process: Review); the named target anchor at `development_guide.md:114` resolves to the Pre-Commit Checklist meta-gap item (which exists as of TASK-007 APPROVED — **no dangling-anchor HTML comment required because the target exists**).

- [ ] **AC6 — Gate-3 prompt cross-references `development_guide.md` § Testing Strategy → Pre-Commit Checklist verbatim.** The AC2 prompt at § Process: Review carries an inline cross-reference to the same anchor as AC5, with the same prose form: **`development_guide.md` § Testing Strategy → Pre-Commit Checklist**. The cross-reference is *identical in form* to AC5's so the two new landings reference one canonical anchor with one canonical phrasing. Verifiable: `grep -n "development_guide.md" shannon/skills/work-items/skill.md` returns one additional hit at the § Process: Review step-adjoining anchor (in addition to AC5's hit at § Process: Plan); the named target anchor at `development_guide.md:114` resolves to the same Pre-Commit Checklist meta-gap item. The two new cross-references (AC5 + AC6) form a single bidirectional pair with `development_guide.md:114`'s outbound parenthetical, completing EPIC-008 AC#5's inbound half.

- [ ] **AC7 — Additivity verification performed and recorded, including cross-Epic preservation of TASK-009's step-internal prompts.** Per the dogfooded equivalent of `conceptual_design.md` § Re-reviewing → *Status semantics* applied to skill content (skills are not gated documents but the same additive/substantive logic applies — does the new prose contradict existing prose?): the two new prompts (AC1 at § Process: Plan as a whole; AC2 at § Process: Review as a whole) are verified to be **additive** — they add new step-adjoining instructions to existing sections without revising any pre-existing instruction. The implementing Task records a single-sentence verification per prompt in § Implementation Notes. **In addition**, the Task explicitly verifies that **TASK-009's two step-internal prompts at lines 185 (step 2 sub-bullet, the scope-guard prompt) and 193 (step 3 Naming paragraph) remain present and unchanged** — this cross-Epic preservation is a named verification step because TASK-010's step-adjoining insertions land in the same section as TASK-009's step-internal ones and could in principle disturb them by accidental edit-tool anchor-match. Specifically: **(a)** § Process: Plan's six numbered steps (174 *Identify Target*; 178 *Spawn Planning Subagent* including TASK-009's step-2 scope-guard sub-bullet at line 185; 189 *Cascading Preparation (for Epics)* including TASK-009's step-3 Naming paragraph at line 193; 195 *Present and Iterate*; 199 *Gate 2 Approval*; 206 *Resume*) are unchanged in semantic content other than potential downstream line-number shifts from AC1's insertion above them; **(b)** § Process: Review's six numbered steps (234 *Identify Target*; 238 *Mark REVIEW*; 242 *Verify Against Acceptance Criteria*; 251 *Present Verification*; 255 *Gate 3 Approval*; 268 *Resume*) are unchanged in semantic content other than potential downstream line-number shifts from AC2's insertion above them; **(c)** no other section of the skill (§ Purpose, § When to Invoke, § Templates, § Unified Status Model, § Work Item ↔ Document Relationships, § Process: Create, § Process: Elaborate, § Process: Implement, § Quality Gates, § Cascading Operations, § Failure Modes, § Self-Identification) is modified. Verifiable by `grep` for the TASK-009 substrings: `grep -n "an unintended file changed" shannon/skills/work-items/skill.md` returns **1** hit (TASK-009's scope-guard prompt still present); `grep -n "TASK-A, TASK-B" shannon/skills/work-items/skill.md` returns **1** hit (TASK-009's naming-convention exclusion still present); `grep -n "descriptive title" shannon/skills/work-items/skill.md` returns **1** hit (TASK-009's positive requirement still present). If any prompt turns out to contradict existing prose (unexpected), implementation pauses and the directing party is flagged before landing — mirroring TASK-009 AC7's protocol.

- [ ] **AC8 — Scope is bounded by the cross-type guard codified at `conceptual_design.md:179` (the rule TASK-008 ratified and TASK-009's step-2 prompt at line 185 references); the gitignored deployed copy is verified separately by `diff`.** This Task modifies exactly the following **tracked** files (those visible in `git status`): `shannon/skills/work-items/skill.md` (the source — AC1 and AC2 edits); `docs/tasks/TASK-010-add-meta-gap-skill-prompts-and-resync-claude.md` (this file — Status / Activity Log / Implementation Notes per the unified lifecycle); `docs/tasks/task_index.md` (TASK-010 status transitions per the unified lifecycle); `docs/epics/EPIC-008-development-conventions-from-dogfooding.md` (§ Plan Task 2 entry status update at line 122). **In addition**, the gitignored file `.claude/skills/work-items/skill.md` is modified by AC3's re-sync — this modification will **not appear in `git status`** because `.claude/` is repository-gitignored; it is verified separately by `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty (the AC3 / AC4 verification mechanism). Specifically out of scope: any other file under `shannon/skills/` (no other skill modified); any other file under `.claude/`; any mandated document (`vision.md`, `technology_stack.md`, `conceptual_design.md`, `technical_design.md`, `development_guide.md`, `ux_guide.md` — `development_guide.md` was TASK-007's territory and is now stable); any work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` (EPIC-008 AC#6 Epic Gate 3 territory); `docs/knowledge/` (EPIC-008 AC#7 Epic Gate 3 territory). **This AC is phrased as a cross-type guard per the very rule the TASK-009 step-2 prompt at line 185 references — *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* at `conceptual_design.md:179` — not as a literal file count.** A third pleasing recursion (the first two were TASK-008 AC12 and TASK-009 AC8): the Task that lands the step-adjoining routing-channel prompts disciplines its own scope assertion by the very scope-guard rule whose step-internal prompt sits adjacent to its own landing point. Verified at Gate 3 by `git status` against the cross-type guard (for tracked files) plus the AC4 `diff` check (for the gitignored deployed copy).

- [ ] **AC9 — Implementation does not commit mid-implementation.** Per `development_guide.md` § Commit Cadence and § Push Cadence (both ratified by TASK-007 at commit `eac486b`), the implementer does not commit until Gate 3 approval. Pre-Gate-3 experimentation is fine — multiple in-progress edits across the source and deployed copies are expected — but no `git commit` runs before Gate 3 approval lands. The Gate-3 commit also triggers a push per `development_guide.md` § Push Cadence trigger (a) — after every Gate 3 approval. This AC governs the Task's own execution, not its output; verified at Gate 3 by checking `git log master..HEAD` for any pre-Gate-3 commit attributable to this Task's diff range — expectation: zero such commits.

### Context

- **Parent Epic**: [EPIC-008 — Development Conventions Surfaced Through Dogfooding](../epics/EPIC-008-development-conventions-from-dogfooding.md) — this is Task 2 of 2; closes EPIC-008 AC#3b (Gate 2 half + Gate 3 half), AC#5 inbound half. EPIC-008 AC#6 (scratchpad bookkeeping) and AC#7 (recursive-dogfood Extension knowledge note) remain Epic Gate 3 work.
- **Parent Feature**: [FEAT-008 — Development Discipline](../features/FEAT-008-development-discipline.md) — Ideal State bullets 2 and 3 anchor EPIC-008; TASK-007 delivered the `development_guide.md` half; this Task completes the work-items-skill half of bullet 3 (the routing channel reaching the implementer at the moments of relevance).
- **Source-of-truth body**: `shannon/skills/work-items/skill.md` (the source). Specific anchor points to confirm at elaboration: § Process: Plan section header (the entire section is the target anchor for the Gate 2 prompt — not any single numbered step); § Process: Review section header (the entire section is the target anchor for the Gate 3 prompt).
- **Deployed copy** (must re-sync after editing source): `.claude/skills/work-items/skill.md`.
- **Sibling Task (already APPROVED)**: TASK-007 (EPIC-008 Task 1, APPROVED 2026-05-24, commit `eac486b`) landed the Pre-Commit Checklist line at `development_guide.md:114` that this Task's prompts cross-reference inbound. Specifically the verbatim trigger phrasing: *"framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive"*.
- **Cross-Epic sibling (APPROVED)**: TASK-009 (EPIC-007 Task 2, APPROVED 2026-05-25, commit `1293b98`) landed step-internal prompts in the same skill file at lines 185 + 193. TASK-010 rebases mechanically against TASK-009's landed state (read the skill at implementation time; the two TASK-009 prompts already exist; add the two TASK-010 step-adjoining prompts; re-sync `.claude/`).
- **Precedent for `.claude/` re-sync mechanic**: TASK-005 + TASK-009 (both archived) established `cp -R shannon/skills/work-items/. .claude/skills/work-items/` + `diff -rq` verification. The trailing-dot form ensures contents-into-existing-target semantics (verified by TASK-009's Deviations from Plan).
- **Conventions this Task itself dogfoods**:
  - **TASK-007's editing-order convention** (`development_guide.md:79`): `shannon/skills/work-items/skill.md` is the source-of-truth body; `.claude/skills/work-items/skill.md` is a derived artefact (a deployed copy); edit the body first, then re-sync, then verify by diff-comparing.
  - **TASK-008's scope-guard rule** (`conceptual_design.md:179`): the scope-bounded-diff AC uses cross-type guard phrasing, not a literal file count.
  - **TASK-009's plain-prose substring lesson** (now in `docs/scratchpad.md` § Items, surfaced 2026-05-25): the verbatim trigger phrasing at AC1 and AC2 must land in plain prose so the `grep` verification at Step 7 confirms the substring is present without Markdown formatting interposing.
- **Source artefacts** (the scratchpad items addressed by EPIC-008 AC#6 at Epic Gate 3): *"Meta-gap: no channel for feeding framework-general resolutions back into the framework"* (the very meta-gap this Task and TASK-007 together close); *"Push-frequency directive for development_guide"* (TASK-007 territory); *"Framework clarifications buried in TASK-003's Plan"* sub-item (a) editing-order (TASK-007 territory — closes at EPIC-008 Gate 3 via the second-approver move of the shared scratchpad item).

---

## Plan

*Drafted at `/task-plan TASK-010` (Gate 2) — 2026-05-27.*

### Approach

A **two-file editing pass** against `shannon/skills/work-items/skill.md` (source-of-truth body) plus an explicit `cp -R` re-sync of `.claude/skills/work-items/` (gitignored deployed copy), followed by `diff -rq` verification, verbatim-phrasing `grep` verification, additivity verification (per the dogfooded *Status semantics* test applied to skill content), an **explicit cross-Epic preservation check** that TASK-009's two step-internal prompts at lines 185 + 193 remain present unchanged, derived-bookkeeping updates (`task_index.md` + EPIC-008 § Plan Task 2 entry at line 122), and a **two-part scope-confined-diff** that explicitly accounts for the gitignored `.claude/` copy. The Task itself dogfoods three landed conventions: **TASK-007's editing-order convention** (`development_guide.md:79` — source body before derived artefacts; verify); **TASK-008's scope-guard rule** (`conceptual_design.md:179` — cross-type-guard phrasing at AC8); **TASK-009's plain-prose substring discipline** (`docs/scratchpad.md` § Items 2026-05-27 — the verbatim trigger phrasing at AC1+AC2 lands as plain prose, no Markdown formatting interposing between substring chars). A **fourth pleasing recursion** (after TASK-008 AC12 + TASK-009 AC8 + this Task's AC8): the Task that lands the meta-gap routing-channel prompts will, at its own Gate 3, exercise the routing channel it just landed — its review subagent will face the same prompt it lands.

**Edit order — strictly bottom-to-top by pre-edit line number.** AC2 (§ Process: Review at line 232) has the higher pre-edit line number → **first** source edit. AC1 (§ Process: Plan at line 172) is second. Editing AC2 first leaves AC1's anchor at line 172 stable across AC2's downstream-line insertion.

**Sub-position decision — Plan recommends italicised single-sentence soft prompt** preceded and followed by a blank line, sitting between section header and first numbered step. House style for step-adjoining prompts is not yet established in the skill (every existing `## Process: *` jumps straight from heading to `### 1.`), so TASK-010 sets the convention. Italicised-sentence rationale: visually distinct from numbered-step prose; matches soft-directive register (guidance, not blocking check); short enough to constrain proportionality. Implementer may override at landing (e.g. short-paragraph form) with rationale recorded.

**Verbatim trigger phrasing copied letter-for-letter** from `development_guide.md:114` — both AC1 and AC2 prompts must land this substring as plain prose (no backticks, italic `*`, bold `**`, or other Markdown interposing between substring chars): `framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive`. The surrounding sentence may be italicised per the recommended sub-position form (italic-around-the-whole-sentence keeps the substring contiguous); what's disallowed is Markdown *inside* the substring boundary.

**Cross-reference style matches TASK-009's existing skill cross-references** at lines 185 + 193: backtick-quoted file path; `§` symbol; `→` for sub-anchor; plain-prose section name; no Markdown link. Form: `` `development_guide.md` § Testing Strategy → Pre-Commit Checklist ``.

**Edit-tool anchor selection — section-boundary uniqueness.** The string `### 1. Identify Target` appears in **both** § Process: Plan and § Process: Review and is **not** safe as a standalone Edit anchor. Steps 2 and 3 use the full-section-header prefix to disambiguate: `## Process: Review (Gate 3)\n\n### 1. Identify Target` and `## Process: Plan (Gate 2)\n\n### 1. Identify Target`. This is the most consequential anchor-selection decision in the Plan because TASK-009's step-internal landings (lines 185 + 193) sit inside the same § Process: Plan section AC1 targets.

**Re-sync mechanic uses the trailing-dot form** from TASK-009's Deviations from Plan: `cp -R shannon/skills/work-items/. .claude/skills/work-items/`. Verified by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning no differences.

**Scope-confined-diff requires two complementary mechanisms** because `.claude/` is gitignored: (a) `git status` confirms exactly 4 tracked files modified; (b) `diff` confirms the deployed copy matches source. Both must hold for AC8 to pass.

**Commit deferral per AC9 + § Commit Cadence.** No `git commit` runs mid-implementation; the Gate-3 commit lands after directing-party approval and triggers a push per `development_guide.md` § Push Cadence trigger (a) — the rule TASK-007 itself ratified at commit `eac486b`.

### Steps

Steps proceed in **strict bottom-to-top order by pre-edit line number** through the two substantive source insertions, then the re-sync, then verifications (including the named cross-Epic preservation check), then derived bookkeeping, then the two-part scope-confined-diff verification. Each step names its AC coverage explicitly.

1. **Pre-flight: re-read `shannon/skills/work-items/skill.md` end-to-end and confirm anchors haven't drifted since ELABORATED.** Confirm: `## Process: Plan (Gate 2)` at line 172; `### 1. Identify Target` (under § Process: Plan) at line 174; TASK-009's step-2 scope-guard sub-bullet at line 185 (substrings `an unintended file changed` + `Scope-Boundary Acceptance Criteria Use Cross-Type Guards`); TASK-009's step-3 Naming paragraph at line 193 (substrings `descriptive title` + `TASK-A, TASK-B, TASK-C` + `the first Task`); `## Process: Review (Gate 3)` at line 232; `### 1. Identify Target` (under § Process: Review) at line 234. Re-run `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` and confirm empty output (clean baseline still holds). *(Orientation only — closes no AC; baseline for AC3 / AC4 / AC7.)*

2. **Insert AC2 Gate-3 step-adjoining prompt at § Process: Review (Gate 3) — higher pre-edit line number → first source edit.** Insertion point: between `## Process: Review (Gate 3)` at line 232 and `### 1. Identify Target` at line 234 (step-adjoining; under the section as a whole). Recommended form (overridable at landing with rationale): italicised single sentence preceded and followed by a blank line. Three required elements: **(i)** verbatim trigger phrasing from `development_guide.md:114` letter-for-letter as plain prose (no Markdown formatting interposing between substring chars) — `framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive`; **(ii)** direct the **review subagent** (Gate-3 framing) to surface any such ambiguity at **the moment of review** by routing it to `scratchpad.md` for promotion or by opening a follow-up work item against the relevant mandated document; **(iii)** inline cross-reference `` `development_guide.md` § Testing Strategy → Pre-Commit Checklist `` (matches TASK-009's existing cross-reference form). Edit-tool anchor: the **unique** boundary substring `## Process: Review (Gate 3)\n\n### 1. Identify Target` — disambiguates from § Process: Plan. Preserves § Process: Review's six numbered steps (234 *Identify Target*; 238 *Mark REVIEW*; 242 *Verify Against Acceptance Criteria*; 251 *Present Verification*; 255 *Gate 3 Approval*; 268 *Resume*) unchanged in semantic content; line numbers shift downward. *(Closes AC2, AC6.)*

3. **Insert AC1 Gate-2 step-adjoining prompt at § Process: Plan (Gate 2) — lower pre-edit line number → second source edit.** Insertion point: between `## Process: Plan (Gate 2)` at line 172 and `### 1. Identify Target` at line 174 (step-adjoining). Same shape as Step 2 — italicised single-sentence form preceded and followed by blank lines. Adapted framing: directed at the **planning subagent** (Gate-2) and naming **the moment of planning**. Same verbatim plain-prose trigger phrase: `framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive`. Disposition: route to `scratchpad.md` for promotion or open a follow-up work item against the relevant mandated document. Cross-reference: `` `development_guide.md` § Testing Strategy → Pre-Commit Checklist `` (identical form to AC2). Edit-tool anchor: the **unique** boundary substring `## Process: Plan (Gate 2)\n\n### 1. Identify Target` — disambiguates from § Process: Review. Preserves § Process: Plan's six numbered steps (174 *Identify Target*; 178 *Spawn Planning Subagent* including TASK-009's step-2 sub-bullet at line 185; 189 *Cascading Preparation (for Epics)* including TASK-009's step-3 Naming paragraph at line 193; 195 *Present and Iterate*; 199 *Gate 2 Approval*; 206 *Resume*) unchanged in semantic content; line numbers shift downward by AC2's insertion above plus AC1's own. *(Closes AC1, AC5.)*

4. **Re-sync `.claude/skills/work-items/` from source — trailing-dot form per TASK-009 precedent.** Execute: `cp -R shannon/skills/work-items/. .claude/skills/work-items/`. The trailing dot copies the *contents* of the source directory into the existing target (TASK-009's Deviations from Plan recorded this as the safe contents-into-existing-target form; the bare form can create nested-directory pathology under some `cp` versions). *(Closes AC3 — named re-sync mechanic; supports AC4 — preserves source↔deployed alignment for content other than the two new prompts.)*

5. **`diff` verification post-re-sync.** Run `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` and confirm **no differences output**. (Equivalent alternative: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returning empty / exit code 0.) Record the verification result in Implementation Notes (the verification is the AC3 / AC4 evidence at Gate 3; `.claude/` being gitignored leaves no tracked diff). *(Closes AC3 — re-sync mechanic verified; closes AC4 — source↔deployed byte-identical invariant preserved across the diff range.)*

6. **Additivity verification per the dogfooded *Status semantics* test, including explicit cross-Epic preservation of TASK-009's step-internal prompts.** Three single-sentence statements in Implementation Notes: **AC1 Gate-2 prompt** — *additive*: § Process: Plan's header-to-step-1 gap is empty prose-wise; new step-adjoining sentence adds guidance without revising any numbered step or TASK-009's step-internal prompts at lines 185 + 193. **AC2 Gate-3 prompt** — *additive*: § Process: Review's header-to-step-1 gap is empty prose-wise; new step-adjoining sentence adds guidance without revising any numbered step. **Cross-Epic preservation** — *preserved unchanged*: TASK-009's step-2 scope-guard sub-bullet and step-3 Naming paragraph remain present with all four verbatim substrings intact (`an unintended file changed`; `Scope-Boundary Acceptance Criteria Use Cross-Type Guards`; `TASK-A, TASK-B, TASK-C`; `descriptive title`). The Step-2 and Step-3 Edit-tool anchors are scoped to the section boundary and **cannot** match anywhere inside TASK-009's landings. If any prompt turns out to contradict existing prose (unexpected), implementation pauses and the directing party is flagged before landing — mirroring TASK-009 AC7 / TASK-008 AC11 protocol. *(Closes AC7.)*

7. **Verbatim-phrasing `grep` verification — six literal-substring searches against the landed source.**
   - `grep -n "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` → **2 hits** (one per section anchor — primary AC1+AC2 verification; proves both prompts landed the verbatim trigger phrase as plain prose with no Markdown interposing).
   - `grep -n "Pre-Commit Checklist" shannon/skills/work-items/skill.md` → **≥2 new hits** (one per cross-reference; substring did not appear in the skill before this Task).
   - `grep -n "an unintended file changed" shannon/skills/work-items/skill.md` → **1 hit** (TASK-009's step-2 prompt preserved) — cross-Epic preservation confirmation.
   - `grep -n "TASK-A, TASK-B" shannon/skills/work-items/skill.md` → **1 hit** (TASK-009's step-3 Naming preserved) — cross-Epic preservation confirmation.
   - `grep -n "descriptive title" shannon/skills/work-items/skill.md` → **1 hit** (TASK-009's positive requirement preserved) — cross-Epic preservation confirmation.
   - `grep -n "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" shannon/skills/work-items/skill.md` → **1 hit** (TASK-009's step-2 cross-reference preserved) — cross-Epic preservation confirmation.

   *(Closes the verbatim-phrasing portion of AC1, AC2, AC5, AC6; the four cross-Epic-preservation greps are mechanical evidence for AC7's cross-Epic-preservation clause.)*

8. **Update derived bookkeeping — `task_index.md` and EPIC-008 § Plan Task 2 entry at line 122.** Update `docs/tasks/task_index.md` TASK-010 line to reflect new status at each transition (PLANNED at Plan acceptance; IMPLEMENTING when implementation begins; IMPLEMENTED at completion of Steps 1–7; REVIEW at Gate 3 entry; APPROVED at Gate 3 exit). Update `docs/epics/EPIC-008-development-conventions-from-dogfooding.md` § Plan Task 2 entry (currently labelled `(ELABORATED, 9 ACs grounded in live shannon/skills/work-items/skill.md 301-line post-TASK-009 anchors)` at line 122) to reflect TASK-010's advancing status. *(Bookkeeping per the unified lifecycle — supports AC8's scope-confined diff; dogfoods TASK-007's editing-order convention.)*

9. **Scope-confined-diff verification — two-part for the gitignored split.**
   - **(a) `git status` check** — confirm modified tracked files are exactly: `shannon/skills/work-items/skill.md` (source — AC1 + AC2 edits); `docs/tasks/TASK-010-add-meta-gap-skill-prompts-and-resync-claude.md` (this file); `docs/tasks/task_index.md` (TASK-010 status transitions); `docs/epics/EPIC-008-development-conventions-from-dogfooding.md` (§ Plan Task 2 status update at line 122). Exactly 4 tracked files.
   - **(b) `diff` check** — `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns empty (deployed copy matches source).

   Both must hold for AC8 to pass. Confirm specifically out of scope: no file under `shannon/skills/` outside `work-items/`; no other file under `.claude/`; no mandated document modified (including `development_guide.md` — TASK-007's territory, now stable — and `conceptual_design.md` — TASK-008's territory, now stable); no work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` (EPIC-008 AC#6 Epic Gate 3 territory) and `docs/knowledge/` (EPIC-008 AC#7 Epic Gate 3 territory) unchanged. Cross-type guard per the rule TASK-008 ratified at `conceptual_design.md:179`. *(Closes AC8 — fourth pleasing recursion: scope-guard disciplines its own implementer.)*

10. **No commit until Gate 3.** Per AC9 and `development_guide.md` § Commit Cadence, no `git commit` runs across Steps 1–9. Pre-Gate-3 diffs accumulate in the working tree; the Gate-3 commit lands after directing-party approval and triggers a push per § Push Cadence trigger (a). Verified at Gate 3 by `git log master..HEAD` showing zero new commits attributable to TASK-010's diff range until the Gate-3 commit lands. *(Closes AC9.)*

### Dependencies

- **EPIC-008 PLANNED** — met (PLANNED 2026-05-24; § Plan Task 2 names TASK-010 at line 122).
- **TASK-007 (EPIC-008 Task 1) APPROVED** — met (APPROVED 2026-05-24, commit `eac486b`). The outbound cross-reference target at `development_guide.md:114` exists with the verbatim trigger phrasing and the outbound parenthetical naming both target anchors (§ Process: Plan + § Process: Review).
- **TASK-009 (EPIC-007 Task 2) APPROVED** — met (APPROVED 2026-05-25, commit `1293b98`). The skill file is in its post-TASK-009 301-line state with step-internal prompts at lines 185 + 193 intact (all four verbatim substrings grep-verifiable).
- **TASK-005 + TASK-009 precedent** — met for the `cp -R` re-sync mechanic + `diff -rq` verification. The trailing-dot form is captured in TASK-009's Deviations from Plan.
- **`shannon/skills/work-items/skill.md` 301 lines** — anchors at lines 172 / 174 / 185 / 193 / 232 / 234 stable (confirm at Step 1).
- **`.claude/skills/work-items/skill.md` byte-identical to source** — clean baseline (confirm at Step 1; `diff` empty at elaboration time).
- **No dependency on any future Task** — TASK-010 closes EPIC-008's implementing work. After APPROVED, only EPIC-008's Gate 3 bookkeeping (AC#6 scratchpad — including the second-approver move of the shared "Framework clarifications" item — and AC#7 Extension knowledge note) remains.

### Risks

- **`.claude/` re-sync omission — TASK-005 / TASK-009's flagged risk.** If Step 4 is skipped, the deployed framework silently drifts from source: `.claude/` is gitignored, so an omission leaves no tracked diff. **Mitigation**: re-sync is an explicit named Step 4 with its own verification Step 5 (`diff -rq`); Implementation Notes records the verification result; Gate 3 re-runs the diff.

- **Verbatim trigger phrasing drift — Markdown formatting interposing breaks the AC1/AC2 grep tests.** Plausible variations: backticks around individual words; bold around `framework-general ambiguity`; italic around `during this work`; smart-quote / smart-dash auto-conversion of `—`. Any such formatting *inside* the substring boundary breaks the verbatim grep at Step 7. (Italicising the *whole* surrounding sentence is fine — the substring chars remain contiguous.) **Mitigation**: copy the substring letter-for-letter from `development_guide.md:114` rather than retyping; Step 7's first grep verifies; the implementer reads the scratchpad lesson at Step 1 pre-flight. This is precisely TASK-009's scratchpad-recorded lesson applied prospectively.

- **Cross-Epic prompt collision — TASK-009's step-internal landings disturbed by accidental edit-tool anchor-match.** TASK-009's prompts at lines 185 + 193 sit inside § Process: Plan, the same section AC1 targets. If the Step 3 Edit anchor is too broad (e.g. anchoring on `### 1. Identify Target` alone — which appears under both § Process: Plan AND § Process: Review and is **not** unique), the Edit tool may match the wrong section or partially overlap a TASK-009 landing. **Mitigation**: Edit-tool anchors at Steps 2 and 3 use **section-boundary uniqueness** (`## Process: Plan (Gate 2)\n\n### 1. Identify Target` and `## Process: Review (Gate 3)\n\n### 1. Identify Target`) — the full-section-header prefix disambiguates and confines the match to the empty header-to-step-1 gap. Step 6 records named cross-Epic-preservation verification; Step 7's four cross-Epic-preservation greps provide mechanical evidence.

- **House style for step-adjoining prompts not yet established — TASK-010 sets the convention.** Every existing `## Process: *` section jumps straight from heading to `### 1.` with no intervening prose. The recommended form (italicised single sentence preceded and followed by blank lines) is a *proposal* this Task lands; subsequent step-adjoining additions inherit it. If the form turns out visually wrong at landing, implementer switches to short-paragraph form. **Mitigation**: AC text constrains shape (single-sentence soft directive); implementer rationale recorded in Implementation Notes at landing; Gate 3 reviewer checks proportionality.

- **Additivity violation — a new prompt unexpectedly contradicts existing prose.** Most plausible failure modes: (a) AC1's "moment of planning" framing implicitly contradicts § Process: Plan step 1 (*Identify Target*)'s ELABORATED-status check; (b) AC2's "moment of review" framing implicitly contradicts § Process: Review step 5 (*Gate 3 Approval*)'s explicit approval ritual. **Mitigation**: Step 6 per-prompt additivity check, with single-sentence verification recorded; pause-and-flag protocol if non-additivity surfaces. The two new prompts are guidance additions framed as section-level soft directives (not step revisions), so the risk is residual but real.

- **Over-formalisation — two small soft prompts becoming heavyweight prose.** Each AC's three required elements (verbatim trigger phrase + disposition instruction + cross-reference) can easily bloat to a paragraph. **Mitigation**: AC text constrains shape; the recommended italicised-single-sentence form keeps the prompt visually constrained; Gate 3 reviewer checks proportionality against TASK-009's step-internal prompts.

- **Bookkeeping drift — `task_index.md` or EPIC-008 § Plan Task 2 line not updated at the right transition.** **Mitigation**: Step 8 names the bookkeeping update explicitly, including the specific line in EPIC-008 § Plan Task 2 (line 122). The editing-order convention TASK-007 ratified disciplines this.

---

## Implementation Notes

*Filled at `/task-implement TASK-010` (2026-05-27). All 10 Plan Steps executed in strict bottom-to-top order by pre-edit line number; all 9 ACs satisfied; pending Gate 3 review.*

### Additivity Verification (AC7)

Three single-sentence statements, including the named cross-Epic preservation check:

- **AC1 Gate-2 prompt at § Process: Plan (line 174)** — *additive*: § Process: Plan's existing header-to-step-1 gap was empty prose-wise; the new step-adjoining italicised sentence adds guidance without revising any of the six numbered steps (174 *Identify Target* now at 176, 178 *Spawn Planning Subagent* now at 180 incl. TASK-009's step-2 sub-bullet at 187, 189 *Cascading Preparation* now at 191 incl. TASK-009's step-3 Naming paragraph at 195, 195 *Present and Iterate* now at 197, 199 *Gate 2 Approval* now at 201, 206 *Resume* now at 208) — line numbers shifted downward by 4 (AC1's 2 lines + AC2's 2 lines above § Process: Review) but semantic content unchanged.
- **AC2 Gate-3 prompt at § Process: Review (line 236)** — *additive*: § Process: Review's existing header-to-step-1 gap was empty prose-wise; the new step-adjoining italicised sentence adds guidance without revising any of the six numbered steps (234 *Identify Target* now at 238, etc.) — line numbers shifted downward by 2 (AC2's 2 lines) but semantic content unchanged.
- **Cross-Epic preservation (TASK-009's step-internal prompts)** — *preserved unchanged*: TASK-009's step-2 scope-guard sub-bullet (originally line 185, now line 187 — shifted +2 by AC1 insertion) and step-3 Naming paragraph (originally line 193, now line 195 — shifted +2 by AC1 insertion) remain present with all four verbatim substrings intact: `an unintended file changed` (1 hit at line 187), `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` (1 hit at line 187), `TASK-A, TASK-B` (1 hit at line 195), `descriptive title` (1 hit at line 195). The section-boundary-uniqueness Edit-tool anchors (`## Process: Review (Gate 3)\n\n### 1. Identify Target` and `## Process: Plan (Gate 2)\n\n### 1. Identify Target`) confined the matches to the empty header-to-step-1 gaps as intended.

No prompt turned out non-additive; no pause-and-flag required.

### Verbatim-Phrasing Verification (Step 7)

Six literal-substring `grep` searches against the landed source skill plus the three-way bold-lead sanity check:

- `grep -nc "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` → **2** hits at lines 174 (§ Process: Plan landing) and 236 (§ Process: Review landing) ✓ — primary AC1+AC2 verification; proves both prompts landed the verbatim trigger phrase as plain prose with no Markdown interposing between substring chars.
- `grep -nc "Pre-Commit Checklist" shannon/skills/work-items/skill.md` → **2** new hits at lines 174 + 236 ✓ — the cross-reference targets landed at both prompts.
- `grep -nc "an unintended file changed" shannon/skills/work-items/skill.md` → **1** hit at line 187 ✓ — TASK-009's step-2 scope-guard prompt preserved unchanged.
- `grep -nc "TASK-A, TASK-B" shannon/skills/work-items/skill.md` → **1** hit at line 195 ✓ — TASK-009's step-3 Naming exclusion preserved unchanged.
- `grep -nc "descriptive title" shannon/skills/work-items/skill.md` → **1** hit ✓ — TASK-009's step-3 positive requirement preserved unchanged.
- `grep -nc "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" shannon/skills/work-items/skill.md` → **1** hit at line 187 ✓ — TASK-009's step-2 cross-reference target preserved unchanged.

**Three-way bold-lead consistency sanity check** — `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` still character-identical across the three files: `shannon/skills/work-items/skill.md:187`, `docs/conceptual_design.md:179`, `docs/development_guide.md:79`.

### Scope-Confined-Diff Verification (AC8, Step 9)

**Two-part verification** for the gitignored split:

**(a) `git status` — tracked files** showed exactly the four expected files:

- ` M shannon/skills/work-items/skill.md` (source — AC1 + AC2 edits at lines 174 + 236)
- ` M docs/tasks/TASK-010-add-meta-gap-skill-prompts-and-resync-claude.md` (this file — Status / Activity Log / Implementation Notes per the unified lifecycle)
- ` M docs/tasks/task_index.md` (TASK-010 status update)
- ` M docs/epics/EPIC-008-development-conventions-from-dogfooding.md` (§ Plan Task 2 entry status update at line 122)

Exactly 4 tracked files. No file under `shannon/skills/` outside `work-items/`; no other `.claude/` file modified; no mandated document modified (including `development_guide.md` — TASK-007's territory, now stable; and `conceptual_design.md` — TASK-008's territory, now stable); no work-item template under `shannon/skills/*/templates/`; `docs/scratchpad.md` and `docs/knowledge/` unchanged.

**(b) `diff` — gitignored deployed-copy check**: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` returns **empty** post-final-re-sync — the deployed copy matches source byte-for-byte. Both files at 305 lines (3 lines added per prompt: italicised sentence + blank line + blank line). AC3 re-sync mechanic and AC4 alignment invariant both satisfied.

Cross-type guard at AC8 satisfied. **Fourth pleasing recursion** confirmed: the very Task that lands the meta-gap routing-channel prompts disciplines its own scope assertion by the scope-guard rule whose step-internal prompt sits adjacent to its own landing point.

### Commit Deferral (AC9)

No `git commit` ran across Steps 1–9. Pre-Gate-3 diffs accumulate in the working tree per § Commit Cadence. `git log master..HEAD` empty over this Task's diff range. The Gate-3 commit will land after directing-party approval and trigger a push per `development_guide.md` § Push Cadence trigger (a) — landing all three pending Gate commits (`1a68711` Gate 1 + `025f28f` Gate 2 + the Gate-3 commit) in one push.

### Deviations from Plan

- **Sub-position decision** — landed as **italicised single-sentence form** per Plan recommendation. No override to short-paragraph form needed; the italicised-single-sentence form rendered cleanly between the section header and the `### 1.` heading at both insertion points. Establishes the convention for step-adjoining prompts in `## Process: *` sections (none existed before; if future Tasks add such prompts, they inherit this form).

- **Sentence framing — close mirror of `development_guide.md:114`'s outbound form**. The Plan recommended the soft-prompt sentence open with "Soft prompt at the moment of planning" / "Soft prompt at the moment of review" — kept as-is. The trigger-question structure ("did this work resolve a framework-general ambiguity ...? If yes, capture it ...") mirrors the form at `development_guide.md:114` precisely so the prompt feels familiar to anyone reading both landings.

- **No backticks or bold inside the verbatim trigger phrase** — the substring `framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive` lands as plain prose inside the italicised soft-prompt sentence. The italic `*...*` markers sit at the boundaries of the whole sentence (one `*` before "Soft prompt" and one `*` after "Pre-Commit Checklist.") — NOT interposing between the substring chars, so `grep -n "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` returns 2 hits cleanly. This is the explicit application of TASK-009's scratchpad-recorded lesson: Markdown formatting at the boundaries is fine; only formatting that interposes within the substring breaks grep matching.

### Gotchas

- **None this Task.** All 2 Edits succeeded on first attempt (no system-reminder-induced read-state retries). The section-boundary-uniqueness Edit anchors confined matches to the empty header-to-step-1 gaps as designed; no accidental match inside TASK-009's step-internal landings (confirmed by Step 7's four cross-Epic-preservation greps all returning their expected hit counts at the expected lines).

### Documents Updated

- **`shannon/skills/work-items/skill.md`** (source) — two step-adjoining soft prompts added per EPIC-008's AC#3b Gate-2 + Gate-3 halves. File grew from 301 lines to 305 lines (4 lines added in total: each prompt = 1 italicised sentence + 1 blank line above + 1 blank line below; the section-header-to-step-1 gap was already 1 empty line so each insertion net-added 2 lines).
  - **Line 174** — new step-adjoining italicised soft prompt under § Process: Plan (Gate 2) directing the planning subagent to surface framework-general ambiguity at the moment of planning by routing to `scratchpad.md` or opening a follow-up work item; cross-references `development_guide.md` § Testing Strategy → Pre-Commit Checklist.
  - **Line 236** — new step-adjoining italicised soft prompt under § Process: Review (Gate 3) with identical structure and trigger phrasing adapted to the review subagent and the moment of review.
  - All other content of the skill unchanged: TASK-009's step-internal prompts at (newly) lines 187 + 195 preserved with all four verbatim substrings intact; all six numbered steps in both sections preserved (with line numbers shifted +2 each from AC1's and AC2's insertions).
- **`.claude/skills/work-items/skill.md`** (deployed copy, gitignored) — re-synced from source via `cp -R shannon/skills/work-items/. .claude/skills/work-items/`. Post-sync `diff` against source returns empty (byte-identical). Re-sync ran once after both source edits landed (Step 4).

### Recursive Dogfood — Fourth Pleasing Instance

The pattern TASK-008 introduced (AC8 / AC12 disciplined by the very rule the Task ratifies at AC5) plays out here as a **fourth instance** with a structural twist unique to TASK-010:

- **The Task lands the meta-gap routing channel itself** — the soft prompts at § Process: Plan and § Process: Review are *the channel* (the discipline that gets surfaced framework-general ambiguities routed to scratchpad or follow-up work items at the moments of action).
- **At this Task's own Gate 3, the review subagent will face the very prompt this Task lands.** The Gate-3 review subagent reads `shannon/skills/work-items/skill.md` § Process: Review (Gate 3) and encounters the new step-adjoining prompt — directed at itself — asking whether *this work* resolved a framework-general ambiguity. The answer at Gate 3 will be honest: yes, the framework-general ambiguity of "how do framework-general resolutions reach the framework at the moments of action" is *itself* the ambiguity this work resolves; the channel codified is the resolution.
- **Three landed-convention dogfoods inherited and applied during execution**: (i) TASK-007's editing-order convention (source body first; deployed copy after; `diff` verify) — Steps 2–5; (ii) TASK-008's scope-guard rule — AC8 cross-type-guard phrasing; (iii) TASK-009's plain-prose substring discipline — AC1+AC2 verbatim trigger phrase landed as plain prose, no Markdown formatting interposing between substring chars; verified by Step 7's first grep returning the expected 2 hits.

**One small framework-general observation worth recording** (candidate for EPIC-008 Gate 3 Extension knowledge note per AC#7): the **section-boundary-uniqueness Edit-anchor technique** TASK-010 introduced (using `## Section Header\n\n### Subsection Header` as the boundary anchor when the subsection name alone appears multiple times) is a generalisable pattern for editing skill files where step names repeat across sections. Worth capturing alongside the recursive-dogfood record EPIC-008 AC#7 will land.

---

## Review

*Filled at `/task-review TASK-010` (Gate 3) — 2026-05-27. Independent verification subagent verdict: APPROVE. All 9 ACs Met; no Gaps or Issues.*

### Verification

- [x] All acceptance criteria met — 9/9 Met per independent subagent (AC1 line 174; AC2 line 236; AC3+AC4 both `diff`s empty / both files 305 lines; AC5 line 174 cross-reference; AC6 line 236 cross-reference; AC7 Implementation Notes additivity + cross-Epic preservation block, all four TASK-009 substrings present at lines 187+195; AC8 git status 4-file tracked scope + diff empty for deployed; AC9 git log 025f28f..HEAD empty)
- [x] Changes follow `development_guide.md` — Markdown § Code Style conventions adhered to; TASK-007's editing-order convention (`development_guide.md:79`) executed during this Task's execution (source body first; deployed copy re-synced after; grep + diff verify); Pre-Commit Checklist all five items tickable including the meta-gap routing-channel item (now operationalised on the skill side); § Push Cadence trigger (a) will fire on Gate-3 commit landing
- [x] Relevant documents updated — `shannon/skills/work-items/skill.md` v[301-line] → v[305-line] with two new step-adjoining soft prompts at § Process: Plan (line 174) + § Process: Review (line 236); `.claude/skills/work-items/skill.md` re-synced byte-identical; no mandated document modified
- [x] Knowledge captured where useful — three pleasing-recursion landings recorded in Implementation Notes § Recursive Dogfood (fourth instance of the pattern, structurally unique to TASK-010 because the prompt this Task lands is the prompt its own Gate-3 review subagent encounters); one Plan deviation recorded transparently (sub-position landed as italicised single-sentence form per Plan recommendation — establishes the convention for step-adjoining prompts in `## Process: *` sections); **one framework-general observation surfaced and flagged for EPIC-008 Gate 3 Extension knowledge note per AC#7**: the section-boundary-uniqueness Edit-anchor technique (using `## Section\n\n### Subsection` as the boundary anchor when subsection names alone repeat across sections)

### Review Notes

- **Plain-prose substring discipline verified** (TASK-009's scratchpad lesson applied prospectively): `grep -n "framework-general ambiguity surfaced during this work" shannon/skills/work-items/skill.md` returns exactly **2 hits at lines 174 + 236** — italic `*` markers at sentence boundaries did not interpose within the substring; the lesson worked as designed at first application.
- **Cross-Epic preservation verified character-by-character**: TASK-009's four verbatim substrings intact at the +2 shifted positions (originally lines 185+193, now lines 187+195): `an unintended file changed` (1 hit at line 187); `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` (1 hit at line 187); `TASK-A, TASK-B` (1 hit at line 195); `descriptive title` (1 hit at line 195). Section-boundary-uniqueness Edit anchors confined matches as designed.
- **Three-way bold-lead consistency preserved**: `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` still character-identical across `shannon/skills/work-items/skill.md:187`, `docs/conceptual_design.md:179`, `docs/development_guide.md:79`.
- **Bidirectional EPIC-008 AC#5 pair complete**: outbound at `development_guide.md:114` names both skill anchors; inbound at `skill.md:174` and `skill.md:236` names the Pre-Commit Checklist anchor. Both halves resolve.
- **`.claude/` re-sync verified**: `diff shannon/skills/work-items/skill.md .claude/skills/work-items/skill.md` → empty; `diff -rq` of directories → empty; both files at 305 lines.
- **Scope-confined-diff (tracked half) verified**: `git status -s` shows exactly the four expected tracked files; no file under `shannon/skills/` outside `work-items/`; no mandated document modified; no `docs/scratchpad.md` or `docs/knowledge/` change.
- **AC9 commit-deferral verified**: `git log 025f28f..HEAD --oneline` returns empty.
- **EPIC-008 coverage closed by this Task**: AC#3b Gate-2 half, AC#3b Gate-3 half, AC#5 inbound half. EPIC-008 now has only **AC#6 (scratchpad bookkeeping) + AC#7 (Extension knowledge note recording the recursive-dogfood character)** remaining for Epic Gate 3.
- **FEAT-008 § Ideal State bullet 3** (routing channel for resolved framework-general ambiguity): substantively delivered for the skill half. Bullet annotation flip from `*(not yet met — addressed by EPIC-008)*` → `*(met — delivered by EPIC-008)*` belongs to EPIC-008 Gate 3 bookkeeping, not to TASK-010.
- **Fourth pleasing recursion — structurally unique to TASK-010**: the prompt this Task lands is the prompt its own Gate-3 review subagent encounters (the review subagent reads `## Process: Review (Gate 3)` line 236's prompt while reviewing the work that landed it). The channel processes its own implementing Task. Recorded in Implementation Notes § Recursive Dogfood.
- **One framework-general observation surfaced** (candidate for EPIC-008 Gate 3 Extension knowledge note per AC#7): the section-boundary-uniqueness Edit-anchor technique — using `## Section\n\n### Subsection` as the boundary anchor when the subsection name alone repeats across sections — is a generalisable pattern. Worth capturing alongside the recursive-dogfood record.
- **Findings**: Strengths only; Gaps/Issues: None; Suggested fixes: None required.

---

## Activity Log

- **2026-05-27** — APPROVED via Gate 3. Independent verification subagent returned all 9 ACs Met with no Gaps or Issues; full per-AC report folded into § Review. Plain-prose substring discipline verified (TASK-009's lesson applied prospectively without slippage; `grep -n "framework-general ambiguity surfaced during this work"` returns 2 hits at lines 174 + 236 as designed). Cross-Epic preservation of TASK-009's step-internal prompts verified character-by-character at the +2 shifted positions (lines 187 + 195). Three-way bold-lead consistency for `Scope-Boundary Acceptance Criteria Use Cross-Type Guards` still character-identical across `skill.md:187` + `conceptual_design.md:179` + `development_guide.md:79`. Bidirectional EPIC-008 AC#5 pair complete (`development_guide.md:114` ↔ `skill.md:174`+`:236`). `.claude/` re-sync verified byte-identical (both 305 lines; `diff` empty). EPIC-008 coverage closed for this Task — AC#3b Gate-2 half, AC#3b Gate-3 half, AC#5 inbound half. Final disposition: Status REVIEW → APPROVED; file archived to `docs/tasks/archive/`; `task_index.md` link path updated; EPIC-008 § Plan Task 2 entry updated to APPROVED. Commit message: *"Approve TASK-010 (Gate 3) — Add meta-gap skill prompts and re-sync .claude/"*. Push triggered per `development_guide.md` § Push Cadence trigger (a) — landing all three pending Gate commits (`1a68711` Gate 1 + `025f28f` Gate 2 + this Gate-3 commit) in one push. **TASK-010 is the closing implementing Task of the FEAT-003 / FEAT-008 dogfooding-harvest pair**: all four implementing Tasks (TASK-007 + TASK-008 + TASK-009 + TASK-010) APPROVED; EPIC-007 APPROVED 2026-05-27 (commit `cffb1ab`); EPIC-008 needs only its Gate 3 bookkeeping (AC#6 scratchpad — including the second-approver move of the shared "Framework clarifications" item that EPIC-007 Gate 3 annotated with partial-closure — and AC#7 Extension knowledge note recording recursive-dogfood character; the section-boundary-uniqueness Edit-anchor technique surfaced by TASK-010 is a candidate addition to that knowledge note). **Fourth pleasing recursion** confirmed structurally and uniquely: at this Gate 3, the review subagent reading `## Process: Review (Gate 3)` encountered the very step-adjoining prompt this Task lands at line 236 — directed at itself — asking whether this work resolved a framework-general ambiguity. The honest answer at Gate 3 is yes: this work resolves the framework-general meta-gap of "how do framework-general resolutions reach the framework at the moments of action"; the channel codified is the resolution. The channel processes its own implementing Task.
- **2026-05-27** — IMPLEMENTED. All 10 Plan Steps executed in strict bottom-to-top order by pre-edit line number against `shannon/skills/work-items/skill.md` + `.claude/` re-sync: Step 1 (pre-flight anchor + clean-baseline confirmation — all anchors stable at lines 172/174/185/193/232/234; source ↔ deployed byte-identical at 301 lines) → Step 2 (AC2 Gate-3 step-adjoining prompt inserted at § Process: Review at line 236 in post-edit positions, between header line 234 and `### 1. Identify Target` line 238) → Step 3 (AC1 Gate-2 step-adjoining prompt inserted at § Process: Plan at line 174, between header line 172 and `### 1. Identify Target` line 176) → Step 4 (`.claude/` re-sync via `cp -R shannon/skills/work-items/. .claude/skills/work-items/` — trailing-dot form per TASK-009 precedent) → Step 5 (`diff -rq` verification post-re-sync — empty output, both files 305 lines, byte-identical) → Step 6 (additivity verification — three single-sentence statements recorded in Implementation Notes including the named cross-Epic preservation check; all additive; no pause-and-flag) → Step 7 (verbatim-phrasing `grep` verification — `framework-general ambiguity surfaced during this work` returns 2 hits at lines 174 + 236; `Pre-Commit Checklist` returns 2 new hits; all four TASK-009 cross-Epic-preservation greps return 1 hit each at the expected shifted lines; three-way bold-lead consistency sanity check across `skill.md:187` + `conceptual_design.md:179` + `development_guide.md:79` still character-identical) → Step 8 (derived bookkeeping updated: `task_index` TASK-010 line; EPIC-008 § Plan Task 2 entry at line 122) → Step 9 (two-part scope-confined-diff verified — `git status` exactly 4 tracked files; `diff` between source and deployed empty — cross-type guard at AC8 satisfied) → Step 10 (no commit mid-implementation; verified by `git log master..HEAD` empty). All 9 ACs satisfied. **One Plan deviation** recorded (sub-position decision landed as italicised single-sentence form per Plan recommendation; no override needed — establishes the convention for step-adjoining prompts in `## Process: *` sections). **No gotchas this Task** — section-boundary-uniqueness Edit anchors confined matches to the empty header-to-step-1 gaps as designed; no accidental match inside TASK-009's step-internal landings. **Fourth pleasing recursion** confirmed (and uniquely structural for TASK-010): the Task lands the meta-gap routing channel itself, and at its own Gate 3 the review subagent will face the very prompt this Task lands — the channel processes its own implementing Task. **Three landed-convention dogfoods inherited and applied**: TASK-007 editing-order; TASK-008 scope-guard; TASK-009 plain-prose substring discipline. **One framework-general observation surfaced** (candidate for EPIC-008 Gate 3 Extension knowledge note per AC#7): the section-boundary-uniqueness Edit-anchor technique (using `## Section\n\n### Subsection` as the boundary anchor when the subsection name alone repeats across sections). Pending `/task-review TASK-010` (Gate 3).
- **2026-05-27** — IMPLEMENTING via `/task-implement TASK-010`. Status PLANNED → IMPLEMENTING; executing the 10-step bottom-to-top plan against `shannon/skills/work-items/skill.md` + `.claude/skills/work-items/` re-sync.
- **2026-05-27** — PLANNED via Gate 2. Planning subagent drafted a 10-step bottom-to-top plan adapted from TASK-009's approved precedent, with AC1/AC2 ordering inverted (AC2 first at line 232, AC1 second at line 172) because § Process: Review is higher-line than § Process: Plan in TASK-010's scope. Plan highlights: **section-boundary-uniqueness Edit anchors** (`## Process: Review (Gate 3)\n\n### 1. Identify Target` / `## Process: Plan (Gate 2)\n\n### 1. Identify Target`) — the `### 1. Identify Target` substring appears in both § Process: Plan AND § Process: Review and is not safe as a standalone Edit anchor; the full-section-header prefix disambiguates and confines the match to the empty header-to-step-1 gap; **recommended italicised single-sentence soft-prompt form** with rationale (visually distinct from numbered-step prose; matches soft-directive register; constrains proportionality) — implementer may override at landing; **trailing-dot `cp -R` form** for the re-sync per TASK-009's captured Deviations; **two-part scope-confined-diff** (Step 9 (a) `git status` for tracked + (b) `diff` for the gitignored deployed copy) — TASK-009's gitignored-split discipline applied. Three landed-convention dogfoods inherited explicitly: TASK-007 editing-order, TASK-008 scope-guard cross-type-guard form, **TASK-009 plain-prose substring discipline** (the verbatim trigger phrasing from `development_guide.md:114` lands as plain prose at AC1+AC2 — no backticks/italic/bold inside substring boundary — so the `grep -n "framework-general ambiguity surfaced during this work"` returns 2 hits cleanly). **Fourth pleasing recursion** named: the Task lands the meta-gap routing-channel prompts and will, at its own Gate 3, be processed by the very routing channel it lands (the review subagent will face the same prompt). All 9 ACs trace 1:1 to numbered Steps; cross-Epic preservation of TASK-009's step-internal prompts at lines 185 + 193 has a named verification step and four mechanical grep checks. 7 risks with mitigations (re-sync omission; verbatim drift via Markdown-in-substring; cross-Epic collision via anchor disambiguation; step-adjoining house-style proposal; additivity violation; over-formalisation; bookkeeping drift). No drift; one minor under-specified element (sub-position form deferred to implementer with recommendation — appropriate). Plan approved by directing party without modification.
- **2026-05-27** — ELABORATED via Gate 1. Elaboration subagent grounded each AC in the live `shannon/skills/work-items/skill.md` (301 lines; post-TASK-009 state with step-internal prompts intact at lines 185 + 193) and confirmed the deployed copy at `.claude/skills/work-items/skill.md` is byte-identical (also 301 lines; `diff` empty — clean post-TASK-009 baseline). Confirmed anchor points: § Process: Plan (Gate 2) at `## Process: Plan (Gate 2)` line 172 (section spans 172–208; first numbered step `### 1. Identify Target` at line 174 — step-adjoining insertion point between header and first step); § Process: Review (Gate 3) at `## Process: Review (Gate 3)` line 232 (section spans 232–270; first numbered step at line 234 — same insertion shape). House style for step-adjoining prose not yet established in the skill (every existing `## Process: *` jumps straight from heading to `### 1.`) — TASK-010 establishes the convention; implementer chooses italicised-sentence vs short-paragraph form at landing with rationale recorded. 9 testable ACs drafted (AC1–AC9), traced 1:1 to EPIC-008 ACs: AC1+AC5 → EPIC-008 AC#3b Gate-2 half (Gate-2 prompt + cross-reference to `development_guide.md:114`); AC2+AC6 → EPIC-008 AC#3b Gate-3 half (Gate-3 prompt + cross-reference to same anchor); AC3 covers the explicit `.claude/` re-sync per the TASK-005 / TASK-009 precedent using the trailing-dot `cp -R` form TASK-009 captured; AC4 covers preservation of the byte-identical source↔deployed invariant; AC7 covers additivity verification including cross-Epic preservation of TASK-009's step-internal prompts at lines 185 + 193 (named verification step because TASK-010's step-adjoining insertions land in the same section); AC8 covers cross-type-guard scope (with explicit accounting for the gitignored `.claude/` copy verified separately by `diff` — third pleasing recursion of the scope-guard rule disciplining its own implementer's scope assertion, mirroring TASK-008 AC12 + TASK-009 AC8); AC9 covers no-commit-mid-implementation. **Three landed-convention dogfoods inherited**: TASK-007's editing-order convention (source body first; deployed copy after; diff-verify); TASK-008's scope-guard rule (cross-type-guard phrasing at AC8); **TASK-009's plain-prose substring discipline** (now in `docs/scratchpad.md` § Items — the verbatim trigger phrase lands as plain prose, no backticks/italic/bold around the substring chars, so the AC1+AC2 grep tests resolve cleanly). Cross-document verbatim consistency: AC1 and AC2 prompts repeat `framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive` letter-for-letter from `development_guide.md:114` (the canonical landing TASK-007 made); AC5 and AC6 cross-references match TASK-009's existing skill cross-reference form at lines 185 + 193 (backtick-quoted file path + `§` + `→` + plain-prose section name). Findings: all Strength + one minor under-specified Gap (step-adjoining prose form choice deferred to implementer — appropriate); no Drift — section headers match exactly between EPIC-008 AC text and live skill; no pre-existing source↔deployed drift; verbatim trigger phrasing at `development_guide.md:114` is intact. Directing party approved without modification.
- **2026-05-27** — DRAFT: Task created via `/task-create` under EPIC-008 as Task 2 of 2 (the `work-items` skill prompts + `.claude/` re-sync half — meta-gap routing channel skill side). EPIC-008's Plan resolved both location decisions for the skill prompts (AC#3b Gate 2 half → § Process: Plan step-adjoining; AC#3b Gate 3 half → § Process: Review step-adjoining) and named out-of-scope items (`development_guide.md` checklist line → TASK-007 already APPROVED; AC#6 scratchpad bookkeeping + AC#7 Extension knowledge note → Epic Gate 3 review). Initial intent captured in the Overview; Acceptance Criteria and Plan to be drafted at `/task-elaborate` and `/task-plan`. Three Tasks' worth of prior precedent applies: TASK-005 + TASK-009 (re-sync mechanic); TASK-007 (editing-order convention + outbound cross-reference at `development_guide.md:114`); TASK-008 (scope-guard rule + canonical double-quote form for verbatim landings); TASK-009 (plain-prose substring discipline for verbatim grep tests; cross-Epic preview confirmation that step-adjoining insertions don't collide with step-internal ones). This Task is the closing implementing Task of the FEAT-003 / FEAT-008 dogfooding-harvest pair (both EPIC-007 implementing Tasks already APPROVED; EPIC-007 itself APPROVED 2026-05-27 commit `cffb1ab`). After TASK-010's APPROVED, EPIC-008 needs only its Gate 3 bookkeeping (AC#6 scratchpad — including the second-approver move of the shared "Framework clarifications" item — and AC#7 Extension knowledge note recording the recursive-dogfood character) to close the whole harvest.
