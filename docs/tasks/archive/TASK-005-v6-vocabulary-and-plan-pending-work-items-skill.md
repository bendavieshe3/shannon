# TASK-005: V6 Vocabulary and PLAN-PENDING Removal in work-items Skill

## Metadata

- **Status**: APPROVED
- **Type**: Task
- **Parent**: [EPIC-005](../epics/EPIC-005-v6-skills-and-claude-template.md)
- **Feature**: [FEAT-006](../features/FEAT-006-multi-party-supervision.md)
- **Tags**: #framework #v6 #skills #work-items
- **Created**: 2026-05-23
- **Updated**: 2026-05-23 (approved)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Tasks are archived to `./archive/` once APPROVED.

---

## Requirements

### Overview

This is **TASK-005** of EPIC-005's three-Task plan (TASK-004 → TASK-005 → the verification Task). TASK-004 ([TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md), APPROVED 2026-05-23) settled the V6 vocabulary house style on the lower-risk files; **TASK-005 applies that style to `shannon/skills/work-items/skill.md` alone, AND completes the structural `PLAN-PENDING` rewrite that the file still contradicts technical_design v1.1 on.**

TASK-005 carries the heaviest and structurally distinct load of the three sibling Tasks — vocabulary judgement **plus** a structural rewrite. Elaboration verified the actual occurrence map by `rg -n "\b[Uu]sers?\b"` and `rg -n "PENDING"` against `shannon/skills/work-items/skill.md`:

- **Vocabulary sweep — 17 `[Uu]sers?` matches** (EPIC-005's Plan estimated ~16; the +1 discrepancy is recorded here and proceeded with, not blocked on — matches TASK-004's project-setup precedent where EPIC-005's ~15 estimate was actually 17). **All 17 are directing-party-sense** — there is no preserve-sense occurrence in this file (no "Target Users" heading, no file-merge "user content", no quoted-from-test). Lines: 27, 28, 81, 104, 110, 120, 144, 194, 227 (paired with implementer-sense "AI" — EPIC-005 AC#5), 271, 272, 273 (the three Gate sentences), 279, 283, 284, 290, 292. Each is reworded to name the *directing party*, or rephrased to drop the role noun where the sentence reads naturally without it. Phrasing copied from the V6-aligned document layer (conceptual_design v1.5, technical_design v1.1), not invented — same per-occurrence judgement TASK-004 used.
- **Implementer-sense rewordings** — three `AI` occurrences that name the work-doing role and must become *implementer*: line 275 (§ Quality Gates — "AI never self-approves across a gate"), line 282 (§ Cascading Operations — "AI marks children…"), line 283 (§ Cascading Operations — "AI presents a summary…"), and the `AI` half of line 227 (§ Process: Implement step 5 — "The user or AI may move…"). Lines 282 / 283 are subsumed by the structural rewrite below.
- **§ Quality Gates rewrite — work-items half of EPIC-005 AC#3.** The three "Explicit user approval" Gate sentences (lines 271–273) and the "AI never self-approves across a gate" sentence (line 275) are reworded so the **directing party** approves and the **implementer** cannot approve its own work — phrasing tracking conceptual_design *Supervisor Distinct From Implementer* / *Three Hard Gates* and technical_design § Gate Enforcement ("by convention, not technical control"). Lift, do not paraphrase.
- **Structural `PLAN-PENDING` rewrite — EPIC-005 AC#4.** `rg "PENDING"` returns **three** locations, all needing rewrite: line 190 (§ Process: Plan **step 3** — "Cascading Preparation"), line 200 (§ Process: Plan **step 5** — Gate 2 Approval note about children remaining `PLAN-PENDING`), and line 282 (§ Cascading Operations step 2). technical_design v1.1 explicitly retired these sub-statuses (commit `1943f3f`). All three rewrite to the *Batch Preparation, Individual Gates* model in technical_design § Cascading Operations: the implementer creates each child in DRAFT with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file (marked clearly as "prepared during EPIC-XXX planning, not yet reviewed"); the prepared content is surfaced when the directing party later runs the child's own gate command. No new sub-statuses, no `*-PENDING` tokens. **Match technical_design § Cascading Operations' wording verbatim where the mechanics are named** — a loose paraphrase that drops `*-PENDING` but invents different mechanics would itself be new drift (EPIC-005 § Risks).
- **§ Process: Implement step 5 — EPIC-005 AC#5.** Line 227 "The user or AI may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless." → names the **directing party** and the **implementer** as the two roles iterating, naming the iterative zone in V6 terms (conceptual_design *Iterative Implementation Zone* — "the implementer and the directing party iterate freely").
- **Leave-untouched anchors — confirmed V6-clean at survey time**: the **Re-elaboration Branch** sub-section under § Process: Elaborate (TASK-001, lines 157–170) already uses "directing party" (line 164) and is V6-clean; the **Feature `(Partial)` index-suffix** language under § Process: Create step 7 (TASK-002, line 116) and the *Index out of sync* cross-reference under § Failure Modes (TASK-002, line 293) carry no `[Uu]sers?` occurrence and use "the skill" / "the body is authoritative" — both already V6-clean. Confirm-only, not re-edited by this Task.

Delivers EPIC-005 **AC#4** and **AC#5**, and contributes the work-items half of **AC#2 / AC#3**. Cross-file consistency verification across all four EPIC-005 files (this skill, TASK-004's three files, and the document-layer reference) is the verification Task's job — out of scope here.

After editing the `shannon/` source, the deployed `.claude/skills/work-items/` is re-synced from source (the deployed tree is gitignored).

### Acceptance Criteria

Vocabulary sweep — directing-party-sense `[Uu]sers?` occurrences:

- [x] In `shannon/skills/work-items/skill.md`, each of the 17 directing-party-sense `[Uu]sers?` occurrences — lines 27, 28, 81, 104, 110, 120, 144, 194, 227, 271, 272, 273, 279, 283, 284, 290, 292 — is reworded to name the *directing party*, or rephrased to drop the role noun where the sentence reads naturally without it (e.g. "report creation"). No directing-party-sense `[Uu]sers?` token remains in the file. Phrasing tracks conceptual_design v1.5 (*Directing Party* glossary; *Three Hard Gates*) and the precedent already set by TASK-004 on the sibling skills — not invented.
- [x] No occurrence in `work-items/skill.md` has a preserve sense — survey confirmed at elaboration time. If any later occurrence is judged preserve-sense by the directing party during review, it is named in the Activity Log finding so the verification Task's audit knows it is intentional.

Implementer-sense rewordings — `AI` occurrences that name the work-doing role:

- [x] § Quality Gates line 275 ("AI never self-approves across a gate") is reworded so the **implementer** (not "AI") is named as the agent that cannot approve its own work, lifting the phrasing from conceptual_design *Supervisor Distinct From Implementer* ("the implementer cannot approve its own work") rather than paraphrasing.
- [x] The "AI" tokens at lines 282 and 283 are reworded to name the **implementer** as part of the § Cascading Operations structural rewrite below (subsumed there, not a separate edit).
- [x] At line 227, the "AI" half of "The user or AI may move…" is reworded to name the **implementer** (handled with the AC#5 rewrite below).

§ Quality Gates rewrite — work-items half of EPIC-005 AC#3:

- [x] § Quality Gates lines 271–273 (the three "Explicit user approval" Gate sentences) name the **directing party** as the explicit approver — phrasing tracking conceptual_design *Three Hard Gates* ("explicit approval by the directing party") and the project-documentation § Quality Gates rewrite TASK-004 already landed.
- [x] § Quality Gates line 275 ("AI never self-approves across a gate") is consistent with conceptual_design *Supervisor Distinct From Implementer* and technical_design § Gate Enforcement ("by convention, not technical control"); names the **implementer**.
- [x] After the rewrite, `work-items/skill.md § Quality Gates` and `project-documentation/skill.md § Quality Gates` use the same role nouns (directing party / implementer) for the same concepts. Strict four-way cross-file consistency is the verification Task's audit; this AC closes the work-items-side preconditions for it.

Structural `PLAN-PENDING` rewrite — EPIC-005 AC#4:

- [x] After the rewrite, `rg "PENDING" shannon/skills/work-items/skill.md` returns **zero** matches. The three current locations (line 190 § Process: Plan step 3; line 200 § Process: Plan step 5; line 282 § Cascading Operations) no longer contain `PLAN-PENDING`, `*-PENDING`, or any other `-PENDING` token.
- [x] § Process: Plan **step 3 ("Cascading Preparation")** is rewritten so that, when planning an Epic, the implementer **creates each child Task in DRAFT** with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file, marked clearly as "prepared during EPIC-XXX planning, not yet reviewed" — wording tracking technical_design § Cascading Operations: Batch Preparation, Individual Gates (steps 3 and 5: "creates each child task in DRAFT, with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the task file (marked clearly as 'prepared during EPIC-XXX planning, not yet reviewed')" and "the subagent surfaces the prepared elaboration instead of starting fresh; same for `/task-plan TASK-XXX`. Each child still requires its own Gate 1 and Gate 2 approval"). The wording is lifted, not loosely paraphrased — see EPIC-005 § Risks "PLAN-PENDING rewrite drifting from technical_design".
- [x] § Process: Plan **step 5 (Gate 2 Approval)** is reworded so that, on approval, child items prepared during the parent's planning **remain in DRAFT until each child's own gate command surfaces and approves the prepared draft** — naming the unified status lifecycle (no `PLAN-PENDING` sub-status), not a new transitional state.
- [x] § Cascading Operations is rewritten so steps 1–4 describe the **Batch Preparation, Individual Gates** model from technical_design § Cascading Operations: implementer plans the parent and writes the plan into its file → implementer identifies the child work items → implementer creates each child in DRAFT with prepared elaboration / plan drafts stashed inside the child file → implementer presents a summary of the parent plan and the prepared children to the directing party → when the directing party later runs each child's own gate command (e.g. `/task-elaborate TASK-XXX`, `/task-plan TASK-XXX`), the subagent surfaces the prepared content instead of starting fresh; each child still passes through Gate 1 and Gate 2 individually. No new sub-statuses introduced.
- [x] The § Cascading Operations rewrite explicitly tracks technical_design v1.1 § Cascading Operations: Batch Preparation, Individual Gates — the mechanics named in the skill (DRAFT children + stashed prepared drafts + surfaced at child's own gate; no `*-PENDING`) match that section verbatim where mechanics are named, not a loose paraphrase. the verification Task's AC#3 audit cross-checks against that section as the canonical reference.

§ Process: Implement step 5 — EPIC-005 AC#5:

- [x] § Process: Implement step 5 (line 227) is reworded from "The user or AI may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless." to name the **directing party or implementer** as the two roles, and the iterative zone in V6 terms — tracking conceptual_design *Iterative Implementation Zone* ("the implementer and the directing party iterate freely without gates. Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone").

Leave-untouched anchors — confirm V6-clean, do not re-edit:

- [x] The **Re-elaboration Branch** sub-section under § Process: Elaborate (TASK-001, lines 157–170) is confirmed V6-clean at survey time — it uses "directing party" (line 164) and "the implementer" framing throughout — and is **not modified** by this Task. The `git diff` for this Task shows no edits between the *Re-elaboration Branch* heading and the end of § Process: Elaborate.
- [x] The **Feature `(Partial)` index-suffix** language under § Process: Create step 7 (TASK-002, line 116) and the *Index out of sync* cross-reference under § Failure Modes (TASK-002, line 293) are confirmed V6-clean at survey time — neither carries a `[Uu]sers?` occurrence and both use role-neutral skill-as-actor phrasing — and are **not modified** by this Task.

Scope boundary and integrity:

- [x] Only `shannon/skills/work-items/skill.md` is edited under `shannon/`. No other skill file (`project-setup/skill.md`, `project-documentation/skill.md`), no template (`templates/*.md`, `templates/CLAUDE.md`), no command file (`shannon/commands/*.md`), no mandated document (`docs/*.md`), and no other work item (`docs/features/*.md`, `docs/epics/*.md`, `docs/tasks/*.md`, `docs/spikes/*.md` apart from this TASK-005 file itself), is modified by this Task. Phrased as a cross-type guard rather than a literal file count — the TASK-002 / TASK-003 lesson (recorded in TASK-003's archive) was that bare file-count caps obscure intent; the actual rule is "vocabulary + structural rewrite are confined to the work-items skill". The directing party may approve a same-purpose ripple edit if one surfaces during implementation, but it must be named in the Activity Log.
- [x] The skill self-identification line `"Activating work-items skill for [type] [verb]."` is retained unchanged at line 8 (the header instruction) and in § Self-Identification (currently line 297). Both instances appear in the file with their pre-edit wording.
- [x] No structural changes outside the named rewrites: no headings added or removed, no Process steps reordered, no sections moved, no templates list changed, no Work Item ↔ Document Relationships table edited. The diff is confined to (a) word-level vocabulary edits and (b) the prose rewrites at § Process: Plan steps 3 and 5, § Cascading Operations, § Process: Implement step 5, and § Quality Gates.

Verification — mechanically checkable:

- [x] After the edits, `rg "\b[Uu]sers?\b" shannon/skills/work-items/skill.md` returns **zero** matches (no preserve-sense occurrence exists in this file). If any match survives, it is named individually in the Activity Log as a directing-party-judged preserve-sense exception so the verification Task's audit treats it as intentional, not as missed.
- [x] After the edits, `rg "PENDING" shannon/skills/work-items/skill.md` returns **zero** matches — no `PLAN-PENDING`, no `*-PENDING`, no `-PENDING` token of any kind.
- [x] `rg -n "Activating work-items skill"` returns the two pre-existing lines unchanged (line 8 header instruction; § Self-Identification line ~297).

Distribution and Markdown alignment:

- [x] After editing the `shannon/` source, the deployed `.claude/skills/work-items/` copy is re-synced from `shannon/skills/work-items/` so source and deployed copies match (verified by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning no differences) — per development_guide § Distribution and EPIC-005's per-Task re-sync obligation. `.claude/` is gitignored, so this leaves no tracked diff; the Activity Log records the re-sync explicitly so Gate 3 can confirm it happened.
- [x] Edits follow development_guide § Code Style: British-English spelling preserved (e.g. "behaviour", "organisation"), Markdown heading hierarchy intact, no stale cross-references introduced (e.g. no reference to a `*-PENDING` status in any rewritten section). Per development_guide § Commit Cadence, **this Task produces no commit** — the commit lands only after Gate 3 review, batched with EPIC-005's APPROVED state at the end of the A → B → C sequence.

### Context

- **Parent Epic**: [EPIC-005 — V6 Vocabulary in Skills and CLAUDE.md Template](../epics/EPIC-005-v6-skills-and-claude-template.md) — this is TASK-005 in its § Plan; delivers EPIC-005 AC#4 (`PLAN-PENDING` structural rewrite) and AC#5 (§ Process: Implement step 5), and contributes the work-items half of AC#2 (gate-approval names directing party; self-approval-prevention names implementer) and AC#3 (§ Quality Gates consistent with conceptual_design and technical_design)
- **Parent Feature**: [FEAT-006 — Multi-Party Supervision](../features/FEAT-006-multi-party-supervision.md)
- **Sibling Tasks**: [TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md) (TASK-004, APPROVED 2026-05-23 — the V6 vocabulary precedent on `project-setup/skill.md`, `project-documentation/skill.md`, and the `CLAUDE.md` template; copy its house phrasing rather than invent new) and the verification Task (cross-file verification audit — the AC#1 `rg` survey, AC#2/AC#3 four-way consistency, AC#8 self-identification check; created only after TASK-004 and TASK-005 are APPROVED)
- **Canonical phrasing references** (lift, do not paraphrase):
  - `conceptual_design.md` v1.5 — *Directing Party* / *Implementer* glossary entries (lines 16–17); *Three Hard Gates* business rule ("explicit approval by the directing party"; "the implementer cannot approve its own work across a gate"); *Supervisor Distinct From Implementer* business rule; *Iterative Implementation Zone* glossary entry ("the implementer and the directing party iterate freely without gates. Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone")
  - `technical_design.md` v1.1 — **§ Cascading Operations: Batch Preparation, Individual Gates** (the load-bearing reference for AC#4 — implementer creates each child in DRAFT with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections, marked "prepared during EPIC-XXX planning, not yet reviewed"; surfaced when each child's gate command runs; no new sub-statuses) and § Gate Enforcement ("by convention, not technical control")
- **TASK-004 precedent (`project-documentation/skill.md` § Quality Gates rewrite)** — TASK-004's Gate 1 line read "Explicit approval by the directing party. The implementer cannot approve its own work." This is the house phrasing for the three Gate sentences and the self-approval-prevention sentence in `work-items/skill.md § Quality Gates`
- **Relevant guide**: `development_guide.md` § Distribution (the `.claude/skills/` re-sync obligation — deployed copies are gitignored), § Code Style (British-English Markdown conventions), § Git Workflow → Commit Cadence (commit only after Gate 3; pre-gate experimentation is fine, but no commit lands in this Task before Gate 3 approval)
- **Target file**: `shannon/skills/work-items/skill.md` (only) — 17 directing-party-sense `[Uu]sers?` occurrences (EPIC-005 estimated ~16; +1 discrepancy recorded in Overview), 3 `PENDING` locations (lines 190 / 200 / 282), three implementer-sense `AI` occurrences (lines 275, 282, 283) plus the dual line 227. EPIC-005 § Plan TASK-005 named "step 3 and § Cascading Operations" for the PENDING work; the survey confirms step 5 (line 200) is the third location and is in scope under AC#4 ("step 3 / step 5 / § Cascading Operations")
- **Leave-untouched anchors** (confirmed V6-clean at survey time; confirm-only, not re-edited): § Process: Elaborate → *Re-elaboration Branch* sub-section (TASK-001, lines 157–170, uses "directing party" at line 164); § Process: Create step 7 → Feature `(Partial)` index-suffix language (TASK-002, line 116, no `[Uu]sers?` occurrence); § Failure Modes → *Index out of sync* cross-reference (TASK-002, line 293, no `[Uu]sers?` occurrence). These are the recent TASK-001 / TASK-002 edits; isolating TASK-005 in its own Task keeps its diff small and avoids colliding with that recent work (EPIC-005 § Risks — "Editing `work-items/skill.md` again so soon")

---

## Plan

*Drafted at `/task-plan TASK-005` (Gate 2).*

### Approach

A single editing pass over **`shannon/skills/work-items/skill.md`** that handles two distinct kinds of edit, ordered carefully because they interact:

1. **Word-level vocabulary edits** — 17 directing-party-sense `[Uu]sers?` rewordings (lines 27, 28, 81, 104, 110, 120, 144, 194, 227, 271, 272, 273, 279, 283, 284, 290, 292) and four `AI` rewordings (lines 227, 275, 282, 283). Each is an in-line replacement that adds and removes no lines, so the file's line count stays invariant *across the entire word-level pass*. Occurrences are worked **top-to-bottom purely as a reading-order discipline** so none is missed — not as a line-number-safety measure. (The Edit tool matches on unique surrounding text, not line numbers; direction is moot for safety here.) The two `AI` tokens on lines 282 and 283 are *not* edited as standalone word-level changes — they are subsumed by the § Cascading Operations structural rewrite below.

2. **Structural rewrite** — three `PENDING` locations at lines 190, 200, and 282, all retired by technical_design v1.1 (commit `1943f3f`). These are paragraph-level rewrites that **will change line counts** (the *Batch Preparation, Individual Gates* replacement is likely longer than the present `PLAN-PENDING` prose). They go **last in the editing block**, after every line-pinned word-level edit has landed. If the structural rewrite were done first, the pinned line numbers (27, 28, 81, …, 271–273, 279, …) would shift and the survey map would become stale before the vocabulary pass began — the Edit tool would still match text, but the implementer would be flying blind. Doing it last means only the *final verification grep* sees the shifted numbers, and that grep counts matches rather than checking specific lines — so the shift is harmless. (Were a line-count-changing edit ever to precede a line-pinned one, the recovery would be a **bottom-to-top** sequence, as recorded in TASK-004's Plan § Approach; that case is avoided here by ordering.)

Editing is **per-occurrence semantic judgement, not find-and-replace** — each directing-party-sense "user" is reworded to name the *directing party*, or the role noun is dropped where the sentence reads naturally without it. Phrasing is copied from the V6-aligned document layer (conceptual_design v1.5 *Directing Party* / *Implementer* glossary, *Three Hard Gates*, *Supervisor Distinct From Implementer*, *Iterative Implementation Zone*; technical_design v1.1 § Gate Enforcement — "by convention, not technical control") and from TASK-004's already-landed house phrasing in `project-documentation/skill.md` ("Explicit approval by the directing party. The implementer cannot approve its own work."). Nothing is invented.

**The § Cascading Operations / § Process: Plan rewrite lifts technical_design § Cascading Operations: Batch Preparation, Individual Gates wording verbatim where the mechanics are named.** Specifically, the load-bearing phrases to lift (not paraphrase) are:

- "creates each child task in DRAFT, with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the task file (marked clearly as 'prepared during EPIC-XXX planning, not yet reviewed')" (technical_design step 3)
- "presents a summary of the parent plan and the prepared children to the directing party" (technical_design step 4)
- "When the directing party later runs `/task-elaborate TASK-XXX`, the subagent surfaces the prepared elaboration instead of starting fresh; same for `/task-plan TASK-XXX`. Each child still requires its own Gate 1 and Gate 2 approval" (technical_design step 5)
- The framing sentence: "the implementer does bulk preparation for the entire subtree — including drafting elaborations and plans for child work items — but each child still passes through its own gate individually. No new statuses are introduced."

After the rewrite, **§ Process: Plan step 3 ("Cascading Preparation")** should read: when planning an Epic, the planning subagent creates each child Task in DRAFT with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file (marked clearly as "prepared during EPIC-XXX planning, not yet reviewed"). **§ Process: Plan step 5 (Gate 2 Approval)** should read: on approval, child items prepared during the parent's planning **remain in DRAFT** until each child's own gate command surfaces and approves the prepared draft — no `PLAN-PENDING` sub-status, no new transitional state. **§ Cascading Operations** steps 1–4 should read: implementer plans the parent and writes the plan into its file → implementer identifies the child work items → implementer creates each child in DRAFT with prepared elaboration / plan drafts stashed inside the child file → implementer presents a summary of the parent plan and the prepared children to the directing party → when the directing party later runs each child's own gate command, the subagent surfaces the prepared content instead of starting fresh; each child still passes through Gate 1 and Gate 2 individually.

**Leave-untouched anchors** are confirmed (not re-edited) by `git diff` after the editing block: the *Re-elaboration Branch* sub-section (TASK-001, lines 157–170, pre-edit), the Feature `(Partial)` index-suffix language (TASK-002, line 116), and the *Index out of sync* cross-reference (TASK-002, line 293). Each is already V6-clean per the elaboration survey.

After the source edits, the gitignored `.claude/skills/work-items/` deployed copy is re-synced from `shannon/skills/work-items/` (development_guide § Distribution). Per development_guide § Commit Cadence, **no commit is made in this Task** — the commit lands after Gate 3 review, batched with EPIC-005's APPROVED state at the end of the A → B → C sequence.

### Steps

1. **Orientation baseline.** Re-run `rg -n "\b[Uu]sers?\b" shannon/skills/work-items/skill.md` and `rg -n "PENDING" shannon/skills/work-items/skill.md` and confirm the occurrence map matches the Requirements' survey exactly — 17 `[Uu]sers?` matches on the 17 pinned lines (27, 28, 81, 104, 110, 120, 144, 194, 227, 271, 272, 273, 279, 283, 284, 290, 292); 3 `PENDING` locations (190, 200, 282); 4 `AI` rewordings due (lines 227, 275, 282, 283 — `rg -n "\bAI\b"` confirms). This pins the editing map before any text changes. *(Supports every vocabulary and structural AC; no AC closed here.)*

2. **Word-level vocabulary pass — `[Uu]sers?` rewordings, top-to-bottom.** Reword each of the 17 directing-party-sense occurrences in this exact order: line 27 (§ When to Invoke — "The user asks to create…" → name the directing party), line 28 (§ When to Invoke — "The user asks to 'move TASK-042…'"), line 81 (§ Create step 1 — "the user supplied"), line 104 (§ Create step 5 — "from the user's invocation context"), line 110 (§ Create step 6 — "from the user's hint"), line 120 (§ Create step 8 — "Report creation to the user" → drop the role noun if the sentence reads naturally: e.g. "Report creation"), line 144 (§ Elaborate step 3 — "Show the user…"), line 194 (§ Plan step 4 — "Show the plan to the user…"), and line 227's "user" half (§ Implement step 5; the paired "AI" is handled in step 3 below as one combined rewrite per AC#5). Then the § Quality Gates / Cascading-context cluster: lines 271–273 (§ Quality Gates — the three "Explicit user approval" sentences become "Explicit approval by the directing party", verbatim from TASK-004's project-documentation precedent), line 279 (§ Cascading Operations preamble — "When the user invokes a verb on a parent…" → "When the directing party invokes…"), lines 283 and 284 (§ Cascading Operations — handled inside the structural rewrite at step 4, *not* edited as standalone word-level changes), line 290 (§ Failure Modes — "user runs `/task-plan`…"), line 292 (§ Failure Modes — "ask whether to create the parent"). Each rewording copies phrasing from TASK-004's house style on `project-documentation/skill.md` and from conceptual_design v1.5 / technical_design v1.1 — not invented. **Leave-untouched anchors are not touched**: nothing between the *Re-elaboration Branch* heading and the end of § Process: Elaborate (lines 157–170); nothing in the Feature `(Partial)` paragraph (line 116) or the *Index out of sync* cross-reference (line 293). All edits here are in-line word-level replacements, so line counts are invariant for the whole word-level pass. *(Closes AC#1, AC#2 — vocabulary sweep; lines 271–273 close the AC#7 / AC#8 § Quality Gates rewordings' word-level half; lines 283/284 are deferred to step 4.)*

3. **§ Process: Implement step 5 — AC#5 combined rewrite (line 227).** Rewrite the sentence "The user or AI may move the work item back to IMPLEMENTING from IMPLEMENTED or REVIEW. The iterative zone is gateless." to name the **directing party or implementer** as the two roles iterating, and to name the iterative zone in V6 terms — lifting conceptual_design *Iterative Implementation Zone* phrasing ("the implementer and the directing party iterate freely without gates. Approval to enter (Gate 2) and approval to exit (Gate 3) bracket the zone"). This is a single combined edit replacing both the "user" and "AI" role nouns on line 227 in one operation. *(Closes AC#11 — line 227 AC#5 — and the line-227 "AI" half of AC#5 / AC#6 in the implementer-sense group.)*

4. **§ Quality Gates line 275 — AC#7 / AC#8 self-approval-prevention rewrite.** Rewrite line 275 "AI never self-approves across a gate." so the **implementer** (not "AI") is named as the agent that cannot approve its own work, lifting the phrasing from conceptual_design *Supervisor Distinct From Implementer* ("the implementer cannot approve its own work") and TASK-004's house phrasing ("The implementer cannot approve its own work."), and consistent with technical_design v1.1 § Gate Enforcement ("by convention, not technical control"). The result is verbatim-aligned with `project-documentation/skill.md § Quality Gates` after TASK-004, closing the work-items-side preconditions for the verification Task's four-way audit. *(Closes AC#4 — line 275 implementer-sense rewording; AC#8 — § Quality Gates rewrite consistent with conceptual_design + technical_design; the work-items half of EPIC-005 AC#2 / AC#3.)*

5. **Structural rewrite — `PLAN-PENDING` removal at three locations (lines 190 / 200 / 282).** This is the line-count-changing edit and goes **last in the editing block**, after every line-pinned word-level edit has landed. All three rewrites lift technical_design v1.1 § Cascading Operations: Batch Preparation, Individual Gates verbatim where the mechanics are named — not loose paraphrase.

   - **§ Process: Plan step 3 ("Cascading Preparation"), line 190.** Rewrite the present sentence ("the planning subagent may draft tasks for the epic, marking them `PLAN-PENDING` (awaiting their own Gate 2 approval)") to: when planning an Epic, the planning subagent creates each child Task in DRAFT with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file, marked clearly as "prepared during EPIC-XXX planning, not yet reviewed". Lift the parenthetical and the prepared-draft phrasing verbatim from technical_design step 3.

   - **§ Process: Plan step 5 (Gate 2 Approval), line 200.** Rewrite the bullet ("Child items (if any) remain `PLAN-PENDING` until individually approved") to: on approval, child items prepared during the parent's planning **remain in DRAFT** until each child's own gate command surfaces and approves the prepared draft. Name the unified status lifecycle explicitly (no `PLAN-PENDING` sub-status, no new transitional state).

   - **§ Cascading Operations, line 282 onward (steps 1–4).** Rewrite the four-step numbered list so it describes the Batch Preparation, Individual Gates model in the verbatim mechanics named in technical_design § Cascading Operations: (1) the implementer plans the parent and writes the plan into its file; (2) the implementer identifies the child work items; (3) the implementer **creates each child in DRAFT, with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file (marked clearly as "prepared during EPIC-XXX planning, not yet reviewed")**; (4) the implementer **presents a summary of the parent plan and the prepared children to the directing party**; closing line: **when the directing party later runs each child's own gate command (e.g. `/task-elaborate TASK-XXX`, `/task-plan TASK-XXX`), the subagent surfaces the prepared content instead of starting fresh — each child still passes through Gate 1 and Gate 2 individually. No new sub-statuses are introduced.** This single rewrite simultaneously closes the structural AC, removes the lines-282/283 `AI` tokens (now "implementer"), and removes the line-279/283/284 `[Uu]sers?` tokens (now "directing party"). *(Closes AC#9 — three `PENDING` locations rewritten, zero `PENDING` tokens remain; AC#10 — Plan step 3 wording; AC#11 — Plan step 5 wording; AC#12 — § Cascading Operations steps 1–4; AC#13 — verbatim alignment with technical_design § Cascading Operations: Batch Preparation, Individual Gates; subsumes AC#5 / AC#6 — lines 282/283 implementer-sense rewordings.)*

6. **Re-sync the deployed `.claude/skills/work-items/` tree.** Re-copy `shannon/skills/work-items/` → `.claude/skills/work-items/` so the gitignored deployed copy matches source. Verify by `diff -rq shannon/skills/work-items/ .claude/skills/work-items/` returning no differences. *(Closes AC#18 — development_guide § Distribution re-sync obligation.)*

7. **Verify.** Run `rg "\b[Uu]sers?\b" shannon/skills/work-items/skill.md` and confirm **zero** matches (no preserve-sense occurrence exists in this file — survey confirmed at elaboration). Run `rg "PENDING" shannon/skills/work-items/skill.md` and confirm **zero** matches. Run `rg -n "Activating work-items skill"` and confirm exactly two unchanged pre-existing lines (line 8 header instruction; § Self-Identification line, now near the new end-of-file location after the structural rewrite's line-count change). Run `git diff -- shannon/skills/work-items/skill.md` and confirm: (a) no edits between the *Re-elaboration Branch* heading and the end of § Process: Elaborate (lines 157–170 pre-edit); (b) the Feature `(Partial)` paragraph (line 116 pre-edit) is unchanged; (c) the *Index out of sync* cross-reference (line 293 pre-edit) is unchanged; (d) no headings added or removed, no Process steps reordered, no sections moved, no templates list edited, no Work Item ↔ Document Relationships table edited. Run `git diff --name-only` and confirm the diff is confined to `shannon/skills/work-items/skill.md` and this TASK-005 file. (The Activity Log entry that records the re-sync will be appended at the implementation step.) *(Closes AC#3 — leave-untouched: Re-elaboration Branch; AC#14 — leave-untouched: `(Partial)` / *Index out of sync*; AC#15 — scope boundary; AC#16 — self-identification lines unchanged; AC#17 — no structural changes; AC#19 — verification grep results; AC#18 — re-sync diff check; the cross-type scope-boundary AC closed via `git diff --name-only`.)*

### Dependencies

- TASK-004 ([TASK-004](archive/TASK-004-v6-vocabulary-setup-documentation-template.md)) — APPROVED 2026-05-23. The vocabulary precedent set by TASK-004 (phrasing choices in the sibling skills, especially `project-documentation/skill.md § Quality Gates`: "Explicit approval by the directing party. The implementer cannot approve its own work.") is the house style this Task copies verbatim where the same concept is named.
- EPIC-005 PLANNED (met); conceptual_design v1.5 and technical_design v1.1 APPROVED and stable — both load-bearing references (the *Directing Party* / *Implementer* glossary, *Three Hard Gates*, *Supervisor Distinct From Implementer*, *Iterative Implementation Zone*, § Gate Enforcement, and § Cascading Operations: Batch Preparation, Individual Gates) are the canonical phrasing source.
- `.claude/skills/work-items/` deployed tree present and gitignored — re-sync in step 6 produces no tracked diff, which is expected.

### Risks

- **PLAN-PENDING rewrite drifting from technical_design § Cascading Operations.** AC#4 (EPIC-005) requires the rewrite to **match** technical_design's mechanics — not merely strip the `*-PENDING` token. A loose paraphrase that drops `PLAN-PENDING` but invents different mechanics (e.g. a "pre-approved" status, or a different child-creation order) would itself be new drift. *Mitigation*: step 5 names the exact technical_design phrases to lift verbatim (the prepared-draft phrasing on technical_design step 3, the summary-presentation phrasing on step 4, the surfacing phrasing on step 5, and the "No new statuses are introduced" framing); the Approach restates these so the implementer can quote them at the point of editing. the verification Task's AC#3 audit cross-checks the result against technical_design § Cascading Operations as the canonical reference.
- **Accidental edit to a leave-untouched anchor.** A stray edit inside the *Re-elaboration Branch* (lines 157–170), the Feature `(Partial)` paragraph (line 116), or the *Index out of sync* cross-reference (line 293) would silently undo recent TASK-001 / TASK-002 work. *Mitigation*: step 2 enumerates the leave-untouched line ranges and instructs leaving them byte-for-byte intact; step 7 runs `git diff` and confirms no edits land in any of the three named ranges. TASK-002's lesson (file-count caps obscure intent) is honoured by phrasing this as a content-range check, not a line-count check.
- **Structural rewrite shifting line numbers before vocabulary edits land.** Doing the § Cascading Operations / § Process: Plan rewrite first would shift every line number pinned in the Requirements (27, 28, 81, …, 271–273, 279, …), staling the survey map before the word-level pass began. *Mitigation*: step ordering — vocabulary edits (steps 2–4) run *before* the structural rewrite (step 5), so the pinned line numbers hold across the entire word-level pass. Only the final verification grep (step 7) sees the shifted line numbers, and that grep counts matches rather than checking specific lines — so the shift is harmless. The bottom-to-top recovery pattern (TASK-004's Plan § Approach) is noted but not needed here.
- **Re-sync omission.** Editing `shannon/skills/work-items/skill.md` source but forgetting to re-copy to `.claude/skills/work-items/` leaves the running framework stale. *Mitigation*: step 6 is an explicit, named step with a `diff -rq` verification command; the Activity Log records the re-sync explicitly so Gate 3 can confirm it happened (since `.claude/` is gitignored and produces no tracked diff). the verification Task can spot a divergence if it runs its audit against the deployed copies.
- **Cross-file phrasing drift from TASK-004's precedent.** Inventing new phrasing for the same gate concept already named in `project-documentation/skill.md § Quality Gates` would defeat AC#3's cross-file consistency goal and force the verification Task to surface a defect. *Mitigation*: steps 2 and 4 explicitly instruct copying TASK-004's verbatim phrases ("Explicit approval by the directing party"; "The implementer cannot approve its own work.") rather than rewording them; conceptual_design v1.5 / technical_design v1.1 are named as the canonical source where TASK-004 and this Task converge.
- **Accidental structural change outside the named rewrites.** A stray edit adding/removing a heading, reordering Process steps, or moving a section would violate AC#17. *Mitigation*: edits are scoped to (a) the line-pinned word-level occurrences and (b) the three named structural rewrites; step 7 runs a diff review confirming no headings/sections changed and `git diff --name-only` is confined to the two intended files.
- **`git status` appearing empty after re-sync.** Because `.claude/` is gitignored, the re-sync leaves no tracked diff — a reviewer could mistake this for "re-sync not done". *Mitigation*: step 6 verifies the deployed copy by content (`diff -rq`); the Activity Log will record the re-sync explicitly so Gate 3 review can confirm it happened. Per development_guide § Commit Cadence, **no commit is made in this Task** — the commit lands after Gate 3 approval, batched at the end of EPIC-005's A → B → C sequence.

---

## Implementation Notes

Implemented 2026-05-23 via `/task-implement TASK-005`, following the Gate 2 seven-step plan. 18 edits to `shannon/skills/work-items/skill.md` — 15 word-level vocabulary rewordings + 3 structural rewrites — plus the `.claude/` re-sync.

**Word-level vocabulary** (15 edits, lines 27, 28, 81, 104, 110, 120, 144, 194, 227, 271, 272, 273, 275, 290, 292): 17 directing-party-sense `[Uu]sers?` occurrences reworded to *directing party*; the standalone "AI never self-approves across a gate." → "The implementer cannot approve its own work across a gate." — the verbatim phrasing TASK-004 used in `project-documentation/skill.md` for cross-file consistency. The three § Quality Gates bullets (271–273) now read "Explicit approval by the directing party" — verbatim with TASK-004. Line 227 (§ Process: Implement step 5) "The user or AI may move…" → "The directing party or implementer may move…" naming the iterative zone in V6 terms.

**Structural `PLAN-PENDING` rewrite** at three locations:

- **§ Process: Plan step 3 (Cascading Preparation)** — rewritten to "prepared elaboration draft / prepared plan draft stashed in the relevant sections of the task file" + "No new sub-statuses are introduced (see § Cascading Operations)". No `PLAN-PENDING` token remains.
- **§ Process: Plan step 5 Gate 2 on-approval bullet** — "remain `PLAN-PENDING` until individually approved" → "remain in DRAFT with their prepared drafts stashed until the directing party runs each child's gate command individually".
- **§ Cascading Operations (entire section)** — rewritten to lift technical_design v1.1 § Cascading Operations: *Batch Preparation, Individual Gates* verbatim where the mechanics are named: "the implementer does bulk preparation for the entire subtree", "creates each child in DRAFT, with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file (marked clearly as 'prepared during EPIC-XXX planning, not yet reviewed')", "When the directing party later runs `/[type]-elaborate [ID]`…", "No new statuses are introduced". The phrasing is a verbatim lift where mechanics are named, not paraphrased.

**Leave-untouched anchors** — the TASK-001 *Re-elaboration Branch* sub-section (lines 157–170 pre-edit; line numbers will have shifted slightly post-structural-rewrite) and the TASK-002 `(Partial)` index-suffix language (§ Process: Create step 7; § Failure Modes — *Index out of sync*) are confirmed untouched in the diff. The diff shows additions/deletions only inside the named edit targets.

**Verification** (step 7): `grep -nE "\b[Uu]sers?\b" shannon/skills/work-items/skill.md` → ZERO matches; `grep -nE "PENDING" shannon/skills/work-items/skill.md` → ZERO matches; self-identification lines unchanged (line 8 and the trailing § Self-Identification line); `git diff --name-only` confined to `shannon/skills/work-items/skill.md` and this Task file — no other skill, no template, no command, no other index modified.

**Deployed-copy re-sync** (step 6): `cp -R shannon/skills/work-items/ .claude/skills/work-items/` executed; `diff -rq` confirmed the deployed copy matches source. `.claude/` is gitignored so this leaves no tracked diff — recorded here so Gate 3 can confirm independently.

### Deviations from Plan

- **Fifth `AI` occurrence subsumed by the structural rewrite.** The elaboration's survey listed 4 `AI` occurrences (lines 227, 275, 282, 283); the actual file also had `AI` on line 281 ("AI does the heavy work") inside § Cascading Operations. That fifth occurrence was already within the section being structurally rewritten and was absorbed by the rewrite — no separate edit was needed. The Plan's substance held; only the count was incidentally one short. Noted for Gate 3.

### Gotchas

- **Bullet structure of § Quality Gates differs from TASK-004's house style.** TASK-004 combined "Explicit approval by the directing party" and "The implementer cannot approve its own work" into a single Gate-1 bullet in `project-documentation/skill.md`. `work-items/skill.md` has three Gate bullets (Gate 1, 2, 3) plus a separate standalone sentence below them. Preserved this structure (no structural change per AC#15) and used the same verbatim phrases — three "Explicit approval by the directing party" bullets and the standalone "The implementer cannot approve its own work across a gate." sentence below. Cross-file consistency holds at the phrase level, not the bullet structure.

### Documents Updated

- `shannon/skills/work-items/skill.md` — 18 edits: 15 word-level vocabulary rewordings (incorporating AC#5 line 227 and the § Quality Gates work) + 3 structural rewrites (§ Process: Plan step 3, § Process: Plan step 5 on-approval bullet, § Cascading Operations entire section).
- `.claude/skills/work-items/` — re-synced from `shannon/` source via `cp -R`; `diff -rq` confirmed identical (gitignored, no tracked diff).

---

## Review

**Gate 3 — APPROVED 2026-05-23.** A verification subagent audited all Acceptance Criteria against the post-edit state of `work-items/skill.md`, focused on the structural `PLAN-PENDING` rewrite's verbatim alignment with technical_design v1.1 § Cascading Operations (EPIC-005's named rewrite-drift risk) and the cross-file Gate-1 phrasing consistency with TASK-004's `project-documentation/skill.md`. Verdict: *ready for Gate 3 approval* — all ACs MET, no blocking findings.

### Verification

- [x] All acceptance criteria met
- [x] Changes follow development_guide.md
- [x] Relevant documents updated
- [x] Knowledge captured where useful

### Review Notes

**Mechanical gates — all clean.** `grep [Uu]sers?` → 0; `grep PENDING` → 0; `grep \bAI\b` → 0 (every directing-party-sense and implementer-sense token removed); self-identification lines unchanged at lines 8 and 298; `git diff --name-only` confined to `work-items/skill.md` plus standard lifecycle bookkeeping (TASK-005 file, EPIC-005 status line, `task_index.md`); `.claude/skills/work-items/` deployed copy verified identical to source by `diff -rq`.

**Structural § Cascading Operations rewrite — verbatim alignment confirmed.** The high-risk item EPIC-005's § Risks explicitly named is satisfied: every load-bearing technical_design phrase appears in the rewrite intact — "the implementer does bulk preparation for the entire subtree", "creates each child in DRAFT, with a *prepared elaboration draft* and a *prepared plan draft* stored in the relevant sections of the child file (marked clearly as 'prepared during EPIC-XXX planning, not yet reviewed')", "Each child still requires its own Gate 1 and Gate 2 approval", "No new statuses are introduced". The one deliberate divergence — "child task / task file" → "child / child file" — is a sound generalisation to cover the skill's Feature→Epic→Task scope; not paraphrase drift.

**Leave-untouched anchors intact** — TASK-001 Re-elaboration Branch sub-section (pre-edit lines 157–170), TASK-002 `(Partial)` index-suffix language at § Process: Create step 7, and the *Index out of sync* main sentence at § Failure Modes are byte-for-byte unchanged in the diff. The adjacent "DRAFT mandated doc as context" bullet was reworded — an in-scope `[Uu]sers?` vocabulary edit, not the leave-untouched anchor.

**Cross-file Gate-1 phrasing consistency** with `project-documentation/skill.md` — both files now use the verbatim TASK-004 house phrases ("Explicit approval by the directing party"; "The implementer cannot approve its own work…"). The bullet-structure difference (work-items has three Gate bullets + a standalone implementer sentence; project-documentation has one combined Gate-1 bullet) is preserved per the no-structural-changes AC and honestly disclosed in Implementation Notes § Gotchas — phrase-level consistency is the right target; bullet structure would be a separate change. The verification Task (TASK-006) audits this cross-file equivalence at its own Gate 3.

**Deviation sound** — Implementation Notes records that the elaboration's `AI` survey listed 4 occurrences (lines 227, 275, 282, 283) but pre-edit line 281 carried a fifth ("AI does the heavy work"). The fifth sat inside the section being structurally rewritten and was absorbed by the rewrite; `grep \bAI\b` confirms no orphan remains. Plan substance held; only the count was incidentally one short. Disclosed honestly, not scope creep.

**One minor Gap (non-blocking)** — the line 227 reword names the V6 roles ("directing party or implementer may move…") but does not lift conceptual_design's full *Iterative Implementation Zone* "bracket the zone" phrasing. The retained "The iterative zone is gateless" wording is V6-compatible and conveys the concept; arguably met under the AC's loose "in V6 terms" wording. Candidate nit for the verification Task's cross-file audit but not a blocker here.

**Count note** — Earlier session prose called the checklist "19 ACs in seven groups"; the actual file enumerates 18 bullets across the seven groups. Minor bookkeeping; verdict unaffected.

**Alignment.** Delivers EPIC-005 AC#4 (`PLAN-PENDING` removal) and AC#5 (Implement step 5 in V6 terms); contributes the work-items half of AC#2 / AC#3 (gate-approval names directing party; § Quality Gates consistent with conceptual_design + technical_design). No drift against EPIC-005 or the V6 document layer.

---

## Activity Log

- **2026-05-23** — APPROVED via Gate 3. Directing party approved after verification — all Acceptance Criteria MET (the checklist enumerates 18 bullets across seven groups; earlier session prose said 19 — minor bookkeeping, verdict unaffected). Mechanical gates clean: `grep [Uu]sers?` / `grep PENDING` / `grep \bAI\b` all return zero; self-id lines intact; diff confined to `work-items/skill.md` plus standard bookkeeping. Structural § Cascading Operations rewrite tracks technical_design v1.1 verbatim where mechanics are named (EPIC-005's named rewrite-drift risk not realised). Leave-untouched anchors (TASK-001 Re-elaboration Branch; TASK-002 `(Partial)` language; *Index out of sync*) confirmed byte-for-byte intact. Cross-file Gate-1 phrasing matches `project-documentation/skill.md` (TASK-004 house phrases) at the phrase level. Recorded Deviation (fifth `AI` subsumed by structural rewrite) sound. One minor Gap noted (line 227 didn't lift the full *Iterative Implementation Zone* phrasing) — non-blocking nit for the verification Task's cross-file audit. Delivers EPIC-005 AC#4, AC#5, and the work-items half of AC#2 / AC#3. Status REVIEW → APPROVED; file archived to `docs/tasks/archive/`. Only the verification Task remains in EPIC-005.
- **2026-05-23** — REVIEW. Status IMPLEMENTED → REVIEW; Gate 3 verification subagent spawned to audit all 19 Acceptance Criteria against the post-edit state of `work-items/skill.md`, focused on the structural `PLAN-PENDING` rewrite's verbatim alignment with technical_design v1.1 § Cascading Operations (EPIC-005's named rewrite-drift risk) and the cross-file Gate-1 phrasing consistency with TASK-004's `project-documentation/skill.md` precedent.
- **2026-05-23** — IMPLEMENTED. All 7 plan steps executed against `shannon/skills/work-items/skill.md`. 18 edits total: 15 word-level vocabulary rewordings (lines 27, 28, 81, 104, 110, 120, 144, 194, 227, 271, 272, 273, 275, 290, 292) — directing-party / implementer per conceptual_design v1.5 and verbatim-aligned with TASK-004's house phrasing in `project-documentation/skill.md`; plus 3 structural rewrites at § Process: Plan step 3 (Cascading Preparation), § Process: Plan step 5 (Gate 2 on-approval bullet), and § Cascading Operations (entire section) — all lifting technical_design v1.1 § Cascading Operations: *Batch Preparation, Individual Gates* verbatim where mechanics are named. Leave-untouched anchors (TASK-001 Re-elaboration Branch; TASK-002 `(Partial)` language) confirmed untouched in the diff. Verification: `grep [Uu]sers?` → ZERO, `grep PENDING` → ZERO, self-id lines intact, `git diff` confined to this file and the TASK file. `.claude/` deployed copy re-synced via `cp -R` and verified by `diff -rq`. One survey omission caught at implementation: a fifth `AI` on line 281 inside § Cascading Operations, absorbed by the structural rewrite — no separate edit needed; recorded in Implementation Notes § Deviations. Ready for Gate 3.
- **2026-05-23** — IMPLEMENTING via `/task-implement TASK-005`. Status PLANNED → IMPLEMENTING; executing the 7-step plan (vocab top-to-bottom → § Implement step 5 → § Quality Gates line 275 → structural PLAN-PENDING rewrite → `.claude/` re-sync → verify).
- **2026-05-23** — PLANNED via Gate 2. Planning subagent drafted a 7-step plan: orientation → word-level vocab top-to-bottom (line-count invariant) → § Process: Implement step 5 (line 227) → § Quality Gates line 275 → **structural `PLAN-PENDING` rewrite last** (lines 190/200/282; lifts technical_design § Cascading Operations verbatim) → `.claude/` re-sync → verify. Order is deliberate: word-level edits first preserve the line-pinned map for the whole vocab phase; the structural rewrite goes last so its line-count shifts cannot invalidate any pending vocab edit. The bottom-to-top recovery pattern is documented but unused here. The exact technical_design § Cascading Operations phrases to lift are quoted in the Plan for the implementer ("prepared elaboration draft / prepared plan draft"; "no new statuses are introduced"). All 19 Acceptance Criteria mapped to steps; 7 risks with mitigations. Plan approved without modification.
- **2026-05-23** — ELABORATED via Gate 1. Elaboration subagent surveyed `work-items/skill.md` by `rg` and drafted 19 Acceptance Criteria in seven groups (vocabulary sweep, implementer-sense rewordings, § Quality Gates rewrite, structural `PLAN-PENDING` rewrite, § Process: Implement step 5, leave-untouched anchors, scope boundary + verification + re-sync). Survey found 17 directing-party-sense `[Uu]sers?` occurrences (EPIC-005 estimated ~16; +1 recorded, matches TASK-004 precedent), 4 `AI` occurrences to become *implementer* (lines 227, 275, 282, 283), and **3** `PENDING` locations (lines 190, 200, 282 — EPIC-005 named only step 3 + § Cascading Operations; step 5 line 200 was the third). The TASK-001 Re-elaboration Branch (lines 157–170) and TASK-002 `(Partial)` index-suffix anchors confirmed V6-clean at survey — leave-untouched, not re-edited. AC#4 mandates lifting technical_design § Cascading Operations' wording verbatim where the mechanics are named, mitigating EPIC-005's "PLAN-PENDING rewrite drift" risk. Alignment: all Strength; no occurrence needs directing-party judgement, no drift, no internal contradiction. Directing party approved the requirements without modification.
- **2026-05-23** — DRAFT: Task created via `/task-create` under EPIC-005 — TASK-005 of the three-Task plan. Created individually after TASK-004's approval, per EPIC-005's § Plan recommendation (children created individually rather than pre-stashed as DRAFT stubs). Initial intent captured in the Overview and Context; Acceptance Criteria and Plan to be drafted at `/task-elaborate` and `/task-plan`.
