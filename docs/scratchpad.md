# Scratchpad

Capture point for ideas, half-formed work, and unprocessed TODOs that don't yet belong to a work item.

**Process periodically**: promote items to formal work items (Feature, Epic, Task, Spike), move durable findings to knowledge notes, or drop items that no longer matter. The scratchpad is not a backlog — it is pre-workflow. Items that already have a clear home (e.g. a Task under an existing Epic) should be captured there, not here.

**Format**: one line per item. Optional tags. No IDs. No status flow. The whole point is low-friction capture.

---

## Items

- **V6 propagation Epic under FEAT-006** — Now homed in [FEAT-006](./features/FEAT-006-multi-party-supervision.md) as the candidate next Epic. All six mandated documents are V6-aligned. Remaining surface: 3 skill definitions (`shannon/skills/*/skill.md`), the CLAUDE.md template (`shannon/skills/project-setup/templates/CLAUDE.md`), and 22 command files (`shannon/commands/*.md`). Promote to an Epic when ready to do the work #framework #v6 #feat-006
- **Workflow: Alignment Drift Detection (in-review only)** — Vision commits to "drift surfaces in days, not refactors." The new *Re-reviewing an APPROVED Mandated Document* workflow (conceptual_design v1.3) covers drift detection *during* a review via the alignment subagent. What remains open: drift detection *outside* a review, e.g. proactive periodic alignment checks without an explicit trigger. May not be needed; revisit if drift starts slipping through. Originally surfaced as G1 #framework #conceptual-design
- **Business rule: Orphan Tasks** — The Task entity allows `Parent = orphan`, but no business rule sanctions this case. Either add a rule ("Tasks May Be Orphan When the Work Has No Parent Epic") or remove the orphan affordance. Surfaced as C4 #conceptual-design
- **Rationale: Why Guides aren't updated by work items** — The Work Items Consume Guides rule and Higher Work Items May Update Mid-Level Docs rule together imply an asymmetry (Technical Design is updatable; Guides are not). One-line rationale missing. Surfaced as C5 #conceptual-design
- **`/what-next` command** — A command that surfaces the next obvious action for the project based on current state: highest-priority DRAFT docs awaiting review, PLANNED tasks ready to implement, IMPLEMENTED items awaiting Gate 3, scratchpad items that have aged, etc. Strong UX win for solo developers who return to a project after a break. Probably a small feature in its own right #framework #ux #commands
- **Analyse and leverage new Claude Code features** — Survey emerging Claude Code capabilities (e.g. `preCompact` hook for state preservation before context compaction, also any new subagent / skill / settings primitives) and decide which Shannon should adopt. `preCompact` specifically could let Shannon write a session summary into a knowledge note before compaction, preserving in-flight context. Probably a Spike (investigation + recommendation) rather than a Feature directly #framework #claude-code #spike-candidate
- **System Architecture diagram update in technical_design.md** — Current diagram shows Commands → Skills → Subagents but doesn't visualise the directing party / implementer split that V6 makes architecturally important. Would improve clarity but not a blocker. Defer; revisit when a substantive technical_design change is happening anyway #technical-design #v6 #nice-to-have
- **Index update algorithm semantics** — technology_stack §Performance commits to "O(1) appends or in-place edits"; technical_design mentions index updates as a skill responsibility but doesn't describe the algorithm. Worth specifying when implementation work touches the indexes #technical-design
- **Gate Enforcement visibility UX** — When a supervising agent (not a human) approves at a gate, should the framework surface the role distinction in the announcement (e.g. "Approving as the directing party; the implementer is X")? Surfaced during ux_guide Gate 1 review as deferred — too speculative until multi-agent configurations are actually exercised. Revisit when the first multi-agent project ships #ux-guide #v6 #speculative
- **Documentation upkeep ratio surfacing** — Vision § Measurable Targets commits to "<10% of session time in /document-* commands." Currently the directing party has no aggregate metric visible. UX guide deferred surfacing this until instrumentation exists in the project-documentation skill (also captured in development_guide § Instrumentation). Chicken-and-egg: needs both the instrumentation and a UX pattern for surfacing the aggregate #framework #ux-guide #instrumentation
- **Voice differing for human vs agent directing party** — When the directing party is a supervising agent rather than a human, does the implementer's prose style change? Vision is silent; ux_guide deferred as premature speculation. Revisit when agent-as-directing-party becomes real #ux-guide #v6 #speculative
- **Inter-work-item dependency as a first-class concept** — Surfaced during EPIC-006 planning. Tasks have an informal `Dependencies` section, but the framework lifecycle doesn't formally express "this work item is blocked by another." When EPIC-006's recursive-dogfood Task was considered for pre-creation, it would have sat in an exists-but-cannot-progress state — functionally re-inventing the `*-PENDING` sub-status pattern that technical_design v1.1 retired. We sidestepped via forward-declaration in EPIC-006's Plan, but the structural gap remains: the framework has no canonical answer for "Task C can be elaborated and planned but cannot start implementation until Tasks A and B are APPROVED." Worth revisiting if the pattern recurs #framework #work-items #lifecycle
- **Verbatim-phrasing grep tests require plain-prose substring landings** — Markdown formatting (backticks, italic `*`, bold `**`) that interposes between substring chars breaks substring matching. Caught during TASK-009 Step 7 when `TASK-A, TASK-B, TASK-C` initially landed with backticks around each letter and failed the verbatim-grep test; resolved by switching to plain-prose `TASK-A, TASK-B, TASK-C` matching `conceptual_design.md:58`'s landed form. The plain-prose form is the safe default for substrings that participate in verifiable tests. Candidate promotion target: a `development_guide` convention or a `work-items` skill prompt at plan time. Surfaced 2026-05-25 during TASK-009 implementation. #framework #conventions #verification
- **Re-elaborating older Feature when newer Feature implements part of its scope — trigger not perfectly fit by current taxonomy** — `conceptual_design.md` § Re-elaborating a Work Item names three trigger categories: upstream cascade, downstream gap, framework evolution. The case "sibling Feature created that implements part of my scope" (concretely: FEAT-006 Multi-Party Supervision needs re-elaboration now that FEAT-009 Supervisor exists as the implementing Feature) is closer to *downstream gap* than the other two — but not a perfect fit. The downstream-gap framing assumes the gap was a child surfacing missing parent aspiration; here the gap is a sibling crystallising part of the parent's scope. Candidate `conceptual_design.md` § Re-elaborating a Work Item Triggers refinement (new explicit trigger category, e.g. *sibling-Feature implementing-part-of-scope*) or a future follow-up Epic under FEAT-008 (development discipline). Surfaced 2026-05-29 during FEAT-009 elaboration. #framework #conceptual-design #re-elaboration #feat-008
- **Zero-findings supervisor-report presentation pattern not codified** — `ux_guide.md` v1.2 § Interaction Patterns → *Supervisor Report Presentation* codifies the hybrid header + narrative shape for reports surfacing findings, but does not name the **zero-findings degenerate case**: how does a clean cadence run land so the directing party can distinguish "supervisor checked everything and found nothing" from "supervisor silently failed to check"? FEAT-009's *Honest Failure Modes — Supervisor Finds Nothing* User Story names the need; the canonical UX pattern is open. Candidate `ux_guide.md` § Interaction Patterns refinement, or Phase 2 (EPIC-009) Epic-internal scope. Revisit at EPIC-009 elaboration. Surfaced 2026-05-29 during FEAT-009 elaboration. Now also landed as an AC in EPIC-010 (AC#5 — zero-findings degenerate-case presentation pattern, surfaces during EPIC-010 elaboration). #framework #ux-guide #supervisor #feat-009
- **Cascading Preparation at Feature-plan stage vs Epic-plan stage — common mechanism, no single canonical name** — `technical_design.md` v1.2 § Key Algorithms → *Cascading Operations* names the `/epic-plan` → child Task case (prepared Task drafts surface at `/task-elaborate` / `/task-plan`); the `work-items` skill § Process: Plan step 3 *Cascading Preparation (for Epics)* names the symmetric Feature-plan → child Epic case (prepared Epic drafts surface at `/epic-elaborate` / `/epic-plan`). Both cases share the same shape — a parent's planning step pre-fills a child's stub with elaboration and plan drafts marked "*Prepared during [parent] planning, not yet reviewed*" — but the canonical workflow is not yet captured in `conceptual_design.md` § Key Workflows as a single named pattern sibling to *Re-elaborating a Work Item*. Candidate route: new canonical workflow *Cascading Preparation* in `conceptual_design.md` § Key Workflows, with the two cases (Feature-plan and Epic-plan) as named instances of one general pattern. Surfaced 2026-05-30 during FEAT-009 planning. #framework #conceptual-design #work-items #feat-008
- **Forward-deferral pattern for Phase 5+ items named at Feature § Plan but not instantiated as Epic stubs** — Used at FEAT-009 § Plan: Phase 5+ items (Cross-Project supervisor, Managed Agents, MCP exposure of Shannon state) are named in a *Future Epics* paragraph with explicit deferral rationale rather than instantiated as DRAFT Epic stubs. The pattern is sound and reusable — it honours the "one Epic at a time" discipline while preserving forward visibility — but no canonical convention exists for it in `conceptual_design.md` § Domain Model → *Feature* or in the `work-items` skill § Process: Plan. Candidate route: `conceptual_design.md` § Domain Model → *Feature* attribute extension naming the *Future Epics* paragraph as a canonical Feature-plan output (sibling to the Epics list), or `work-items` skill § Process: Plan addition codifying when to use forward-deferral vs Epic-stub creation. Surfaced 2026-05-30 during FEAT-009 planning. #framework #conceptual-design #work-items #feat-008
- **Supervisor audit-log convention not codified at framework level** — EPIC-009 AC#5 names the PostToolUse hook's audit-log location concretely (under `./.claude/skills/shannon-supervisor/`, gitignored) but `development_guide.md` v1.4 § Supervisor Report Files codifies report + state-file commit status without naming the audit log at all. The question "where does the supervisor's audit log live and is it committed" is a framework-general concern, not an EPIC-009-specific one — any project adopting the supervisor skill needs the same convention. Candidate route: `development_guide.md` § Supervisor Report Files extension naming the audit log alongside reports + state file (committed status: gitignored, per the operational-telemetry-not-durable-knowledge framing); alternatively `technical_design.md` § Data Model addition next to *Supervisor Report Files* / *Supervisor Configuration* / *Cadence State*. Surfaced 2026-05-30 during EPIC-009 elaboration. **Update (2026-06-27, TASK-017 Gate 1):** the operational pattern is now landing — the audit log lives at `./.claude/skills/shannon-supervisor/audit.log` (runtime location), written append-only by the PostToolUse hook only when `SHANNON_SUPERVISOR_SCOPE` is active. In Shannon-self it is **already gitignored** by the blanket `*.claude/` rule, so TASK-017 adds no `.gitignore` line. The open framework-general piece is the **shippable targeted-ignore**: a project that *tracks* `.claude/skills/` (the general-codebase norm, per the DECISION 2026-06-27 clarification) must exclude the audit log specifically — so `/shannon-setup` should add a targeted ignore (e.g. `**/shannon-supervisor/audit.log`) at deploy time, and `development_guide.md` § Supervisor Report Files should name the audit log alongside reports + state file with its committed status (gitignored, operational telemetry). This belongs to the deferred `/shannon-setup` deploy Epic. #framework #development-guide #technical-design #supervisor #feat-009
- **Portability discipline as a verifiable convention for skill-creating Epics** — EPIC-009 AC#1 was extended at Gate 1 to require project-relative paths (`./docs/`, `./.claude/`) inside `SKILL.md`, checker definitions, and templates — converting FEAT-009 § Plan Risk *Supervisor skill portability not exercised until first non-Shannon-self deployment* into a Gate-3-checkable item rather than a paragraph commitment. The same discipline applies whenever a Shannon skill is added or amended: any new skill in `./.claude/skills/` should use project-relative paths so the skill is deployable to a non-host-project by directory copy. The convention is currently an EPIC-009-internal AC; it could be a framework-wide convention. Candidate route: `development_guide.md` § Code Style → Patterns to Follow new bullet sibling to *Source-of-truth body before derived artefacts* (e.g. *Project-relative paths in skills*); or `shannon/skills/work-items/skill.md` § Process: Plan addition flagging this as a verifiable AC pattern for skill-creating Epics. Revisit after EPIC-009 implementation surfaces lived-in evidence. Surfaced 2026-05-30 during EPIC-009 elaboration. #framework #development-guide #work-items #portability #feat-009
- **`SKILL.md` filename casing inconsistency in `technical_design.md` v1.2** — Surfaced 2026-05-30 during TASK-011 elaboration. `technical_design.md` v1.2 line 53 (general § Skills subsection) names the skill manifest file as lowercase `skill.md`; v1.2 line 80 (§ System Architecture → *Supervisor* → *Skill directory layout*) names it as uppercase `SKILL.md`. The existing three Shannon skills (`work-items`, `project-documentation`, `project-setup`) all use lowercase `skill.md` in both shipping source (`./shannon/skills/*/skill.md`) and deployed copy (`./.claude/skills/*/skill.md`). The Claude Code SDK may use uppercase `SKILL.md` as the canonical filename for skill manifests with frontmatter — making the supervisor's uppercase form intentional differentiation rather than accidental drift — but the doc itself is inconsistent. Either both should converge to one casing (with sibling skills migrating if uppercase is canonical) or the doc should explicitly note which is current and which is legacy. Candidate route: `technical_design.md` § Skills clarification at next cascade revision, or a one-line note in `technical_design.md` § System Architecture → *Supervisor* → *Skill directory layout* explaining the differentiation. Not a TASK-011 defect — TASK-011 follows the supervisor-specific citation, which is the closer-scoped authority. #framework #technical-design #skills #naming #feat-009
- **Shipping-source mirror convention for new skills not codified at framework level** — Surfaced 2026-05-30 during TASK-011 elaboration. Shannon's existing three skills (`work-items`, `project-documentation`, `project-setup`) are mirrored in both `./shannon/skills/` (shipping source distributed by `/shannon-setup`) AND `./.claude/skills/` (deployed copy). The supervisor skill is being built directly into `./.claude/skills/shannon-supervisor/` only — but FEAT-009 § Ideal State *Portability to non-Shannon-self projects* requires the shipping-source mirror to exist before `/shannon-setup` can deploy the skill to non-Shannon-self projects. The framework lacks an explicit convention for *when* a new skill must be mirrored to `./shannon/skills/` (and *who* does the mirroring — the implementing Task, a separate `/shannon-setup`-update Epic, or a framework-side maintenance task). EPIC-009 has deferred the supervisor-skill mirror to a future `/shannon-setup`-update Epic (G-1 decision at TASK-011 Gate 1); that future Epic likely bundles Supervisor Report subtype handling for `knowledge_index.md` and per-project gate-authority configuration into one coherent deployment Epic. Candidate framework-general route: `development_guide.md` § Code Style → Patterns to Follow extension naming the deployed-source mirror discipline; or `shannon/skills/project-setup/skill.md` extension to detect and warn on missing shipping-source mirrors at `/shannon-setup` time. **DECISION (2026-06-27, TASK-015 Gate 1):** the deferral was reconsidered and partly reversed. Directing party reviewed the consequence — that deferring the mirror left **all** of EPIC-009's authored deliverables (SKILL.md + three checkers) living only in the gitignored `./.claude/` deployed copy, with **no tracked home** (recoverable from Task specs, but lost on `git clean -fdx` and absent from a fresh clone), inconsistent with the other three skills whose source of truth is the tracked `./shannon/`. Resolved: **author the supervisor skill in tracked `./shannon/skills/shannon-supervisor/` going forward, deploy to `./.claude/` as the active copy** (same source/deploy split as the other three skills). The four APPROVED Tasks' deliverables are backfilled into `./shannon/` via a small corrective Task (TASK-019). Only the **`/shannon-setup` deploy-flow update** (the installer changes + Supervisor Report subtype + gate-authority config) stays deferred to the future deployment Epic — version-controlling the source and updating the installer are separable. **Clarification worth codifying:** the `./.claude/` gitignore (`.gitignore` lines 2–3) is **Shannon-self-specific** — it is reasonable here *only because* `./shannon/` is the real tracked source and `./.claude/` is a deployed copy. It is **not** a general-codebase truth: most Claude Code projects *commit* `.claude/skills/` and `.claude/commands/` to share them with the team and gitignore only personal files (`settings.local.json`). The verification discipline "`.claude/` is gitignored, so `ls` is the mechanism" was therefore Shannon-self scaffolding mistaken for a portable rule; with source authored in tracked `./shannon/`, scope-bounded-edit verification reverts to normal `git diff`. #framework #development-guide #project-setup #portability #distribution #feat-009
- **Cross-Task editing-order discipline when a downstream Task extends an upstream Task's source-of-truth body** — Surfaced 2026-05-30 during EPIC-009 planning. The Shannon editing-order convention at `development_guide.md` v1.4 § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts* covers editing-order **within a single Task** (body before derived artefacts in one diff). EPIC-009's Task chain has TASK-015 (Report writer + index updater) extending TASK-011's (Skeleton) `SKILL.md` source-of-truth body, and TASK-017 (PostToolUse hook) extending the `settings.json` first touched by TASK-016 (PreToolUse hook). The **cross-Task case** — when a downstream Task's source-of-truth body is an upstream Task's source-of-truth body — is not codified anywhere. EPIC-009 addresses this Task-locally inside the relevant stubs (TASK-015 Approach + Risks; TASK-017 implicitly via scope-bounded-diff verification), but the framework-general convention is open. Candidate routes: `development_guide.md` § Code Style → Patterns to Follow extension naming cross-Task editing-order alongside the existing within-Task rule; or `shannon/skills/work-items/skill.md` § Process: Plan note for Epics whose Tasks chain edits on one source-of-truth body. Revisit after EPIC-009 implementation surfaces lived-in evidence. #framework #development-guide #work-items #editing-order #feat-009
- **Documentation reference at planning and elaboration not robustly enforced** — Field report from a different project running an older Shannon version: implementer agents do not consistently consult the mandated documents during `*-elaborate` and `*-plan` steps. The Work Item ↔ Document Relationships table in `shannon/skills/work-items/skill.md` names which documents each verb at each work-item type *must align to*, but the enforcement mechanism is prose-level (the skill body tells the implementer to read those docs; nothing structurally compels it). Subagent prompts can be told to read specific docs (as the Shannon-self elaboration subagents do — see EPIC-009 Gate 1 brief), but there is no canonical pattern requiring this, and there is no Gate 1 / Gate 2 verification step that confirms the work item's drafted Requirements / Plan actually cite the documents the relationships table names. Worth reviewing: (a) whether the skill body's prose-level instruction is sufficient or whether the subagent contract should require explicit "Read these documents" steps as a numbered process item rather than implicit; (b) whether the Gate 1 / Gate 2 alignment subagent (which Shannon-self already uses) should be canonicalised as a *mandatory* step rather than an *implementer-judgement* step; (c) whether the work-item template should carry a "Documents Consulted" sub-section that the implementer fills in during elaboration / planning, making non-consultation visible at gate review. Candidate routes: `shannon/skills/work-items/skill.md` § Process: Elaborate and § Process: Plan tightening; `shannon/skills/work-items/templates/*.md` template additions; `conceptual_design.md` § Re-elaborating a Work Item or new § Work Items Consume Documents Rule. Surfaced 2026-05-30 by directing-party field report from a separate project on an older Shannon version. #framework #work-items #conceptual-design #enforcement #documentation
- **Canonical-frontmatter-field naming for supervisor subagents (and Shannon skill / subagent frontmatter generally)** — Surfaced 2026-06-21 during TASK-013 Gate 1; second datapoint after TASK-012 Gate 1 (commit `2a0b31f` — R-1, R-2 refinements applied). At EPIC-009 Gate 2 the Cascading Preparation produced three checker-Task stubs whose ACs each said "explicit frontmatter naming [model]" / "restricted tool access (Read + ...)" without naming the canonical YAML keys (`model:` and `allowed-tools:`) or concrete model identifiers. Both TASK-012 and TASK-013 needed identical R-1 + R-2 refinements at Gate 1 to land the canonical form. The implementer-side risk is that a generic-frontmatter AC permits a wrong key that doesn't take effect at runtime; the runtime check is deferred to TASK-018 dogfood. Candidate canonical convention: a `work-items` skill § Process: Plan note for Tasks whose ACs touch Claude Code skill / subagent frontmatter, requiring the AC to name the canonical YAML field key and a concrete value form. Alternatively: a `technical_design.md` § System Architecture → *Skills* or *Supervisor* extension naming the canonical frontmatter convention so future Tasks inherit it from the cascade docs. Lived precedent: TASK-012 R-1 / R-2 (commit `2a0b31f` 2026-06-09), TASK-013 R-1 / R-2 (2026-06-22). TASK-014 Gate 1 will likely be a third confirming datapoint rather than a triangulating one. Revisit at TASK-014 Gate 1 (confirming) or promote to follow-up work-item against `work-items` skill / `technical_design.md` at next development-discipline Epic under FEAT-008. #framework #work-items #technical-design #skills #frontmatter #feat-009
- **No in-place upgrade / version-migration path for projects on an older Shannon** — Surfaced 2026-05-30 from a review of the `endless-worlds` project, which runs a pre-Epic Shannon (Feature → **Phase** → Task, with `feature-phase-{plan,start,complete}` commands) and is the named Phase-5 dogfood target in FEAT-009's supervisor plan. `/shannon-setup` is greenfield-only: it detects existing state and deploys templates, but there is no defined procedure to *upgrade* a project that already carries artefacts in an older vocabulary. Applying current Shannon "correctly" is therefore undefined for these projects. Concrete reconciliation gaps a migration path must address: **(1) Work-item vocabulary remap** — the retired *Phase* concept maps to *Epic*; existing `task_index.md` / `feature_index.md` and all task parentage need re-homing (in `endless-worlds`: 8 phases/milestones, 46 Tasks). **(2) Document canon reconciliation** — the older doc set carries docs with no home in the current six-doc canon: a standalone `code_style_guide.md` (current `development_guide.md` explicitly absorbs code style/testing/git/CI), a `documentation_style_guide.md` (no canonical home), and a `docs/adr/` directory (no ADR concept in current Shannon). Mechanical rename risks silent content loss; these need explicit merge/relocate/orphan rules. **(3) `product_requirements.md` → `vision.md` is a split, not a rename** — current `vision.md` is narrow (problem, vision, principles, users, metrics); an older PRD also carries scope, user stories, and nav/view model that must be redistributed (vision bits → `vision.md`; entities/nav → `conceptual_design.md`). **(4) CLAUDE.md + templates collision** — an upgraded project's CLAUDE.md (often dense with handoff state) collides with the V6 CLAUDE.md template, and an in-repo `docs/templates/` becomes a stale second source of truth against current Shannon's skill-resident templates. **Timing note:** the heavier three-gate model lands hardest on a fast solo project *before* the supervisor (FEAT-009 / EPIC-009, in flight) ships the gate-authority offload designed to make it bearable — so the upgrade is best sequenced after the supervisor lands, onto a stable cut, to upgrade once rather than chase an in-flight framework. Candidate route: a new `/shannon-upgrade` command (or `project-setup` skill extension) carrying a doc-reconciliation checklist — rename map, merge/orphan rules, and a phase→Epic work-item remap — plus a Shannon version stamp so a project can declare which version it targets. Likely warrants its own Feature (or an Epic under FEAT-001 *Easy to Setup*). Revisit when FEAT-009 Phase 5 (endless-worlds upgrade) becomes active. #framework #project-setup #migration #versioning #work-items #feat-001 #feat-009

---

- **Supervisor-scope activation signal for hooks (`SHANNON_SUPERVISOR_SCOPE`) not yet codified at framework level** — Surfaced 2026-06-27 during TASK-016 (PreToolUse write-guard) Gate 1. A PreToolUse write-guard registered in `settings.json` fires on *every* tool use in the session; to avoid blocking the directing party's ordinary interactive development it must be **inert unless the session is a supervisor-scoped invocation**. `technical_design.md` v1.2 § Hook integration names the trigger as "a supervisor-scoped invocation" but does not define the runtime signal that distinguishes one. TASK-016 introduces the env var `SHANNON_SUPERVISOR_SCOPE` (non-empty → enforce; absent/empty → inert) as that signal and makes the hook inert-by-default. This is a framework-general convention: (a) EPIC-011's headless cadence launcher (`claude --bare -p ...`, per `technical_design.md` § Cadence) must *set* the signal so the guard actually enforces during autonomous runs; (b) every other scope-gated supervisor hook (PostToolUse audit log — TASK-017; SessionStart; Stop) should read the same signal for consistency; (c) TASK-018's dogfood sets it manually to test enforcement. Candidate route: `technical_design.md` § System Architecture → *Supervisor* → *Hook integration* extension naming the canonical signal + inert-by-default contract, or a `development_guide.md` convention. The exact name (`SHANNON_SUPERVISOR_SCOPE` vs an alternative) and whether an env var (propagation to hook subprocesses is a TASK-018 runtime question) or a sentinel file is the more robust carrier should be confirmed at TASK-018. #framework #technical-design #supervisor #hooks #feat-009

- **Skill-building Epics must scope the command entry point — EPIC-009 omitted `/shannon-report`** — Surfaced 2026-06-28 during TASK-018 (dogfood) Gate 1. EPIC-009 decomposed into skill skeleton → checkers → report pipeline → hooks → dogfood, but **never included the thin `/shannon-report` command file** — so the skill's `/shannon-report` contract was described in `SKILL.md` prose yet had no invocation surface (Shannon's Commands+Skills+Subagents architecture requires a thin `./.claude/commands/*.md` delegate). Caught only at the dogfood, fixed by corrective TASK-020. Framework-general lesson: an Epic that adds a *new skill with a slash-command surface* must scope the **command-layer entry point** as an explicit Task (or AC), not assume the skill is reachable once its `SKILL.md` exists. Candidate route: a `work-items` skill § Process: Plan note for skill-creating Epics (decomposition must include the command entry point), or a `conceptual_design.md` / `technical_design.md` § architecture reminder that command + skill ship together. Also relevant: the existing 24 command files are the shape precedent (`shannon/commands/README.md`). #framework #work-items #conceptual-design #commands #feat-009
- **Supervisor runtime integration (hook auto-firing, env propagation, slash-command triggering) is unverifiable in an interactive dev session** — Surfaced 2026-06-28 during TASK-018 Gate 1. The supervisor is a Markdown skill an agent executes by following `SKILL.md`; three things can only be verified by a *real deployed/headless invocation*, not by an agent acting as the supervisor in a dev session: (1) that Claude Code **auto-fires** the `settings.json`-registered PreToolUse/PostToolUse hooks on the agent's tool calls; (2) that `SHANNON_SUPERVISOR_SCOPE` **propagates** from the session env to the hook subprocess (the TASK-016 harness bug showed env does not cross a pipe — the live question is whether Claude Code's hook invocation passes it; if not, the **sentinel-file fallback** in the `SHANNON_SUPERVISOR_SCOPE` scratchpad item is needed); (3) that a literal `/shannon-report` slash-command triggers the skill. TASK-018 demonstrates hook *logic* via direct script runs and produces real report/index artefacts, but defers (1)–(3) to **EPIC-011** (autonomous headless runs — `claude --bare -p ...` per `technical_design.md` § Cadence), which is the natural place to verify them. #framework #technical-design #supervisor #hooks #runtime #feat-009 #epic-011

- **Knowledge-index path mismatch — supervisor codifies `docs/knowledge_index.md`, real index is `docs/knowledge/knowledge_index.md` (needs a corrective Task)** — Surfaced 2026-07-05 by the **first dogfood `/shannon-report` run** (TASK-018) — the exact defect the dogfood existed to catch, missed by every isolated Task's Gate-3 verification (they checked internal consistency, not the real filesystem path). The supervisor consistently references `./docs/knowledge_index.md` — but the project's knowledge index lives at `./docs/knowledge/knowledge_index.md`. The mismatch spans three APPROVED Tasks: `SKILL.md` § /shannon-report + § Report Pipeline (lines 38/83/85/91, TASK-011/015), `templates/footer.md` line 4 (TASK-015), and — most consequentially — the PreToolUse write-guard exception `pretool-writeguard.sh` line 18 `index_exception="docs/knowledge_index.md"` + `pretooluse.settings.json` _comment (TASK-016). **Demonstrated live**: with the guard active, a write to the *real* `docs/knowledge/knowledge_index.md` is refused (exit 2) while the wrong codified path is allowed. A second, minor nuance: the codified "index with **Type** *Supervisor Report*" convention doesn't fit the actual `knowledge_index.md` format (which uses section headers — Research / Implementation / Extensions); the dogfood added a new "Supervisor Reports" section. **Recommended resolution: a corrective Task (TASK-021)** fixing the path across `SKILL.md`, `pretool-writeguard.sh`, `pretooluse.settings.json`, and `footer.md` (deployed to `./.claude/`), plus a decision on whether the knowledge-index convention is a `Type:` field or a section. The three source Tasks stay APPROVED; the corrective Task supersedes the path. #framework #supervisor #bug #knowledge-index #dogfood #feat-009
- **Do parent Epics advance to IMPLEMENTING when their child Tasks are being implemented?** — Surfaced 2026-07-05 by the dogfood's Lifecycle Checker. EPIC-009 stayed at `PLANNED` throughout — nine child Tasks reached APPROVED and TASK-018 is in flight, but the Epic was never transitioned to IMPLEMENTING via `/epic-implement`. The unified lifecycle (`DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`) applies to all work-item types, but Shannon has no explicit convention for *when* a parent Epic advances through the iterative zone relative to its children — is the Epic a planning-only container that jumps PLANNED → (children) → REVIEW, or should it be marked IMPLEMENTING while children implement? Either is defensible; the framework is silent, so the Lifecycle Checker correctly flags the Epic's PLANNED-with-implementing-children state as an uncertain lifecycle finding. Candidate route: `conceptual_design.md` § the lifecycle / `work-items` skill § Process clarification for parent-item status during child implementation. #framework #conceptual-design #work-items #lifecycle #epic-status #dogfood

## Processed

*(When items are promoted to work items or otherwise resolved, move them here briefly with a pointer to where they live now. Keep this list short — old entries can be cleared once they're solidly part of project history.)*

- **V6-stale text in technical_design.md "single-developer tool"** — Resolved 2026-05-16 during technical_design Gate 1 review. Lines 92 and 176 rewritten to reflect V6 cooperative-access model. See commit `1943f3f`.
- **ux_guide.md inherited PENDING drift** — Resolved 2026-05-17 during ux_guide Gate 1 review. §Cascading Operations rewritten to the "prepared draft surfaces at gate invocation" pattern; PLAN-PENDING references removed. See commit `d4ccc03`.
- **Create lightweight idea capture feature** — Partially resolved 2026-05-18. FEAT-007 created to capture the scratchpad pattern as a Shannon capability. Outstanding work split: Vision update tracked as a separate scratchpad item; deeper formalisation (conceptual_design entry, technical_design notes, possible `/scratchpad-*` commands) tracked as FEAT-007's candidate next Epic.
- **Vision update: add Lightweight Idea Capture as a Key Feature** — Resolved 2026-05-18 in Vision v2.2 (G1 review finding). Lightweight Idea Capture added as 5th Key Feature; FEAT-007's Vision Reference now points at it. Also folded in Retrospective adoption acknowledgement (G2) and "from adoption" Vision Statement wording (S1).
- **Workflow: Re-approval of APPROVED Docs After Upstream Change** — Resolved 2026-05-18. Canonised as the *Re-reviewing an APPROVED Mandated Document* workflow in conceptual_design v1.3. The pattern was prototyped during the Vision v2.2 re-review (commit `3a65a73`) and ratified here. Also partially addresses former G1 (in-review drift detection covered; out-of-review proactive checks still open).
- **Workflow: Re-elaborating a Work Item (F1)** — Promoted 2026-05-19 to EPIC-006 (Work Item Lifecycle Maturation) under FEAT-003; **delivered 2026-05-22**. TASK-001 added the canonical *Re-elaborating a Work Item* workflow to conceptual_design v1.5 (APPROVED, commit `2b3bd33`); TASK-003 applied it to FEAT-003/006/007 as the recursive-dogfood pass. FEAT-003 Ideal State aspirational bullet 1 now marked met.
- **Initial Implementation field as a first-class lifecycle position (F2)** — Promoted 2026-05-19 to EPIC-006 (Work Item Lifecycle Maturation) under FEAT-003; **delivered 2026-05-22**. TASK-002 shipped the queryable Partial-completeness affordance — feature-template sub-block + canonised `(Partial)` index suffix (APPROVED, commit `09844df`); TASK-003 applied it to FEAT-003/006/007. FEAT-003 Ideal State aspirational bullet 2 now marked met.
- **Naming convention: avoid opaque plan-letter labels (TASK-A/B/C) for Tasks** — Promoted 2026-05-24 to EPIC-007 (Work-Item Conventions Surfaced Through Dogfooding) under FEAT-003; **delivered 2026-05-25** (both implementing Tasks APPROVED). TASK-008 added the doc-half at `conceptual_design.md:58` § Domain Model → *Work Item* Naming note, with verbatim TASK-A/B/C exclusion + position-language alternative ("the first Task", "the verification Task"); TASK-009 added the skill-half at `shannon/skills/work-items/skill.md:193` § Process: Plan step 3 *Cascading Preparation (for Epics)*, with the same exclusion + alternative. Both halves cross-reference each other. APPROVED at EPIC-007 Gate 3 2026-05-27.
- **Meta-gap: no channel for feeding framework-general resolutions back into the framework** — Promoted 2026-05-24 to EPIC-008 (Development Conventions Surfaced Through Dogfooding) under FEAT-008; **delivered 2026-05-27** (both implementing Tasks APPROVED). TASK-007 added the canonical Pre-Commit Checklist line at `development_guide.md:114` § Testing Strategy → Pre-Commit Checklist with the verbatim trigger phrase *"framework-general ambiguity surfaced during this work — a convention, workflow gap, or rule clarification that future work items would otherwise re-derive"*; TASK-010 added the matching step-adjoining soft prompts at `shannon/skills/work-items/skill.md:174` § Process: Plan (Gate 2) and `:236` § Process: Review (Gate 3). Both halves cross-reference each other. APPROVED at EPIC-008 Gate 3 2026-05-27.
- **Push-frequency directive for development_guide** — Promoted 2026-05-24 to EPIC-008 (Development Conventions Surfaced Through Dogfooding) under FEAT-008; **delivered 2026-05-24** (TASK-007 APPROVED, commit `eac486b`). TASK-007 added the new `### Push Cadence` subsection at `development_guide.md:149` § Git Workflow as sibling to Commit Cadence, with two named triggers (after every Gate 3 approval; at session end), the verbatim inline session-end definition, and reciprocal cross-references with Commit Cadence at lines 147 / 156. APPROVED at EPIC-008 Gate 3 2026-05-27. First real-world invocation of the new directive was the push that landed it (commit `eac486b`).
- **Framework clarifications buried in TASK-003's Plan** — Promoted 2026-05-24 to EPIC-007 (Work-Item Conventions Surfaced Through Dogfooding) under FEAT-003 (sub-items (b) commit-hash timing and (c) bundled re-elaboration) and EPIC-008 (Development Conventions Surfaced Through Dogfooding) under FEAT-008 (sub-item (a) editing order). **Delivered 2026-05-27** at both Epics' approval. EPIC-007 / TASK-008 landed (b) at `conceptual_design.md` § Re-elaborating a Work Item *Flow* step 6 (commit-hash timing — cite upstream commit hash, reference work item by ID) and (c) at *Flow* step 5 sub-bullet (bundled re-elaboration permitted under named conditions); APPROVED 2026-05-27 commit `cffb1ab`. EPIC-008 / TASK-007 landed (a) at `development_guide.md:79` § Code Style → Patterns to Follow → *Source-of-truth body before derived artefacts* (editing-order convention citing TASK-003 as worked precedent); APPROVED 2026-05-27 at EPIC-008 Gate 3. The two Epics' second-approver-moves rule (EPIC-007 AC#7 / EPIC-008 AC#6 co-authored) fired at EPIC-008 Gate 3 (the second of the two to approve), so the shared item lands here with the both-Epics pointer.
