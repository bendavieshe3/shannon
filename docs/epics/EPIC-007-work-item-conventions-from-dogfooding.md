# EPIC-007: Work-Item Conventions Surfaced Through Dogfooding

## Metadata

- **Status**: ELABORATED
- **Type**: Epic
- **Parent**: [FEAT-003](../features/FEAT-003-unified-work-item-model.md)
- **Created**: 2026-05-24
- **Updated**: 2026-05-24 (elaborated post-split)

> **Status** moves through the unified lifecycle: `DRAFT → ELABORATED → PLANNED → IMPLEMENTING ↔ IMPLEMENTED ↔ REVIEW → APPROVED`. Approved epics remain as historical records of how the parent feature evolved.

---

## Requirements

*Initial intent captured at `/epic-create` 2026-05-24; first elaboration pass produced 11 ACs across seven items, then re-scoped (same day) to the work-item-conventions half of the dogfooding harvest by direct rewrite (see Activity Log) — sibling [EPIC-008](EPIC-008-development-conventions-from-dogfooding.md) covers the dev-discipline half. This refinement pass is the first formal Gate 1 elaboration against the trimmed four-item scope.*

### Overview

This Epic promotes the **work-item-workflow** half of the EPIC-005 / EPIC-006 dogfooding harvest into ratified rules in `conceptual_design.md` (with companion soft prompts in the `work-items` skill where the convention meets work-item planning). Four work-item-specific clarifications surfaced during use need promotion:

1. **Commit-hash timing in re-elaboration Activity Log entries** — a re-elaboration entry cannot cite its own work item's commit hash because the commit lands after Gate 3; cite the upstream hash and reference the work item by ID. *(→ clarify `conceptual_design` § Re-elaborating a Work Item step 6's existing "where applicable" phrase; from TASK-003 Plan § Approach + Implementation Notes.)*
2. **Bundled re-elaboration** — multiple Feature re-elaborations run inside one Task's implementation, with the Task's own Gate 2 / Gate 3 serving as the per-Feature directing-party refinement-approval. The workflow is currently silent on this; the pattern is sound, additive, and exercised live by TASK-003. *(→ additive clause in `conceptual_design` § Re-elaborating a Work Item.)*
3. **Naming convention — no opaque plan-letter labels for Tasks**. Plan-time letter placeholders (TASK-A / TASK-B / TASK-C) should never propagate past the moment real IDs are allocated; the descriptive title plus real ID is the name; ordering is expressed by position language ("the first Task", "the verification Task"). *(→ rule in `conceptual_design` work-item naming + planning-time prompt in the `work-items` skill.)*
4. **Cross-type scope-guard AC convention — three-instance fragility pattern**. TASK-002 AC#6 ("only two files modified") contradicted AC#5 (which required a third); TASK-002 AC#8's example grep was mis-worded; TASK-003 AC#9 enumerated six files when the diff carried eight. Three occurrences is a pattern: scope ACs use a cross-type *guard*, never a literal count or fixed file list. *(→ AC-writing rule in `conceptual_design` + planning-time prompt in the `work-items` skill; rule explicitly distinguishes scope-assertion ACs from edit-target ACs.)*

The Epic is one half of a paired structural promotion. Its sibling [EPIC-008](EPIC-008-development-conventions-from-dogfooding.md) under [FEAT-008](../features/FEAT-008-development-discipline.md) covers the dev-discipline half — editing-order convention, push-cadence directive, and the meta-gap routing channel that future learnings flow through. The split was made on the directing party's call (2026-05-24): *"best to acknowledge the split now and pay the upfront cost to prevent more cost/re-work later — future learnings will compound when Shannon is used on real projects beyond dogfooding itself."* FEAT-003 was re-elaborated 2026-05-24 (additive) to add Ideal State bullet 4, the explicit anchor for "work-item conventions accumulated through framework use are promoted into the mandated documents (primarily `conceptual_design`) and the relevant skills via the canonical work-item workflow."

**Recursive-dogfood character (record-only).** EPIC-007's four items reached this Epic via the same kind of resolution-routing that EPIC-008's AC#3 codifies as a channel: the items were captured in `scratchpad.md` and in archived Task Review sections during EPIC-005 / EPIC-006 work, then surfaced for promotion. EPIC-007 is one of the first two formal exercises of that channel (the other is EPIC-008 itself). This recursion is **recorded in EPIC-008's AC#7** (the durable knowledge-note record names EPIC-007 as the contemporaneous sibling exercise); no parallel AC is added here. EPIC-007's contribution to the record is its existence and approval — both visible from EPIC-008's record at lookup time.

### Acceptance Criteria

*Each criterion names a verifiable outcome — the exact location and shape of the text the implementing Task(s) will land in `conceptual_design.md` and/or the `work-items` skill. Locations marked "implementing Task may override" leave the named home as a strong recommendation that reduces planning uncertainty but does not bind the implementer when a better home emerges in context.*

- **AC#1 — Commit-hash timing clarified in re-elaboration workflow** *(item 1)*. `conceptual_design.md` § Re-elaborating a Work Item *Flow* step 6 — which currently reads *"Activity Log entry is added recording (a) trigger category … (b) upstream commit hash where applicable, and (c) summary of what changed"* — gains an explicit clarification of what "where applicable" means in the bundled-Task case. The clarification lands either inline within step 6 or as an adjoining sub-bullet/note immediately beneath step 6 (implementing Task may override based on readability). It must state two things in this order: (i) **when the re-elaboration is bundled inside a Task whose own commit lands after Gate 3** (per `development_guide.md` § Commit Cadence — commits land after Gate 3, not mid-implementation), the entry **cannot** cite its own work item's commit hash because that hash does not exist at write-time; (ii) the entry instead cites the **upstream commit hash** — the hash that delivered the change being absorbed — and **references the bundling work item by ID**. The clarification names **TASK-003** explicitly as the worked example (the case where this exact pattern was exercised live, citing upstream `09844df` and referencing TASK-003 by ID). The clarification does not modify or remove the existing "where applicable" phrase — that phrase is the hook the clarification expands. Verifiable: (a) the text "upstream commit hash" and "where applicable" both remain at step 6; (b) the new prose (inline or sub-bullet) names the bundled-Task case, the commit-cadence reason the work item's own hash is unavailable, and TASK-003 as the worked example; (c) the prose uses the words "upstream commit hash" and "reference … by ID" verbatim.

- **AC#2 — Bundled re-elaboration clause added to re-elaboration workflow** *(item 2)*. `conceptual_design.md` § Re-elaborating a Work Item gains an additive sub-clause naming **bundled re-elaboration** as a valid pattern. **Recommended home: a new sub-bullet attached to *Flow* step 5** (which currently reads *"Implementer applies the approved refinements; directing party explicitly approves the new state via Gate 1"*) — this is where the gating semantics already live, and bundled re-elaboration is precisely a Gate-1 satisfaction variant. The implementing Task may override (e.g. *Status semantics* as an alternative) but should record the placement choice. The clause must state: (i) **multiple work-item re-elaborations may be applied inside a single Task's implementation**; (ii) **the Task's own Gate 2 and Gate 3 serve as the per-item directing-party refinement-approval** the workflow's step 5 otherwise requires; (iii) this is permitted **only when** (a) the bundle is *additive* per § Status semantics and (b) the parent (Epic or Task) **explicitly enumerates** the re-elaborated work items by ID in its Plan or Acceptance Criteria. The clause cross-references AC#1's commit-hash clarification (the two co-occur in TASK-003's worked example) and names TASK-003 as the worked precedent. Verifiable: (a) the new clause exists at the chosen step-5-adjacent home (or alternative with recorded rationale); (b) it contains the phrase "bundled re-elaboration"; (c) it names the three conditions above (Gate 2 / Gate 3 transitively, additive, parent enumerates by ID); (d) it cross-references AC#1's commit-hash text by section pointer; (e) it cites TASK-003 as the worked precedent.

- **AC#3 — Naming convention against opaque plan-letter Task labels** *(item 3)*. `conceptual_design.md` gains a one-line convention against opaque plan-letter labels for child work items. **Recommended home: appended to the *Work Item* entity description in § Domain Model** (under *Attributes* or as a brief naming note immediately following the existing attribute list) — this is the entity whose naming the rule constrains, and naming sits more naturally with entity definition than with business rules. The implementing Task may instead place it as a new § Business Rules entry (e.g. *"Descriptive Work-Item Naming"*) if integration into the entity description proves awkward; record the placement choice. The convention text: *"Child work items in Epic plans are named by descriptive title (and by real ID once allocated) — never by opaque plan-letter labels (TASK-A, TASK-B, TASK-C). Ordering, when relevant, is expressed by position language ('the first Task', 'the verification Task'). Letter placeholders that appear in transient planning notation must not propagate into the Epic § Plan, child work-item files, indexes, or downstream prose."* A companion prompt is added to `shannon/skills/work-items/skill.md` § Process: Plan (Gate 2) at the cascading-preparation step (step 3, *Cascading Preparation (for Epics)*) — the precise point where the planning subagent allocates IDs and slugs for child Tasks — instructing the subagent to use descriptive titles from the outset and to not introduce letter placeholders even as transient draft labels. Verifiable: (a) the convention exists at the chosen `conceptual_design` location and contains the verbatim "TASK-A, TASK-B" exclusion and the position-language alternative; (b) the skill prompt exists at § Process: Plan step 3 (or an adjacent step the implementer judges more natural, with rationale); (c) both halves cross-reference each other (see AC#6).

- **AC#4 — Cross-type scope-guard AC convention** *(item 4)*. `conceptual_design.md` gains a work-item AC-writing rule. **Recommended home: a new § Business Rules entry** (e.g. *"Scope-Boundary Acceptance Criteria Use Cross-Type Guards"*) — the existing Business Rules section is where Shannon's general work-item discipline rules live (*Unified Status Lifecycle*, *Approved Tasks Are Archived*, etc.) and an AC-writing rule sits naturally alongside them. The implementing Task may instead place it as a sub-section under § Key Workflows → *Taking a Task from Idea to Approval* (or as a new general "Writing Acceptance Criteria" workflow) if a workflow framing reads more naturally; record the placement choice. The rule's text:

  > **Scope-Boundary Acceptance Criteria Use Cross-Type Guards.** When a work-item Acceptance Criterion expresses scope (what is *not* touched), phrase it as a guard against changes to other types or layers — e.g. *"no epic/task/spike template, index, or skill modified"*. Do **not** enumerate a literal file count or a fixed file list as the scope assertion: work-item transitions necessarily touch routine bookkeeping files (work-item index entries, parent work-item Tasks-line entries) that literal counts and fixed enumerations cannot anticipate.
  >
  > Acceptance Criteria *may* name target files when describing edit-target intent (e.g. *"the FEAT-XXX file gains a `**Met:** / **Remaining:**` sub-block"*). The rule applies only when the AC's purpose is to assert what is *out of scope*. The distinguishing test: **if the AC's failure mode is "an unintended file changed", it is a scope-guard AC and must use the cross-type-guard form**. (The test phrasing is intentionally general so it transfers to non-work-item contexts later, but the rule itself is scoped to work-item Acceptance Criteria — non-work-item scope assertions are outside this Epic's mandate.)

  The rule cites the three-instance precedent (TASK-002 AC#6 — "only two files modified" contradicted AC#5; TASK-002 AC#8 — example grep missing bold markers around the field name; TASK-003 AC#9 — enumerated six files when the diff carried eight) as the worked pattern that motivated the rule. A companion prompt is added to `shannon/skills/work-items/skill.md` § Process: Plan (Gate 2) — at step 2 (*Spawn Planning Subagent*), under the bullet describing AC drafting — reminding the planning subagent to apply the failure-mode test when drafting scope ACs and to use cross-type-guard phrasing. Verifiable: (a) the rule exists at the chosen `conceptual_design` home with the recommended title; (b) the failure-mode test appears verbatim ("if the AC's failure mode is 'an unintended file changed'"); (c) the scope-of-applicability note (work-item ACs; test phrasing general) appears; (d) all three precedent ACs are cited with their literal-count defects named; (e) the skill prompt exists at § Process: Plan step 2 (or an adjacent step with recorded rationale) and references the failure-mode test verbatim.

- **AC#5 — All amendments to `conceptual_design` are additive; doc stays APPROVED.** Per `conceptual_design.md` § Re-reviewing an APPROVED Mandated Document → *Status semantics*, every amendment landed by this Epic is treated as **additive** (non-contradictory addition to existing approved claims): AC#1 expands an existing "where applicable" hook; AC#2 adds a new clause naming a new pattern without changing existing flow steps; AC#3 adds an entity-naming convention; AC#4 adds a new business rule. None contradicts any previously-approved claim. `conceptual_design.md` therefore stays APPROVED through every amending Task — does not transition to DRAFT. The amendments get one § Version History entry recording the additive bump (e.g. `v1.5 → v1.6`). The entry **must**: (i) name **EPIC-007 by ID** as the source; (ii) **list each amendment by section anchor** — § Re-elaborating a Work Item *Flow* step 6 (AC#1), § Re-elaborating a Work Item *Flow* step 5 or chosen home (AC#2), § Domain Model *Work Item* or chosen home (AC#3), § Business Rules *Scope-Boundary Acceptance Criteria Use Cross-Type Guards* or chosen home (AC#4); (iii) explicitly classify the bump as "additive amendment per § Re-reviewing"; (iv) record the new Approved date. Verifiable: (a) post-Epic, `conceptual_design.md` Status field reads APPROVED with Last Reviewed and Approved dates updated; (b) one Version History entry exists naming EPIC-007 by ID, enumerating the four amendments by section anchor, and classified additive.

- **AC#6 — Cross-references between the new conventions are explicit and bidirectional**. Required cross-references:
  - **AC#2 ↔ AC#1** — the bundled re-elaboration clause cross-references the commit-hash clarification at AC#1 by section anchor (these co-occur in TASK-003's worked example); the commit-hash clarification at AC#1 carries a back-reference to the bundled re-elaboration clause naming it as the typical surrounding context for the bundled-Task case.
  - **AC#3 (`conceptual_design`) ↔ AC#3 (`work-items` skill)** — the naming convention in `conceptual_design` and the planning prompt in `shannon/skills/work-items/skill.md` § Process: Plan reference each other by file path and section anchor.
  - **AC#4 (`conceptual_design`) ↔ AC#4 (`work-items` skill)** — the scope-guard business rule in `conceptual_design` and the planning prompt in `shannon/skills/work-items/skill.md` § Process: Plan reference each other by file path and section anchor.
  - **AC#4 ↔ EPIC-008 AC#1 (editing-order convention in `development_guide.md`)** — companion rules across the FEAT-003 / FEAT-008 split. The scope-guard rule in `conceptual_design` references the editing-order convention in `development_guide.md` § Code Style by section anchor; EPIC-008's AC#5 names the converse direction (editing-order text references scope-guard rule by section anchor). The cross-reference is justified by shared subject matter: editing-order constrains *how* a multi-file edit is performed; scope-guard constrains *how* an AC asserts the resulting change boundary. They are paired edit-discipline / AC-writing rules — together they answer "what must I edit, in what order, and how do I assert the boundary?" Both directions resolve at implementation time (EPIC-007's amending Task lands its end; EPIC-008's amending Task lands the converse).

  Verifiable: each named cross-reference exists at both ends; section anchors resolve.

- **AC#7 — Scratchpad bookkeeping**. At Gate 3, the scratchpad source items addressed by this Epic transition per the following rules. The wording of the partial-closure annotation is letter-for-letter coordinated with EPIC-008 AC#6's mirror clause:
  - **"Framework clarifications buried in TASK-003's Plan"** — sub-items **(b) commit-hash timing** and **(c) bundled re-elaboration** are addressed by this Epic; sub-item (a) editing order is addressed by EPIC-008. The scratchpad item is a single line covering all three sub-items, so it cannot be split. **Bookkeeping rule: whichever of EPIC-007 / EPIC-008 reaches Gate 3 second performs the move to § Processed, with a pointer naming both Epics.** If only EPIC-007 has reached Gate 3 (EPIC-008 still in flight), the item stays in § Items with an inline annotation: *"Partial closure: sub-items (b), (c) addressed by EPIC-007 APPROVED [date]; sub-item (a) pending under EPIC-008."* (The mirror clause in EPIC-008 AC#6 carries the converse annotation. The two ACs are deliberately co-authored so the second approver always performs the move regardless of which Epic that is.)
  - **"Naming convention: avoid opaque plan-letter labels (TASK-A/B/C) for Tasks"** — addressed entirely by this Epic; moves to § Processed at this Epic's Gate 3 with an EPIC-007-citing pointer (one line, naming EPIC-007 and the Approved date).
  - **Item 4 (cross-type scope-guard) is not a discrete scratchpad item** — its source artefacts live in the archived TASK-002 Review (AC#6 and AC#8 caveats) and TASK-003 Review ("Third instance of literal-file-count fragility"). Archived Reviews are the historical record and are not moved. No scratchpad entry exists for this item; none needs to be created.

  Verifiable: post-Epic, *"Naming convention"* lives in § Processed; § Items no longer contains it. *"Framework clarifications"* moves only if EPIC-008 is also APPROVED; otherwise stays in § Items with the partial-closure annotation in the exact phrasing above.

### Context

- **Anchor**: Vision v2.3 § Core Principles #4 *Adaptive Coherence* (upstream direction — lessons flow back into framework rules); **FEAT-003 § Ideal State bullet 4** (added 2026-05-24 via FEAT-003 re-elaboration) anchors this Epic explicitly with the phrasing "work-item conventions accumulated through framework use — re-elaboration mechanics, AC writing patterns, naming discipline — are promoted into the mandated documents (primarily `conceptual_design`) and the relevant skills via the canonical work-item workflow." The Epic's four items map directly: AC#1 and AC#2 deliver re-elaboration mechanics; AC#4 delivers AC writing patterns; AC#3 delivers naming discipline. No part of bullet 4 is uncovered; no out-of-bullet aspiration is in scope. The Gate-1 *Anchor Gap* finding from the original (pre-split) elaboration is closed by FEAT-003's re-elaboration; the *parent-fit Drift* finding (items 1, 5 fit FEAT-003 loosely) is resolved by the EPIC-007 / EPIC-008 split.
- **Sibling Epic**: [EPIC-008 — Development Conventions Surfaced Through Dogfooding](EPIC-008-development-conventions-from-dogfooding.md) — the dev-discipline half (editing-order, push-cadence, meta-gap routing channel) under [FEAT-008 — Development Discipline](../features/FEAT-008-development-discipline.md). Cross-Epic coordination: EPIC-007 AC#6 and EPIC-008 AC#5 mirror each other (each names the other-Epic cross-references its amending Task lands); EPIC-007 AC#7 and EPIC-008 AC#6 mirror each other (shared scratchpad item; second-approver moves to § Processed; partial-closure annotation text co-authored letter-for-letter). The recursive-dogfood record (knowledge note recording the channel-and-its-first-exercises) lives in EPIC-008 AC#7 and names EPIC-007 as contemporaneous sibling exercise; EPIC-007 does not duplicate the record.
- **Scratchpad source items** (per AC#7): "Framework clarifications buried in TASK-003's Plan" sub-items (b) commit-hash timing and (c) bundled re-elaboration; "Naming convention: avoid opaque plan-letter labels (TASK-A/B/C) for Tasks". *(Both confirmed present in `docs/scratchpad.md` § Items at elaboration time.)*
- **Archived-Task Review source items**: TASK-002 Review (AC#6 internal contradiction; AC#8 mis-worded example grep); TASK-003 Review ("Third instance of literal-file-count fragility") — the three-instance precedent for AC#4.
- **Touched documents** (expected): `docs/conceptual_design.md` (additive amendments to § Re-elaborating a Work Item *Flow* for AC#1 and AC#2, to § Domain Model *Work Item* or § Business Rules for AC#3, to § Business Rules for AC#4; one § Version History entry citing EPIC-007 and enumerating amendments by section anchor for AC#5); `shannon/skills/work-items/skill.md` (planning prompts at § Process: Plan steps 2 and 3 for AC#3 and AC#4); `docs/scratchpad.md` (two scratchpad items transitioned per AC#7 conditions).

---

## Plan

*To be filled during `/epic-plan EPIC-007` (Gate 2). Likely 1–2 Tasks: a `conceptual_design` amendment Task covering AC#1–AC#4 (plus AC#5 additivity + AC#6 cross-references), and a `work-items` skill prompt Task (or folded into the conceptual_design Task) covering the planning-prompt halves of AC#3 and AC#4.*

### Approach

*Filled during planning.*

### Tasks

*Filled during planning.*

### Dependencies

**Depends on**: Vision v2.3 (met); FEAT-003 § Ideal State bullet 4 added 2026-05-24 re-elaboration (met — the explicit anchor); `conceptual_design.md` v1.5 (met — the document this Epic additively amends); `work-items` skill (met). All EPIC-005 and EPIC-006 child Tasks APPROVED — the source Reviews and Plans this Epic harvests from are stable.

**Depended on by**: future sessions that would otherwise re-derive these work-item conventions; potentially EPIC-008 (cross-references between AC#4's scope-guard rule and EPIC-008's editing-order convention).

### Risks

*Filled during planning. Anticipated areas:*

- Over-formalisation of small rules into heavyweight prose. Mitigation: each rule gets a sentence or paragraph, not a chapter.
- Re-review cascade — additive amendments to `conceptual_design` may trigger alignment-check cascades to lower-authority documents (technical_design, guides). Mitigation: keep amendments truly additive so cascade alignment is automatic.
- Cross-Epic coordination on scratchpad bookkeeping (AC#7) — EPIC-007 and EPIC-008 share one scratchpad source item. Mitigation: whichever Epic approves second performs the move with pointer to both.

---

## Implementation Notes

*Filled during implementation.*

---

## Review

*Filled during review (Gate 3).*

---

## Activity Log

- **2026-05-24** — ELABORATED via Gate 1. Fresh elaboration pass by subagent on the trimmed scope (post-split). Refinements: (1) AC#1 commit-hash sharpened to ordered (i) why work item's own hash is unavailable + (ii) what to cite, with required verbatim phrasing ("upstream commit hash", "reference … by ID") and TASK-003 as the worked example. (2) AC#2 bundled re-elaboration: recommended home is a new sub-bullet on *Flow* step 5 (where gating semantics live); three permission conditions tightened. (3) AC#3 naming convention: recommended home is the *Work Item* entity description in § Domain Model (entity-attribute naming is more natural than a business rule); skill prompt pinpointed to § Process: Plan step 3 (Cascading Preparation — the exact ID-allocation moment). (4) AC#4 scope-guard rule: recommended home is a new § Business Rules entry "Scope-Boundary Acceptance Criteria Use Cross-Type Guards" (companion to *Unified Status Lifecycle*); scope-vs-test-phrasing tension resolved with explicit parenthetical (test is general so transferable; rule is scoped to work-item ACs); skill prompt at § Process: Plan step 2 (AC-drafting bullet). (5) AC#5 Version History entry shape concretised — 4 explicit must-requirements (name EPIC-007 by ID, list amendments by anchor, classify additive, record new Approved date). (6) AC#6 restructured into 4 bidirectional cross-reference pairs; AC#1↔AC#2 made bidirectional (was unidirectional); AC#4↔EPIC-008 editing-order got a justification paragraph showing the cross-reference isn't a stretch ("edit-discipline / AC-writing rules together answering 'what must I edit, in what order, and how do I assert the boundary?'"). (7) AC#7 partial-closure annotation is letter-for-letter the converse of EPIC-008 AC#6 (deterministic second-approver protocol). Trim sound: all 4 EPIC-007 items belong with FEAT-003 work-item conventions; all 3 EPIC-008 items belong with FEAT-008 dev-discipline. FEAT-003 bullet 4 anchor holds completely — every phrase maps to ≥1 AC. Cross-Epic coordination symmetric. Recursive-dogfood: lighter answer chosen — no parallel AC#8 in EPIC-007; the record is single-sourced in EPIC-008 AC#7's knowledge note; Overview adds a record-only paragraph pointing at EPIC-008's record. Findings: all Strength + one minor non-blocking Gap (no coordination signal for which Epic approves first — same Gap EPIC-008 flagged; symmetric and acceptable). Directing party approved without modification.
- **2026-05-24** — Re-scoped: trimmed from seven items to four; sibling EPIC-008 created under new FEAT-008. The original elaboration (in flight 2026-05-23 / 2026-05-24) produced 11 ACs spanning seven items across both work-item and dev-discipline territory. The directing party identified an architectural improvement: *"acknowledge the split now and pay the upfront cost to prevent more cost/re-work later — future learnings will compound when Shannon is used on real projects beyond dogfooding itself."* The split: items 2 (commit-hash), 3 (bundled re-elaboration), 6 (naming convention), 7 (AC scope-guard) stay here under FEAT-003 — renumbered AC#1–AC#4 (cross-cutting ACs renumbered AC#5–AC#7). Items 1 (editing-order), 4 (meta-gap channel), 5 (push-cadence) move to EPIC-008 under FEAT-008. The file was renamed `EPIC-007-framework-conventions-from-dogfooding.md` → `EPIC-007-work-item-conventions-from-dogfooding.md`. FEAT-003 was re-elaborated 2026-05-24 (additive) to add Ideal State bullet 4 — the explicit anchor for this Epic's work-item-conventions promotion (closing the Gate-1 *Anchor Gap* finding from the original elaboration). The pre-split elaboration's *parent-fit Drift* finding (items 1, 5 fit FEAT-003 loosely) is resolved by the split.
- **2026-05-24** — DRAFT: Epic created via `/epic-create` under FEAT-003 (original scope: seven framework-clarification items captured during EPIC-005 / EPIC-006 dogfooding). Original anchor: Vision v2.3 § Core Principles #4 *Adaptive Coherence* (upstream direction). Originally titled "Framework Conventions Surfaced Through Dogfooding"; re-scoped and renamed 2026-05-24.
